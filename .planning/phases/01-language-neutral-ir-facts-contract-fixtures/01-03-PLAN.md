---
phase: 01-language-neutral-ir-facts-contract-fixtures
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - fixtures/fastapi-bookstore/
  - fixtures/flask-bookstore/
  - fixtures/nestjs-bookstore/
  - crates/gnr8-core/tests/snapshot_fastapi_graph.rs
  - crates/gnr8-core/tests/snapshot_fastapi_openapi.rs
  - crates/gnr8-core/tests/snapshot_flask_graph.rs
  - crates/gnr8-core/tests/snapshot_flask_openapi.rs
  - crates/gnr8-core/tests/snapshot_nestjs_graph.rs
  - crates/gnr8-core/tests/snapshot_nestjs_openapi.rs
  - crates/gnr8-core/tests/snapshots/
  - Makefile
  - docs/milestone-v2-multi-language.md
autonomous: true
requirements: [IR-04]
nyquist_compliant: true

must_haves:
  truths:
    - "A FastAPI fixture service exists with type-rich source exercising objects, arrays, cross-language enums (Literal/Enum), unions (Union), and all four optional x nullable combinations."
    - "A Flask fixture service exists encoding the honest typed-envelope acceptance cases (typed DTOs/returns + blueprint-prefix shapes)."
    - "A NestJS fixture service exists with @nestjs/common decorators + DTO classes deriving schema from the source's own TS types (NOT @nestjs/swagger/zod/class-validator)."
    - "Each fixture has committed snapshot(s) RED by design (visibly failing because no extractor exists), authored to the INTENDED neutral graph/OpenAPI shape so Phases 2-5 turn them green with zero snapshot edits."
    - "The red-by-design tests are kept OUT of the green make-check gate while remaining visible and clearly marked; the goalservice green tests are unaffected."
  artifacts:
    - path: "fixtures/fastapi-bookstore/"
      provides: "Static FastAPI service source encoding the v2.0 acceptance vocabulary"
    - path: "fixtures/flask-bookstore/"
      provides: "Static Flask service source (typed-envelope acceptance cases)"
    - path: "fixtures/nestjs-bookstore/"
      provides: "Static NestJS service source (@nestjs/common decorators + DTO classes)"
    - path: "crates/gnr8-core/tests/snapshot_fastapi_graph.rs"
      provides: "RED-by-design graph snapshot test for the FastAPI fixture"
      contains: "build_graph"
  key_links:
    - from: "crates/gnr8-core/tests/snapshot_fastapi_graph.rs"
      to: "fixtures/fastapi-bookstore/"
      via: "FIXTURE_DIR concat to build_graph"
      pattern: "fastapi-bookstore"
    - from: "Makefile"
      to: "the red-by-design tests"
      via: "red tests excluded from the green gate via ignore attribute, visible under -- --ignored"
      pattern: "ignored"
---

<objective>
Author three static, type-rich fixture services — FastAPI, Flask, and NestJS — that encode the v2.0
acceptance cases in each language's OWN type system, and commit RED-by-design snapshots for each that
represent the INTENDED neutral graph/OpenAPI output, so Phases 2-5 flip them green by landing an
extractor (zero snapshot edits). Keep the red set OUT of the green `make check` gate while leaving it
visible and clearly marked as intentional.

Purpose: IR-04 — the multi-language fixtures + red-by-design snapshots ARE the acceptance contract the
later sidecar phases turn green. This phase ships them with no extraction logic (rule 1 / phase
boundary): fixtures are static source only; the red comes honestly from the missing extractor.

Output: `fixtures/{fastapi,flask,nestjs}-bookstore/` source trees; six new
`snapshot_<lang>_{graph,openapi}.rs` tests; committed intended-green `.snap` files that are RED in
Phase 1; a Makefile/test-gate change that keeps `make check` green for everything Phase 1 delivers while
the red contract remains visible.
</objective>

<execution_context>
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/extensibility.md
@docs/milestone-v2-multi-language.md
@.planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-RESEARCH.md
@.planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-PATTERNS.md
@.planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-01-SUMMARY.md
@.planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-02-SUMMARY.md

<interfaces>
<!-- The proven fixture + snapshot harness to mirror. Extracted from the codebase — adapt, do not re-explore. -->

Existing fixture layout (the template) — fixtures/goalservice/:
- go.mod, go.sum                                  (per-fixture language manifest; NEVER linked into gnr8-core)
- internal/common/dto/{common.go,goal.go}         (type-rich source: objects, enum, request/response)
- internal/goal/ports/{handlers.go,http.go}       (routes/handlers)
- expected/{openapi.yaml,diagnostics.txt,sdk/*.go} (reference artifacts — the semantic anchor)

Snapshot test analog — tests/snapshot_graph.rs (now-GREEN Go harness):
- scoped allow for clippy::unwrap_used / expect_used at the test-target top
- const FIXTURE_DIR: &str = concat!(env!("CARGO_MANIFEST_DIR"), "/../../fixtures/goalservice");
- a #[test] that calls gnr8_core::analyze::build_graph(FIXTURE_DIR).expect(...) then
  insta::assert_yaml_snapshot!("goalservice_graph", graph);

OpenAPI test analog — tests/snapshot_openapi.rs:
- fixture_security() returns Vec<gnr8_core::graph::SecurityScheme> { one ApiKeyAuth / X-API-Key } (code-as-config, rule 4)
- gnr8_core::lower::to_openapi(&graph, "<name>", "/<base>", &fixture_security()).expect(...)
- insta::assert_snapshot!("<name>_openapi", openapi);   (plain text)

insta conventions: assert_yaml_snapshot! for graphs (structured); assert_snapshot! for OpenAPI text.
INSTA_UPDATE=no under CI=true (a red snapshot never auto-flips green).

Makefile gate reality (CRITICAL — read the Makefile fully before editing):
- `test:` runs `cargo test --all-features` (RUNS EVERYTHING — a plain non-ignored red fixture test would break this).
- `gates:` runs an EXPLICIT test list: snapshot_graph/diagnostics/openapi/sdk, determinism, sdk_compile,
  sdk_pipeline, lifecycle. The new red-by-design tests must NOT join that list AND must not break `test:`.
- The Makefile header currently says "the red-by-design era is OVER" — this plan reintroduces a CONTROLLED
  red set; update that comment + the gate so the red tests are skipped by default but runnable + visible
  (the #[ignore] attribute makes `cargo test` skip them while listing them; `-- --ignored` runs them).

The neutral Type enum + optional/nullable axes + the SchemaObject [T,"null"] rendering are defined by
Plan 01 + Plan 02 — read 01-01-SUMMARY.md / 01-02-SUMMARY.md for the exact variant names + shapes the
intended-green snapshots must encode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the FastAPI fixture service + its RED-by-design graph and OpenAPI snapshots</name>
  <files>fixtures/fastapi-bookstore/, crates/gnr8-core/tests/snapshot_fastapi_graph.rs, crates/gnr8-core/tests/snapshot_fastapi_openapi.rs, crates/gnr8-core/tests/snapshots/</files>
  <read_first>
    - fixtures/goalservice/ (the template tree — common.go/goal.go object/enum/request/response shape; expected/openapi.yaml convention)
    - crates/gnr8-core/tests/snapshot_graph.rs and tests/snapshot_openapi.rs (the exact harness to copy + retarget)
    - docs/milestone-v2-multi-language.md (the v2.0 acceptance cases the fixtures must encode)
    - .planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-PATTERNS.md (fixtures section: all four optional/nullable combos, unions, Literal; rule 1 bright line)
    - .planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-01-SUMMARY.md (neutral Type variant names the intended snapshot must encode)
  </read_first>
  <action>
    Create `fixtures/fastapi-bookstore/` as STATIC, type-rich FastAPI source (NO pip install, NO running app, NO
    extractor this phase — RESEARCH A2). Encode the v2.0 acceptance vocabulary in Python's OWN type system: objects
    (Pydantic models and/or @dataclass DTOs), arrays/lists (list[T]), cross-language enums (Python Literal["a","b"]
    AND/OR enum.Enum), unions (Union[A, B] / A | B), and ALL FOUR optional/nullable combinations encoded distinctly:
    a field with a default (optional, not-null), Optional[T] / T | None (nullable), Optional[T] with a default
    (optional AND nullable), and a plain required T (neither). Document in code comments which field encodes which
    axis pair. Include FastAPI routes (path + query params, typed request body, response_model, status codes)
    covering the PYSRC acceptance surface. Add a requirements.txt declaring fastapi ONLY as fixture-local
    documentation of the framework (NEVER installed/linked into gnr8-core this phase; do NOT run pip).

    RULE 1 (non-negotiable): the facts must be expressible from function signatures + Pydantic/dataclass annotations
    alone. Do NOT shape the fixture so it would require reading FastAPI's runtime /openapi.json or any
    @nestjs/swagger-style tool. No code anywhere assumes a third-party schema tool.

    Create `crates/gnr8-core/tests/snapshot_fastapi_graph.rs` and `snapshot_fastapi_openapi.rs` by copying the
    goalservice harness, retargeting FIXTURE_DIR to .../fixtures/fastapi-bookstore and the snapshot names to
    fastapi_graph / fastapi_openapi. Mark BOTH tests with the ignore attribute carrying the reason string
    "red-by-design: pyextract lands in Phase 2; intended-green snapshot is the acceptance contract" so
    `cargo test --all-features` does not fail on them while they stay visible/runnable via `cargo test -- --ignored`.
    Keep the honest .expect("pyextract lands in Phase 2 — intentionally red until then") (build_graph cannot produce a
    Python graph yet). Add a doc-comment header on each test explaining the RED intent (mirror snapshot_graph.rs's
    header style).

    Author the committed .snap files (tests/snapshots/snapshot_fastapi_graph__fastapi_graph.snap and the OpenAPI one)
    BY HAND to the INTENDED neutral graph/OpenAPI shape — the exact neutral Type enum shape (objects, arrays,
    Type::Enum, Type::Union, optional/nullable rendered per Plan 01/02, [T,"null"] in OpenAPI) the Phase-2 extractor
    MUST produce. Ground every field/type in the fixture source; use relativized, sorted, neutral shapes (no
    machine-absolute paths, no language proper nouns in the IR portion). The ignore attribute keeps the gate green;
    running -- --ignored shows the honest red at the .expect().
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test snapshot_fastapi_graph --test snapshot_fastapi_openapi</automated>
  </verify>
  <acceptance_criteria>
    - `find fixtures/fastapi-bookstore -name '*.py'` returns at least one file.
    - `grep -rnE 'Optional\[|\| None' fixtures/fastapi-bookstore` and `grep -rnE 'Literal\[|Enum' fixtures/fastapi-bookstore` and `grep -rn 'Union\[' fixtures/fastapi-bookstore` each return >= 1 match (all axes + unions + enums present).
    - `grep -rniE 'swagger|class-validator|zod' fixtures/fastapi-bookstore` returns no match (rule 1).
    - `grep -c 'red-by-design' crates/gnr8-core/tests/snapshot_fastapi_graph.rs crates/gnr8-core/tests/snapshot_fastapi_openapi.rs` totals >= 2.
    - `cargo test -p gnr8-core --test snapshot_fastapi_graph --test snapshot_fastapi_openapi` reports the tests as ignored with 0 failures (green gate intact).
    - `cargo test -p gnr8-core --test snapshot_fastapi_graph -- --ignored` FAILS at the .expect() (honest red); `test -s crates/gnr8-core/tests/snapshots/snapshot_fastapi_graph__fastapi_graph.snap` confirms the committed snapshot is non-empty.
  </acceptance_criteria>
  <done>The FastAPI fixture encodes the full v2.0 acceptance vocabulary from Python's own types (no third-party-schema coupling); both snapshot tests are ignored-but-visible red-by-design with committed intended-green .snap files grounded in the neutral IR shape.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Flask + NestJS fixture services + their RED-by-design snapshots</name>
  <files>fixtures/flask-bookstore/, fixtures/nestjs-bookstore/, crates/gnr8-core/tests/snapshot_flask_graph.rs, crates/gnr8-core/tests/snapshot_flask_openapi.rs, crates/gnr8-core/tests/snapshot_nestjs_graph.rs, crates/gnr8-core/tests/snapshot_nestjs_openapi.rs, crates/gnr8-core/tests/snapshots/</files>
  <read_first>
    - fixtures/goalservice/ (the template tree)
    - fixtures/fastapi-bookstore/ and crates/gnr8-core/tests/snapshot_fastapi_graph.rs (the pattern established in Task 1 — mirror it)
    - docs/milestone-v2-multi-language.md (Flask typed-envelope + NestJS class-DTO acceptance cases)
    - .planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-PATTERNS.md (fixtures section + rule 1 bright line for NestJS)
    - .planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-01-SUMMARY.md (neutral Type variant names)
  </read_first>
  <action>
    Create `fixtures/flask-bookstore/` as STATIC, type-rich Flask source encoding the HONEST second-class
    typed-envelope (PYSRC-02): routes with blueprint prefixes, opt-in typed DTOs/@dataclass returns, typed request
    envelopes. Exercise objects, arrays, an enum (Literal/Enum), a union, and the optional/nullable combinations the
    typed envelope can express. Document where Flask is genuinely untyped (those become diagnostics in Phase 2, NOT
    guessed facts — encode the fixture so the honest-envelope limits are visible). Declare flask in a fixture-local
    requirements.txt (never installed this phase).

    Create `fixtures/nestjs-bookstore/` as STATIC, type-rich NestJS source: @nestjs/common decorators (@Controller,
    @Get/@Post, @Param/@Query/@Body) + DTO CLASSES whose schema derives from the source's OWN TS types
    (interfaces/classes, string-literal-union enums 'a' | 'b', A | B unions, field?: T optional vs field: T | null
    nullable vs field?: T | null both). RULE 1 (non-negotiable, BRIGHT LINE): derive nothing from @nestjs/swagger,
    zod, or class-validator — the DTOs carry plain TS types, NOT @ApiProperty/zod-schema/class-validator decorators.
    Declare @nestjs/common in a fixture-local package.json (never npm-installed this phase).

    For EACH of the two fixtures, create the graph + OpenAPI snapshot tests mirroring Task 1:
    snapshot_flask_graph.rs, snapshot_flask_openapi.rs, snapshot_nestjs_graph.rs, snapshot_nestjs_openapi.rs —
    retargeted FIXTURE_DIR, retargeted snapshot names, the ignore attribute with reason "red-by-design:
    <py/ts>extract lands in Phase <2/4>; intended-green snapshot is the acceptance contract", the honest .expect()
    (build_graph has no py/ts extractor yet), and a RED-intent doc header. Author the committed intended-green .snap
    files BY HAND to the neutral IR shape (objects/arrays/Type::Enum/Type::Union/optional/nullable + [T,"null"]
    OpenAPI), grounded in each fixture's source, relativized/sorted/neutral. Use the same fixture_security()
    code-as-config helper shape for the OpenAPI tests (rule 4 — security supplied, never scraped).
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test snapshot_flask_graph --test snapshot_nestjs_graph</automated>
  </verify>
  <acceptance_criteria>
    - `find fixtures/flask-bookstore -name '*.py'` and `find fixtures/nestjs-bookstore -name '*.ts'` each return >= 1 file.
    - `grep -rn 'Blueprint' fixtures/flask-bookstore` returns >= 1 match (blueprint-prefix shape encoded).
    - `grep -rnE '@Controller|@Get|@Post' fixtures/nestjs-bookstore` returns >= 1 match (@nestjs/common decorators) AND `grep -rniE 'swagger|class-validator|ApiProperty|zod' fixtures/nestjs-bookstore` returns no match (rule 1 bright line).
    - `grep -rnE '\?: |: [A-Za-z]+ \| null|\| null' fixtures/nestjs-bookstore` returns >= 1 match (optional vs nullable distinct in TS).
    - All four new snapshot tests carry the ignore attribute with a "red-by-design" reason (`grep -lc 'red-by-design' crates/gnr8-core/tests/snapshot_flask_graph.rs crates/gnr8-core/tests/snapshot_flask_openapi.rs crates/gnr8-core/tests/snapshot_nestjs_graph.rs crates/gnr8-core/tests/snapshot_nestjs_openapi.rs` shows all four files match).
    - `cargo test -p gnr8-core --test snapshot_flask_graph --test snapshot_nestjs_graph` reports ignored with 0 failures; running either with `-- --ignored` FAILS at the .expect() (honest red); each committed .snap exists and is non-empty.
  </acceptance_criteria>
  <done>The Flask and NestJS fixtures encode their acceptance cases from each language's own types (NestJS with no third-party-schema coupling); all four snapshot tests are ignored-but-visible red-by-design with committed intended-green .snap files.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the red set out of the green gate, keep it visible, and prove the full Phase-1 contract</name>
  <files>Makefile, docs/milestone-v2-multi-language.md</files>
  <read_first>
    - Makefile (the test / gates / check targets + the "red-by-design era is OVER" header — the file being modified)
    - crates/gnr8-core/tests/snapshot_fastapi_graph.rs (the ignore-attribute pattern from Tasks 1-2)
    - .planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-VALIDATION.md (Sampling Rate + the requirement that make check stays green except the red-by-design set)
    - docs/milestone-v2-multi-language.md (where to record the red-by-design acceptance-contract note)
  </read_first>
  <action>
    Update the Makefile so the green gate (`make check` -> fmt-check, clippy, test, fixture-build, goextract-build)
    stays GREEN for everything Phase 1 delivers while the red-by-design set remains visible and runnable:
    - The six new red tests use the ignore attribute (from Tasks 1-2), so `cargo test --all-features` (the `test:`
      target) already SKIPS them — confirm the `test:` target stays green and does NOT add them to any forced-run
      list.
    - Do NOT add the new snapshot_<lang>_* tests to the explicit `gates:` test list (they must not block CI).
    - Rewrite the Makefile header comment that claims "the red-by-design era is OVER" to accurately describe the
      reintroduced controlled red set: Phase 1 ships intended-green acceptance snapshots for FastAPI/Flask/NestJS that
      are RED by design (run with `cargo test -p gnr8-core --test snapshot_fastapi_graph -- --ignored`, etc.) and turn
      green in Phases 2/4 when the extractors land. Add an optional convenience target (e.g. `red:` /
      `acceptance-red:`) that runs the six ignored tests via `-- --ignored` so reviewers can see the honest red on
      demand.

    Record the red-by-design acceptance contract in docs/milestone-v2-multi-language.md: a short section listing the
    three fixtures, their six snapshots, which phase turns each green, and the "run -- --ignored to see the honest
    red" instruction. This is the human-readable index of the acceptance contract.

    Finally, prove the full Phase-1 contract end to end: `make check` is GREEN (all Phase-1 deliverables — neutral
    enum, consumers, goalservice regression — pass; the six red tests are skipped, not failing). Then run the red set
    explicitly and confirm all six FAIL honestly at the .expect() (the missing extractor), with each committed .snap
    present. Run clippy across the workspace.
  </action>
  <verify>
    <automated>make check</automated>
  </verify>
  <acceptance_criteria>
    - `make check` exits 0 (the full green gate passes; the six red tests are skipped via the ignore attribute, not failing, and the goalservice green tests are unaffected).
    - The Makefile no longer asserts the red-by-design era is over: `grep -n 'red-by-design era is OVER' Makefile` returns no match; a comment describing the reintroduced controlled red set is present (`grep -c 'red-by-design' Makefile` >= 1).
    - The six red tests are NOT in the `gates:` explicit list: `grep -nE 'snapshot_(fastapi|flask|nestjs)' Makefile` shows them only in a dedicated red/acceptance target (if any), never in `gates:`.
    - Running the six tests with `-- --ignored` makes each FAIL at the .expect() (honest red): `cargo test -p gnr8-core --test snapshot_fastapi_graph --test snapshot_fastapi_openapi --test snapshot_flask_graph --test snapshot_flask_openapi --test snapshot_nestjs_graph --test snapshot_nestjs_openapi -- --ignored` reports 6 failures (0 passes).
    - docs/milestone-v2-multi-language.md contains the acceptance-contract section: `grep -niE 'red-by-design|acceptance contract' docs/milestone-v2-multi-language.md` returns >= 1 match.
    - `cargo clippy --workspace --all-targets --locked -- -D warnings` is clean.
  </acceptance_criteria>
  <done>make check is green for all Phase-1 deliverables with the goalservice tests unaffected; the six red-by-design fixture snapshots are skipped-but-visible, fail honestly under -- --ignored, and the acceptance contract is documented in the milestone brief and Makefile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static fixture source files -> repo | Fixtures are static source only; NO interpreter/compiler/extractor runs against them this phase. No code-execution boundary is crossed (RESEARCH A2). |
| third-party-schema tools -> gnr8 | RULE 1 bright line: fixtures must NOT be shaped so any later extractor would need to read @nestjs/swagger / zod / class-validator / marshmallow / FastAPI runtime /openapi.json. Enforced by grep gates in Tasks 1-2. |
| sidecar facts JSON -> Rust host | The red snapshot tests call build_graph, which still routes through the strict-deserialize boundary; for the new fixtures build_graph honestly fails (no extractor) rather than accepting fabricated facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | fixture shaped to require a forbidden third-party schema tool (rule-1 violation) | mitigate | Tasks 1-2 grep gates reject swagger/zod/class-validator/ApiProperty in the NestJS + Python fixtures; the fixture facts must be expressible from each language's own types only. |
| T-01-07 | Tampering / false-green | a red snapshot that flips green by accident (empty/trivial) or can never match (machine-absolute paths) | mitigate | Snapshots authored to the intended-green neutral IR shape, grounded in source; relativized/sorted/neutral; the honest red is the .expect() panic from the missing extractor, and a manual review confirms intended-red (VALIDATION Manual-Only check). |
| T-01-08 | Denial of Service / gate-breakage | red tests breaking `make check` and blocking all CI | mitigate | Task 3 keeps the six tests behind the ignore attribute (skipped by `cargo test`, never in the gates list) so the green gate is unaffected while the red stays visible. |

No code execution is introduced and no new runtime input surface is added (fixtures are inert source). No `high`-severity threats.
</threat_model>

<verification>
- `make check` is GREEN (all Phase-1 deliverables pass; the six red tests are skipped via the ignore attribute; goalservice green tests unaffected).
- The six `snapshot_<lang>_{graph,openapi}` tests FAIL honestly at the `.expect()` only when run with `-- --ignored` (the red-by-design acceptance contract).
- Each committed `.snap` exists, is non-empty, and encodes the intended-green neutral IR shape (no machine-absolute paths, no language proper nouns in the IR portion).
- Rule-1 grep gates pass: no swagger/zod/class-validator/ApiProperty coupling in any fixture.
- `cargo clippy --workspace --all-targets --locked -- -D warnings` is clean.
</verification>

<success_criteria>
- FastAPI, Flask, and NestJS fixture services exist as static, type-rich source encoding the v2.0 acceptance cases in each language's own type system, with no third-party-schema coupling (IR-04 / rule 1).
- Six committed red-by-design snapshots (graph + OpenAPI per fixture) encode the intended neutral graph/OpenAPI output and are visibly failing before any extraction lands (IR-04).
- The red set is excluded from the green `make check` gate while remaining visible and runnable; the existing goalservice green tests are unaffected (IR-04 / VALIDATION).
- The acceptance contract is recorded in docs/milestone-v2-multi-language.md and the Makefile.
</success_criteria>

<output>
Create `.planning/phases/01-language-neutral-ir-facts-contract-fixtures/01-03-SUMMARY.md` when done.
Record: the chosen fixture directory names + layout, exactly which fixture field encodes which optional/nullable
axis pair (per language), the six snapshot file names + which phase turns each green, the Makefile gate change, and
the result of running the red set under -- --ignored (six honest failures).
</output>
