---
phase: 06-cross-language-hardening-examples-docs
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - examples/fastapi-bookstore/app/__init__.py
  - examples/fastapi-bookstore/app/main.py
  - examples/fastapi-bookstore/app/models.py
  - examples/fastapi-bookstore/.gnr8/Cargo.toml
  - examples/fastapi-bookstore/.gnr8/Cargo.lock
  - examples/fastapi-bookstore/.gnr8/.gitignore
  - examples/fastapi-bookstore/.gnr8/src/main.rs
  - examples/fastapi-bookstore/README.md
  - examples/fastapi-bookstore/generated/openapi.yaml
  - examples/fastapi-bookstore/generated/sdk
  - examples/nestjs-bookstore/src/books.controller.ts
  - examples/nestjs-bookstore/src/books.dto.ts
  - examples/nestjs-bookstore/.gnr8/Cargo.toml
  - examples/nestjs-bookstore/.gnr8/Cargo.lock
  - examples/nestjs-bookstore/.gnr8/.gitignore
  - examples/nestjs-bookstore/.gnr8/src/main.rs
  - examples/nestjs-bookstore/README.md
  - examples/nestjs-bookstore/generated/openapi.yaml
  - examples/nestjs-bookstore/generated/sdk
  - Makefile
autonomous: true
requirements: [XLANG-01, XLANG-02, XLANG-05]
must_haves:
  truths:
    - "A developer runs gnr8 generate in examples/fastapi-bookstore/ and gets OpenAPI 3.1 + a Python SDK from a .gnr8/ lifecycle"
    - "A developer runs gnr8 generate in examples/nestjs-bookstore/ and gets OpenAPI 3.1 + a TS SDK from a .gnr8/ lifecycle (no node_modules needed)"
    - "The committed examples/*/generated/ output equals a fresh gnr8 generate byte-for-byte (gnr8 check exits 0)"
    - "make examples-check regenerates and diffs all three examples (Go/Python/TS) and is wired into the green gate"
  artifacts:
    - path: "examples/fastapi-bookstore/.gnr8/src/main.rs"
      provides: "The FastAPI example Pipeline (FastApi → OpenApi31 + PySdk) — config is code"
      contains: "FastApi"
    - path: "examples/nestjs-bookstore/.gnr8/src/main.rs"
      provides: "The NestJS example Pipeline (NestJs → OpenApi31 + TsSdk) — config is code"
      contains: "NestJs"
    - path: "examples/fastapi-bookstore/generated/openapi.yaml"
      provides: "REAL committed gnr8 generate output (OpenAPI 3.1) for the FastAPI example"
    - path: "examples/nestjs-bookstore/generated/openapi.yaml"
      provides: "REAL committed gnr8 generate output (OpenAPI 3.1) for the NestJS example"
    - path: "Makefile"
      provides: "examples-check regen-and-diff target wired into check"
      contains: "examples-check"
  key_links:
    - from: "examples/fastapi-bookstore/.gnr8/src/main.rs"
      to: "examples/fastapi-bookstore/app"
      via: "FastApi::new().inputs([\"app\"]) resolved vs project_root"
      pattern: "FastApi::new\\(\\)\\.inputs"
    - from: "examples/nestjs-bookstore/.gnr8/src/main.rs"
      to: "examples/nestjs-bookstore/src"
      via: "NestJs::new().inputs([\"src\"]) resolved vs project_root"
      pattern: "NestJs::new\\(\\)\\.inputs"
    - from: "Makefile examples-check"
      to: "examples/*/generated"
      via: "gnr8 generate && gnr8 check per example"
      pattern: "examples-check"
---

<objective>
Author two end-to-end examples — `examples/fastapi-bookstore/` (XLANG-01) and
`examples/nestjs-bookstore/` (XLANG-02) — each a self-contained project mirroring the v1
`examples/bookstore/` Go pattern: copied source app + a `.gnr8/` Rust Pipeline crate + REAL committed
`generated/` output. Wire a cross-language `make examples-check` regen-and-diff gate into `make check`
(the determinism proof, XLANG-05).

Purpose: Prove the multi-language pipeline works end-to-end from a real `.gnr8/` lifecycle, and make the
committed example output the enforced byte-identical determinism proof.

Output: Two new `examples/` projects with committed OpenAPI 3.1 + per-language SDK output, plus a
`make examples-check` target that regenerates and diffs all three examples (Go/Python/TS).
</objective>

<execution_context>
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-language-hardening-examples-docs/06-RESEARCH.md
@.planning/phases/06-cross-language-hardening-examples-docs/06-PATTERNS.md
@examples/bookstore/.gnr8/src/main.rs
@examples/bookstore/.gnr8/Cargo.toml
@examples/bookstore/.gnr8/.gitignore
@examples/bookstore/README.md

<interfaces>
<!-- Verified built-in signatures (crates/gnr8-core/src/sdk/builtins.rs) — use directly. -->
- `FastApi::new()` (l.100) → `.inputs<I,S>(...)` (l.106).
- `NestJs::new()` (l.218) → `.inputs(...)` (l.224).
- `PySdk::new()` (l.568) → `.module(impl Into<String>)` (l.579) → `.to(dir)` (l.586).
- `TsSdk::new()` (l.663) → `.module(...)` (l.674) → `.to(...)` (l.681).
- `OpenApi31::new()` (l.415) → `.to(path)` (l.423).
- Prelude (crates/gnr8-core/src/sdk/mod.rs:337-342) re-exports `FastApi, NestJs, PySdk, TsSdk, OpenApi31,
  SetBasePath, SetTitle, Header, Pipeline` — the single `use gnr8_core::sdk::prelude::*;` glob suffices.
- `gnr8_core::runner::run(Pipeline)` is the `.gnr8/` child entry (parses `__emit`/`__inspect` from argv).

Source apps to COPY (do NOT point .gnr8/ at fixtures/ — keep examples self-contained, RESEARCH A2):
- fixtures/fastapi-bookstore/app/{__init__.py, main.py, models.py} → examples/fastapi-bookstore/app/
- fixtures/nestjs-bookstore/src/{books.controller.ts, books.dto.ts} → examples/nestjs-bookstore/src/

Makefile (current, verified):
- `.PHONY: fmt fmt-check clippy test gates fixture-build goextract-build red check all` (l.21)
- `fixture-build:` (l.64) — cd into a dir + run a toolchain command (the cd-and-run analog).
- `check: fmt-check clippy test fixture-build goextract-build` (l.82).
- `gnr8 check` already exits 1 on drift (crates/gnr8/src/main.rs:148 run_check) — it IS the regen-diff.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the FastAPI example (copied app + .gnr8/ crate + committed generated output)</name>
  <files>examples/fastapi-bookstore/app/, examples/fastapi-bookstore/.gnr8/Cargo.toml, examples/fastapi-bookstore/.gnr8/Cargo.lock, examples/fastapi-bookstore/.gnr8/.gitignore, examples/fastapi-bookstore/.gnr8/src/main.rs, examples/fastapi-bookstore/README.md, examples/fastapi-bookstore/generated/</files>
  <read_first>
    - examples/bookstore/.gnr8/src/main.rs (the verbatim v1 Pipeline pattern, l.23-36; the doc-comment header style l.1-21)
    - examples/bookstore/.gnr8/Cargo.toml (name + the `../../../crates/gnr8-core` path dep + empty `[workspace]`)
    - examples/bookstore/.gnr8/.gitignore (copy verbatim) + examples/bookstore/README.md (mirror the sectioning)
    - fixtures/fastapi-bookstore/app/{__init__.py, main.py, models.py} (the SOURCE to copy)
    - .planning/phases/02-*/02-03-SUMMARY.md (the proven FastAPI extraction — confirm the base-path/routes match)
    - 06-PATTERNS.md §"examples/fastapi-bookstore/.gnr8/src/main.rs" (the FastAPI adaptation) + §"Cargo.toml" + §".gitignore" + §"README.md" + §"generated/*" (REAL bytes, never hand-write) + Pitfall 2 (inputs(["app"]) scoping) + Pitfall 4 (structural sanity-check vs fixture snapshot)
  </read_first>
  <action>
    Create `examples/fastapi-bookstore/` as a self-contained project mirroring `examples/bookstore/`:
    (1) COPY `fixtures/fastapi-bookstore/app/{__init__.py, main.py, models.py}` into
    `examples/fastapi-bookstore/app/` verbatim (the static source; never installed/run). (2) Create the
    `.gnr8/` crate: `Cargo.toml` (name `fastapi-bookstore-gnr8-gen`, the IDENTICAL `../../../crates/gnr8-core`
    path dep, the required empty `[workspace]` table), the verbatim `.gitignore` (`/cache/` + `/target/`), and
    `src/main.rs` composing `Pipeline::new().source(FastApi::new().inputs(["app"]))` — scope to the `app/` subdir
    (Pitfall 2) — `.transform(SetBasePath::new(...))` + `.transform(SetTitle::new("Bookstore API"))` +
    `.target(OpenApi31::new().to("generated/openapi.yaml"))` + `.target(PySdk::new().module("example.com/bookstore/sdk").to("generated/sdk"))` +
    `.post(Header::generated())`, handed to `gnr8_core::runner::run`. Confirm the exact base-path string against
    the committed fixtures/fastapi-bookstore snapshot (APIRouter prefix as a base path, rule 1) — do NOT add
    `ApplySecurity` unless the fixture has an auth scheme (it does not). Mirror the bookstore doc-comment header
    adapted to Python/`@app`. (3) Build the gnr8 binary (`cargo build --release -p gnr8`), run
    `gnr8 generate` IN the example dir to produce REAL output, and COMMIT `generated/openapi.yaml` +
    `generated/sdk/*.py` (NEVER hand-write the bytes, Pitfall 4). Commit `.gnr8/Cargo.lock` (mirror bookstore).
    Structurally sanity-check the generated OpenAPI against the fixtures/fastapi-bookstore snapshot (same
    routes/schemas/refs). No OSS dep, no `pip install`, the app is never executed (static `ast`).
  </action>
  <acceptance_criteria>
    - `cargo build --release -p gnr8` succeeds; running gnr8 generate in the example then `gnr8 check` exits 0
      (committed output == fresh regen): `cd examples/fastapi-bookstore && <gnr8> generate && <gnr8> check` → exit 0.
    - `examples/fastapi-bookstore/generated/openapi.yaml` exists and contains `openapi: 3.1` (real output).
    - `examples/fastapi-bookstore/generated/sdk/` contains `*.py` files (PySdk output).
    - `grep -c 'FastApi::new()' examples/fastapi-bookstore/.gnr8/src/main.rs` == 1 and
      `grep -c 'PySdk::new()' examples/fastapi-bookstore/.gnr8/src/main.rs` == 1; no `GoSdk`/`TsSdk` present.
    - `examples/fastapi-bookstore/.gnr8/Cargo.toml` has `name = "fastapi-bookstore-gnr8-gen"`, the
      `../../../crates/gnr8-core` path dep, and an empty `[workspace]` table.
    - Determinism: a SECOND `gnr8 generate` + `gnr8 check` is still exit 0 (byte-identical).
    - No new dependency added to gnr8-core: `git diff --exit-code crates/gnr8-core/Cargo.toml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --release -p gnr8 >/dev/null 2>&1 && (cd examples/fastapi-bookstore && "$PWD/../../target/release/gnr8" generate && "$PWD/../../target/release/gnr8" check)</automated>
  </verify>
  <done>
    `examples/fastapi-bookstore/` is a self-contained example (copied app + `.gnr8/` Pipeline crate +
    committed REAL OpenAPI 3.1 + Python SDK) that regenerates byte-identically; XLANG-01 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the NestJS example (copied src + .gnr8/ crate + committed generated output, no node_modules)</name>
  <files>examples/nestjs-bookstore/src/, examples/nestjs-bookstore/.gnr8/Cargo.toml, examples/nestjs-bookstore/.gnr8/Cargo.lock, examples/nestjs-bookstore/.gnr8/.gitignore, examples/nestjs-bookstore/.gnr8/src/main.rs, examples/nestjs-bookstore/README.md, examples/nestjs-bookstore/generated/</files>
  <read_first>
    - examples/bookstore/.gnr8/src/main.rs (the verbatim v1 Pipeline pattern) + the .gnr8/ Cargo.toml/.gitignore/README analogs
    - examples/fastapi-bookstore/.gnr8/src/main.rs (the Python twin authored in Task 1 — the immediate same-shape analog)
    - fixtures/nestjs-bookstore/src/{books.controller.ts, books.dto.ts} (the SOURCE to copy)
    - .planning/phases/04-*/04-03-SUMMARY.md (the proven NestJS extraction — confirm @Controller prefix/routes match)
    - 06-PATTERNS.md §"NestJS adaptation" (NestJs::new().inputs(["src"]) + TsSdk) + §"README.md" (NOTE: NO node_modules needed) + Pitfall 4 + RESEARCH A5 (static extraction reads .ts types only — no node_modules)
  </read_first>
  <action>
    Create `examples/nestjs-bookstore/` mirroring Task 1's shape (and `examples/bookstore/`): (1) COPY
    `fixtures/nestjs-bookstore/src/{books.controller.ts, books.dto.ts}` into `examples/nestjs-bookstore/src/`
    verbatim — do NOT copy `package.json`/`node_modules` (static extraction reads `.ts` source types only;
    `@nestjs/common` imports are treated as unresolved decorators by design, RESEARCH A5). (2) Create the
    `.gnr8/` crate: `Cargo.toml` (name `nestjs-bookstore-gnr8-gen`, identical path dep, empty `[workspace]`),
    verbatim `.gitignore`, and `src/main.rs` with
    `Pipeline::new().source(NestJs::new().inputs(["src"]))` (scope to `src/`, Pitfall 2) +
    `.transform(SetBasePath::new(...))` (confirm string vs the fixtures/nestjs snapshot — `@Controller('books')`
    is a base path, never folded, rule 1) + `.transform(SetTitle::new("Bookstore API"))` +
    `.target(OpenApi31::new().to("generated/openapi.yaml"))` +
    `.target(TsSdk::new().module("example.com/bookstore/sdk").to("generated/sdk"))` + `.post(Header::generated())`.
    Mirror the doc-comment header adapted to TS/`@Controller`; the README MUST state that NO `node_modules` is
    needed. (3) Run the release gnr8 `generate` in the example dir to produce REAL output and COMMIT
    `generated/openapi.yaml` + `generated/sdk/*.ts` (never hand-write, Pitfall 4). Commit `.gnr8/Cargo.lock`.
    Structurally sanity-check the OpenAPI vs the fixtures/nestjs snapshot (same routes/schemas/refs). No OSS dep;
    the vendored typescript is the existing carve-out (tsextract reads source types — no install here).
  </action>
  <acceptance_criteria>
    - `cd examples/nestjs-bookstore && <gnr8> generate && <gnr8> check` exits 0 (committed == fresh regen).
    - `examples/nestjs-bookstore/generated/openapi.yaml` exists and contains `openapi: 3.1`.
    - `examples/nestjs-bookstore/generated/sdk/` contains `*.ts` files (TsSdk output).
    - `grep -c 'NestJs::new()' examples/nestjs-bookstore/.gnr8/src/main.rs` == 1 and
      `grep -c 'TsSdk::new()' examples/nestjs-bookstore/.gnr8/src/main.rs` == 1; no `GoSdk`/`PySdk`.
    - No node_modules committed: `test ! -e examples/nestjs-bookstore/node_modules` (true) and
      `grep -ri 'node_modules' examples/nestjs-bookstore/README.md` shows the "no node_modules needed" note.
    - Determinism: a second `gnr8 generate` + `gnr8 check` is still exit 0.
    - No new dependency: `git diff --exit-code crates/gnr8-core/Cargo.toml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>(cd examples/nestjs-bookstore && "$PWD/../../target/release/gnr8" generate && "$PWD/../../target/release/gnr8" check)</automated>
  </verify>
  <done>
    `examples/nestjs-bookstore/` is a self-contained example (copied src + `.gnr8/` Pipeline crate +
    committed REAL OpenAPI 3.1 + TS SDK, no node_modules) that regenerates byte-identically; XLANG-02 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the cross-language examples-check regen-and-diff gate into make check</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (the `.PHONY` line l.21; `fixture-build:` l.64 — the cd-and-run analog; `goextract-build:` l.70; `check:` l.82; `gates:` l.57)
    - examples/bookstore/ (the Go example — the third example the gate must cover)
    - examples/fastapi-bookstore/ + examples/nestjs-bookstore/ (the two new examples from Tasks 1-2)
    - 06-PATTERNS.md §"Makefile (build gate, ADD examples-check)" (the cd-and-run shape; build-binary vs cargo-run decision) + Pitfall 6 (make check does not gate examples today) + RESEARCH Open Q5 (build the release binary; go not on default PATH — MEMORY)
  </read_first>
  <action>
    Add an `examples-check` target to the `Makefile` mirroring the `fixture-build:` cd-and-run shape. Per
    RESEARCH Open Q5, build the release gnr8 binary once (`cargo build --release -p gnr8`) and invoke it (NOT a
    bespoke compare script — `gnr8 check` already exits 1 on drift; rule 2 / Don't-Hand-Roll). For each of the
    three examples (`examples/bookstore` Go, `examples/fastapi-bookstore` Python, `examples/nestjs-bookstore` TS),
    `cd` into it and run `<release-gnr8> generate` then `<release-gnr8> check` (non-zero exit on drift fails the
    gate — this is the cross-language byte-identical determinism proof, XLANG-05). Ensure `go` is on PATH for the
    Go example (MEMORY: go not on default PATH in this sandbox; source the toolchain PATH or set it in the
    recipe). Add `examples-check` to `.PHONY` (l.21) and as a prerequisite of `check:` (l.82). Do not break the
    existing `fixture-build`/`goextract-build` prerequisites. No OSS dep, no new tool.
  </action>
  <acceptance_criteria>
    - `grep -c 'examples-check' Makefile` >= 2 (the target definition + the `check:` prerequisite + `.PHONY`).
    - `make examples-check` runs to completion exit 0: all three examples regenerate byte-identically
      (each example's `gnr8 check` exits 0).
    - The target references all three example dirs: `grep -E 'examples/bookstore|examples/fastapi-bookstore|examples/nestjs-bookstore' Makefile` shows all three.
    - `examples-check` is a prerequisite of `check`: `grep -n '^check:' Makefile` line includes `examples-check`.
    - `make check` is green end-to-end (fmt + clippy + test + fixture builds + examples-check).
    - No new dependency: `git diff --exit-code crates/gnr8-core/Cargo.toml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>make examples-check 2>&1 | tail -20</automated>
  </verify>
  <done>
    `make examples-check` regenerates and diffs all three examples (Go/Python/TS) via the release gnr8 binary's
    own `check`, is wired into `make check`, and proves cross-language byte-identical determinism; XLANG-05
    determinism gate satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed artifact ↔ fresh regen | The committed `examples/*/generated/` bytes are trusted only insofar as they equal a fresh `gnr8 generate`; the gate enforces equality. |
| `.gnr8/` child crate → sidecars | The example Pipeline crate spawns `python3`/`node` sidecars (via build_graph) with discrete args over the copied static source. |
| Static example source → analysis | The copied `app/`/`src/` is parsed statically; the example app is NEVER executed/imported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | committed `examples/*/generated/` output | mitigate | Output is produced ONLY by `gnr8 generate` and asserted byte-identical by `gnr8 check` in `make examples-check`; hand-editing the bytes fails the gate (Pitfall 4). Never hand-write generated bytes. |
| T-06-05 | Elevation | executing the example app at analysis time | mitigate | pyextract is static `ast` (never imports/executes); tsextract reads types via the Compiler API without running the app. No "run the example app" step exists; the apps are static source only. |
| T-06-06 | Tampering | sidecar subprocess args | mitigate | The example `.gnr8/` crates compose existing Source built-ins that spawn sidecars with DISCRETE args (no `sh -c`) — the proven goextract/pyextract/tsextract pattern, unchanged. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO packages installed: no `pip install`, no `npm install`/node_modules in the NestJS example (static extraction), no `cargo add`. `git diff --exit-code crates/gnr8-core/Cargo.toml` asserted; vendored typescript is the existing Phase-4 carve-out, unchanged. No `[ASSUMED]`/`[SUS]` package → no install checkpoint (rule 2: no approval path adds an OSS dep). |
</threat_model>

<verification>
- Both examples: `gnr8 generate` then `gnr8 check` exit 0 (committed == fresh) AND a second pass is still exit 0 (determinism).
- `make examples-check` green across all three languages; `make check` green end-to-end.
- The OpenAPI of each example structurally matches its `fixtures/*` snapshot (same routes/schemas/refs) — the example does not misrepresent the tool (Pitfall 4).
- No `node_modules` in the NestJS example; no `pip`-installed deps in the FastAPI example.
- No new OSS dependency in gnr8-core (`git diff --exit-code crates/gnr8-core/Cargo.toml`).
</verification>

<success_criteria>
- A developer drives FastAPI end-to-end → OpenAPI 3.1 + Python SDK from a `.gnr8/` lifecycle with real committed output (XLANG-01).
- A developer drives NestJS end-to-end → OpenAPI 3.1 + TS SDK from a `.gnr8/` lifecycle with real committed output, no node_modules (XLANG-02).
- `make examples-check` proves byte-identical cross-language determinism and is wired into `make check` (XLANG-05 determinism).
- Config is code: both examples are `.gnr8/` Rust Pipeline crates, never data files (rule 4).
</success_criteria>

<output>
Create `.planning/phases/06-cross-language-hardening-examples-docs/06-02-SUMMARY.md` when done.
</output>
