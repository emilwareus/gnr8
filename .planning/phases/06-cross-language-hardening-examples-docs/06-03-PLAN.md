---
phase: 06-cross-language-hardening-examples-docs
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - docs/USAGE.md
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [XLANG-03, XLANG-05]
must_haves:
  truths:
    - "docs/USAGE.md states the honest per-language envelope (FastAPI full; Flask typed-only with its gaps; NestJS class DTOs) with limits, no overclaiming"
    - "CLAUDE.md records the typescript carve-out as a bounded, single documented rule-2 exception — without loosening rule 2 generally"
    - "gnr8-core takes zero NEW OSS deps (the v1 serde/serde_json/blake3/thiserror debt is unchanged, not retired) — asserted against the baseline"
    - "WR-02/WR-04 (Phase-5 TsSdk findings) are recorded as backlog, not folded into the green snapshots"
  artifacts:
    - path: "docs/USAGE.md"
      provides: "Per-language honest-envelope section + updated Source/Target/watch/doctor tables (Go/Python/TS)"
      contains: "FastAPI"
    - path: "CLAUDE.md"
      provides: "The bounded typescript carve-out subsection under rule 2"
      contains: "Documented exception"
    - path: ".planning/PROJECT.md"
      provides: "The carve-out Key Decision verified consistent with CLAUDE.md"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog note recording WR-02/WR-04 (deferred Phase-5 findings)"
      contains: "WR-02"
  key_links:
    - from: "CLAUDE.md carve-out note"
      to: ".planning/PROJECT.md carve-out Key Decision"
      via: "wording audited for agreement (both scope the TS sidecar typescript toolchain only)"
      pattern: "carve-out"
    - from: "docs/USAGE.md envelope"
      to: ".planning/phases/02-* + 04-* SUMMARYs"
      via: "the documented limits are derived from the verified extractor behavior"
      pattern: "FastAPI|Flask|NestJS"
---

<objective>
Document the honest per-language supported envelope in `docs/USAGE.md` (XLANG-03), RECORD the `typescript`
rule-2 carve-out in `CLAUDE.md` (bounded; PROJECT.md already logs it — verify/touch-up), assert
gnr8-core takes ZERO NEW OSS deps (the v1 debt stays tracked, NOT retired — XLANG-05), and BACKLOG the
deferred Phase-5 TsSdk findings (WR-02/WR-04) rather than folding them in.

Purpose: Users get an honest, limit-stated multi-language reference; the single rule-2 exception is
recorded in the invariants doc itself; the no-new-dep claim is verifiable; and the deferred hardening
items are tracked without disturbing the green snapshots.

Output: An updated `docs/USAGE.md`, a bounded carve-out note in `CLAUDE.md`, a verified/consistent
PROJECT.md decision, a no-new-dep assertion, and a ROADMAP Backlog note for WR-02/WR-04.
</objective>

<execution_context>
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-language-hardening-examples-docs/06-RESEARCH.md
@.planning/phases/06-cross-language-hardening-examples-docs/06-PATTERNS.md
@CLAUDE.md
@docs/USAGE.md

<interfaces>
<!-- Touch points (verified line numbers). -->
docs/USAGE.md (Go/Gin-only v1 reference — EXTEND in place, do not duplicate; Pitfall 7):
- l.24 "Requires a Go toolchain on PATH" -> generalize to source toolchain.
- l.47-48 watch/doctor rows; l.54 "watch re-runs on a `*.go` source edit" -> "a source-language edit".
- l.83 `Source` built-ins table lists only `GoGin` -> ADD `FastApi`/`Flask`/`NestJs`.
- l.85 `Target` built-ins table lists `OpenApi31`/`GoSdk` -> ADD `PySdk`/`TsSdk`.
- l.162 "The full runnable example: `examples/taskflow/`" -> also point to the two new examples.
- l.172 "## Recognized Go/Gin patterns" + l.188 "## Type mapping (Go -> ...)" are Go-specific -> ADD a
  per-language envelope SECTION beside them (preserve the accurate Go sections).

CLAUDE.md:
- `## 2. No third-party / OSS dependencies` (line 23) — anchor for the carve-out subsection.
- `## Known debt` (line 66) — lists serde/serde_json/blake3/thiserror as v1 debt to retire LATER (not this phase).

.planning/PROJECT.md:
- l.119 Key Decision already logs the typescript carve-out ("Documented carve-out to rule 2. Revisit").

The honest per-language envelope table (verbatim from 06-RESEARCH.md §Code Examples, derived from the
verified Phase 2/4 SUMMARYs): columns `Frontend | Lang | Status | Recognized | Limits/diagnostics` for
Gin (Go, full), FastAPI (Python, full), Flask (Python, typed-envelope second-class), NestJS (TS, class-DTO
scope). Plus: "Generated SDKs are dependency-free in every language: GoSdk (net/http), PySdk (urllib +
@dataclass), TsSdk (built-in fetch + typed interfaces)."

The bounded CLAUDE.md carve-out wording (verbatim from 06-RESEARCH.md §Code Examples): the ONLY OSS dep
permitted in any sidecar; gnr8-core + generated SDKs stay dependency-free; NEVER licenses reading
@nestjs/swagger/zod/class-validator (rule 1 absolute); FUT-04 may retire it.

WR-02 / WR-04 (06-CONTEXT.md "Optional hardening" + 06-RESEARCH.md Open Q4 / A4): WR-02 = TsSdk non-scalar
query param String() coercion needs a defined wire-encoding rule; WR-04 = TsSdk asymmetric success/error
JSON decode. RECOMMEND BACKLOG — folding them changes the green TsSdk acceptance snapshots + the NestJS
example output (the Phase-5 REVIEW-FIX skipped them for this reason).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs/USAGE.md with the honest per-language envelope</name>
  <files>docs/USAGE.md</files>
  <read_first>
    - docs/USAGE.md (the full Go/Gin-only reference — the touch points l.24/47/48/54/83/85/162/172/188)
    - .planning/phases/02-*/02-03-SUMMARY.md + 02-04-SUMMARY.md (the verified FastAPI full + Flask typed-envelope behavior incl. its gaps)
    - .planning/phases/04-*/04-03-SUMMARY.md (the verified NestJS class-DTO scope + bright-line exclusions)
    - 06-PATTERNS.md §"docs/USAGE.md (docs, EXTEND — Pitfall 7)" (the exact touch points + the honest-envelope table) + 06-RESEARCH.md §"The honest per-language envelope" in §Code Examples
  </read_first>
  <action>
    EXTEND `docs/USAGE.md` in place (do NOT duplicate or rewrite the accurate Go content). Add a
    "## Supported source frontends (the honest envelope)" section with the per-language table from RESEARCH
    §Code Examples: `Frontend | Lang | Status | Recognized | Limits/diagnostics` covering Gin (Go, full),
    FastAPI (Python, full), Flask (Python, typed-envelope — state the gaps PLAINLY: untyped request.json /
    unannotated request.args / missing return annotation produces a diagnostic, NEVER inferred), NestJS
    (TypeScript, class-DTO scope — DTO CLASSES only, bare interfaces erased; never reads
    @nestjs/swagger/zod/class-validator, rule 1). Add the line "Generated SDKs are dependency-free in every
    language: GoSdk (net/http), PySdk (urllib + @dataclass), TsSdk (built-in fetch + typed interfaces)." Update
    the Source built-ins table (l.83) to ADD `FastApi`/`Flask`/`NestJs`; the Target built-ins table (l.85) to
    ADD `PySdk`/`TsSdk`. Generalize the Go-specific wording: l.24 "Go toolchain" to "the source language's
    toolchain (Go/Python/TypeScript)"; l.54 "watch re-runs on a `*.go` source edit" to "a source-language edit
    (`.go`/`.py`/`.ts`)"; the doctor row to "source toolchain" (matching the Plan 01
    `LifecycleHealth.source_toolchain` + `language` field). Point to the two new examples
    (`examples/fastapi-bookstore/`, `examples/nestjs-bookstore/`) alongside `examples/taskflow`/`examples/bookstore`
    (l.162). Limits must be HONEST — derived from the Phase 2/4 SUMMARYs, no overclaiming. Docs only; no OSS dep.
  </action>
  <acceptance_criteria>
    - `grep -cE 'FastAPI|Flask|NestJS' docs/USAGE.md` >= 3 (all three new frontends named).
    - `grep -E 'FastApi|Flask|NestJs' docs/USAGE.md` and `grep -E 'PySdk|TsSdk' docs/USAGE.md` both return matches (the built-in tables updated).
    - The Flask gaps are stated: `grep -iE 'typed-envelope|untyped.*diagnostic|never inferred' docs/USAGE.md` returns a match.
    - The dependency-free SDK line is present: `grep -i 'dependency-free in every language' docs/USAGE.md` returns a match.
    - watch/doctor wording generalized: `grep -n 'source-language edit\|source toolchain' docs/USAGE.md` returns matches.
    - The two new examples are referenced: `grep -E 'fastapi-bookstore|nestjs-bookstore' docs/USAGE.md` returns matches.
    - The accurate Go/Gin content is preserved: `grep -c 'Recognized Go/Gin\|Type mapping' docs/USAGE.md` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE 'FastAPI|Flask|NestJS' docs/USAGE.md && grep -E 'PySdk|TsSdk' docs/USAGE.md && grep -i 'dependency-free in every language' docs/USAGE.md && grep -E 'fastapi-bookstore|nestjs-bookstore' docs/USAGE.md</automated>
  </verify>
  <done>
    `docs/USAGE.md` documents the honest per-language envelope (FastAPI full; Flask typed-only with stated gaps;
    NestJS class DTOs), updates the Source/Target/watch/doctor tables for Go/Python/TS, points to the new
    examples, and preserves the accurate Go content; XLANG-03 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the bounded typescript carve-out in CLAUDE.md; verify PROJECT.md agreement; backlog WR-02/WR-04</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    - CLAUDE.md (`## 2. No third-party / OSS dependencies` l.23; the `## Known debt` section l.66 — the four debt crates to retire LATER)
    - .planning/PROJECT.md (l.119 the carve-out Key Decision already logged — the wording to stay consistent with)
    - .planning/ROADMAP.md (ALL 6 phase sections + the Progress table — append a Backlog section; do NOT rewrite/drop any phase)
    - 06-RESEARCH.md §"The bounded CLAUDE.md carve-out note" in §Code Examples (the exact bounded wording) + Open Q4 / A4 (WR-02/WR-04 → backlog) + Pitfall 5 (do NOT retire the serde/blake3 debt this phase)
    - .planning/phases/05-*/05-REVIEW-FIX.md (why WR-02/WR-04 were skipped — they would change green snapshots)
  </read_first>
  <action>
    Add a clearly-scoped `### Documented exception (the ONE sanctioned carve-out)` subsection UNDER CLAUDE.md
    rule 2 (`## 2. No third-party / OSS dependencies`, line 23) using the bounded wording from RESEARCH
    §Code Examples: the `typescript` npm package is the ONLY OSS dependency permitted in any sidecar (the TS
    sidecar's reference Compiler API, behind the JSON-facts boundary); every other sidecar stays stdlib-only;
    `gnr8-core` and every generated SDK remain dependency-free; it NEVER licenses reading
    @nestjs/swagger/zod/class-validator (rule 1 absolute); FUT-04 may retire it. Do NOT loosen rule 2 generally
    and do NOT touch the `## Known debt` section's retire-later schedule. AUDIT the wording against the
    PROJECT.md l.119 decision so the two agree (touch-up PROJECT.md only if inconsistent — it already logs the
    carve-out). Then RECORD WR-02/WR-04 as backlog: append a `## Backlog (deferred)` section at the END of
    ROADMAP.md (AFTER the Progress table) with entries `Backlog 999.1 — WR-02: TsSdk non-scalar query param
    String() wire-encoding rule (deferred Phase-5 finding; folding it changes the green TsSdk snapshots)` and
    `Backlog 999.2 — WR-04: TsSdk asymmetric success/error JSON decode (same reason)`. CRITICAL: edit ONLY by
    APPENDING — preserve ALL 6 existing phase sections + the Progress table verbatim (a prior planner truncated
    ROADMAP.md via a full-file Write; do not repeat). No OSS dep (docs only).
  </action>
  <acceptance_criteria>
    - `grep -i 'Documented exception\|sanctioned carve-out' CLAUDE.md` returns a match (the subsection exists).
    - The carve-out is bounded: `grep -iE 'typescript' CLAUDE.md` and `grep -iE '@nestjs/swagger|zod|class-validator' CLAUDE.md` both return matches (the bright-line is stated); `grep -i 'dependency-free' CLAUDE.md` shows gnr8-core/SDKs stay dependency-free.
    - rule 2 not loosened: the `## 2. No third-party / OSS dependencies` heading and the `## Known debt` section both still exist (`grep -c '## 2. No third-party\|## Known debt' CLAUDE.md` >= 2); the four debt crates are NOT removed from `## Known debt`.
    - ROADMAP integrity: all 6 phases still present (`grep -c '^### Phase [1-6]:' .planning/ROADMAP.md` == 6) AND the Progress table is intact (`grep -c '^## Progress' .planning/ROADMAP.md` == 1).
    - Backlog recorded: `grep -E 'WR-02|WR-04' .planning/ROADMAP.md` returns matches under a `## Backlog (deferred)` section.
  </acceptance_criteria>
  <verify>
    <automated>grep -i 'Documented exception\|sanctioned carve-out' CLAUDE.md && [ "$(grep -c '^### Phase [1-6]:' .planning/ROADMAP.md)" = "6" ] && grep -E 'WR-02|WR-04' .planning/ROADMAP.md</automated>
  </verify>
  <done>
    The bounded typescript carve-out is recorded in CLAUDE.md (consistent with PROJECT.md), rule 2 is not
    loosened, the known-debt schedule is untouched, all 6 ROADMAP phases + the Progress table are preserved, and
    WR-02/WR-04 are recorded as backlog 999.x.
  </done>
</task>

<task type="auto">
  <name>Task 3: Assert gnr8-core adds zero NEW OSS deps + the full green gate (make check + examples-check)</name>
  <files>crates/gnr8-core/Cargo.toml, Makefile</files>
  <read_first>
    - crates/gnr8-core/Cargo.toml (the v1 baseline: serde/serde_json/blake3/thiserror — the FOUR debt crates that must remain unchanged, not retired)
    - Makefile (`check:` + the new `examples-check` target from Plan 02)
    - 06-RESEARCH.md Open Q1 / A1 / Pitfall 5 ("zero OSS" = no NEW dep + carve-out recorded + cargo tree shows no addition; NOT a serde rewrite) + the Phase-Requirements→Test-Map (the no-new-dep gate)
    - 06-VALIDATION.md (the phase gate: make check green AND examples-check green)
  </read_first>
  <action>
    Verify the no-new-dep invariant (the no-new-dep reading of XLANG-05, locked Q1): confirm
    `crates/gnr8-core/Cargo.toml` `[dependencies]` is UNCHANGED versus the v1 baseline — exactly the four
    tracked debt crates (serde, serde_json, blake3, thiserror), with NO new crate added by this milestone's
    work. Do NOT attempt to retire those four (Pitfall 5 — that is FUT/cleanup, explicitly deferred). Establish
    the assertion as a committed check: confirm `git diff` on `crates/gnr8-core/Cargo.toml` versus the v1
    baseline shows no NEW `[dependencies]` entry, and that `cargo tree -p gnr8-core` adds no crate beyond the
    tracked debt + their transitive deps. Then run the FULL green gate to confirm the phase is shippable:
    `make check` (fmt + clippy + test + fixture builds + the new `examples-check` from Plan 02). Ensure go,
    python3, and node are on PATH first (MEMORY: go is not on the default PATH in this sandbox). Record the
    no-new-dep result + the green-gate result in the SUMMARY. No OSS dep added (the whole point).
  </action>
  <acceptance_criteria>
    - gnr8-core deps unchanged: `crates/gnr8-core/Cargo.toml` `[dependencies]` contains exactly the four tracked debt crates and no new one — `grep -A20 '^\[dependencies\]' crates/gnr8-core/Cargo.toml` shows serde/serde_json/blake3/thiserror and nothing else added this phase.
    - No new crate in the dep tree: `cargo tree -p gnr8-core 2>/dev/null` does not list a crate absent from the v1 baseline (no `cargo add` was run).
    - The four debt crates are NOT retired (still present) — XLANG-05 is "no NEW dep + carve-out recorded," not a serde rewrite.
    - Full gate green: `make check` exits 0 (incl. `examples-check` regen-and-diff across Go/Python/TS).
    - `cargo test -p gnr8-core -p gnr8` green; `cargo clippy -- -D warnings` clean.
  </acceptance_criteria>
  <verify>
    <automated>make check 2>&1 | tail -25 && cargo tree -p gnr8-core 2>/dev/null | head -30</automated>
  </verify>
  <done>
    gnr8-core adds zero NEW OSS deps (the four v1 debt crates unchanged, not retired), the no-new-dep invariant
    is asserted, and `make check` (including the cross-language `examples-check`) is green; XLANG-05 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the envelope is honest and the carve-out is bounded</name>
  <files>docs/USAGE.md, CLAUDE.md</files>
  <action>Human verification of the prose/invariant edits (truthfulness + boundedness) — see what-built/how-to-verify below.</action>
  <what-built>
    docs/USAGE.md now carries the honest per-language envelope (FastAPI full; Flask typed-only with its
    documented gaps; NestJS class DTOs), and CLAUDE.md records the bounded `typescript` carve-out as the single
    documented rule-2 exception (consistent with PROJECT.md). These are prose/invariant edits whose TRUTHFULNESS
    and BOUNDEDNESS need a human read (per 06-VALIDATION.md Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Read the new `## Supported source frontends (the honest envelope)` section in `docs/USAGE.md`.
       Confirm each row matches actual extractor behavior (cross-check the Flask gaps + NestJS class-DTO scope
       against the Phase 2/4 SUMMARYs). Confirm NO overclaiming — every limit is stated.
    2. Read the `### Documented exception` subsection in `CLAUDE.md`. Confirm it scopes the carve-out to the TS
       sidecar's `typescript` toolchain ONLY, behind the JSON-facts boundary; that it bright-line EXCLUDES
       @nestjs/swagger/zod/class-validator; that it states gnr8-core + generated SDKs stay dependency-free; and
       that it does NOT loosen rule 2 generally (the `## Known debt` retire-later schedule is intact).
    3. Confirm the CLAUDE.md wording agrees with the PROJECT.md l.119 carve-out decision.
  </how-to-verify>
  <verify>
    <human-check>The envelope is honest (no overclaiming) and the carve-out is bounded (TS sidecar toolchain only; excludes swagger/zod/class-validator; rule 2 not loosened).</human-check>
  </verify>
  <done>Human confirms the docs/USAGE.md envelope is honest and the CLAUDE.md carve-out is bounded and consistent with PROJECT.md.</done>
  <resume-signal>Type "approved" or describe what to fix (overclaim / carve-out too broad / inconsistency).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documented invariant ↔ actual behavior | docs/USAGE.md + CLAUDE.md must describe what the code actually does (honest envelope) and what is actually permitted (bounded carve-out). |
| Dependency manifest ↔ supply chain | `crates/gnr8-core/Cargo.toml` is the supply-chain surface; the no-new-dep assertion guards it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Repudiation/Info disclosure | docs envelope overclaiming | mitigate | The envelope is derived from the verified Phase 2/4 SUMMARYs and the limits are stated plainly; a human checkpoint verifies truthfulness (no overclaiming). |
| T-06-08 | Elevation (scope creep) | the carve-out note loosening rule 2 | mitigate | The note is a BOUNDED subsection; a human checkpoint verifies it scopes to the TS sidecar toolchain only and bright-line excludes @nestjs/swagger/zod/class-validator; rule 2 + the known-debt schedule stay intact. |
| T-06-09 | Tampering | silent ROADMAP truncation | mitigate | The acceptance criteria assert all 6 phase sections + the Progress table survive; edits are append-only (the Backlog section). |
| T-06-SC | Tampering | npm/pip/cargo installs (supply chain) | mitigate | ZERO packages installed; the no-new-dep assertion (`Cargo.toml` unchanged vs baseline + `cargo tree` adds nothing) is the explicit supply-chain gate. The vendored typescript is the recorded, pinned Phase-4 carve-out — unchanged. No `[ASSUMED]`/`[SUS]` package → no install checkpoint (rule 2: no approval path adds an OSS dep). |
</threat_model>

<verification>
- docs/USAGE.md names FastAPI/Flask/NestJS + PySdk/TsSdk, states the Flask gaps + NestJS scope, keeps the accurate Go content, and points to the two new examples.
- CLAUDE.md records the bounded carve-out (consistent with PROJECT.md); rule 2 + the known-debt schedule are intact.
- All 6 ROADMAP phase sections + the Progress table preserved; WR-02/WR-04 recorded as backlog 999.x.
- gnr8-core adds zero NEW OSS deps (Cargo.toml unchanged vs baseline; cargo tree adds nothing); the four debt crates are NOT retired.
- `make check` (incl. `examples-check`) is green end-to-end.
- Human checkpoint confirms the envelope is honest and the carve-out is bounded.
</verification>

<success_criteria>
- docs/USAGE.md documents the honest per-language supported envelope with limits, no overclaiming (XLANG-03).
- The typescript carve-out is recorded in CLAUDE.md as the single bounded rule-2 exception, consistent with PROJECT.md (XLANG-05).
- gnr8-core takes zero NEW OSS deps, asserted; the v1 debt stays tracked, not retired (XLANG-05).
- Cross-language determinism (`make check` + `examples-check`) is green (XLANG-05).
- WR-02/WR-04 are recorded as backlog, not folded into the green snapshots.
</success_criteria>

<output>
Create `.planning/phases/06-cross-language-hardening-examples-docs/06-03-SUMMARY.md` when done.
</output>
