---
phase: 04-typescript-source-tsextract
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
autonomous: true
requirements: [TSSRC-02, TSSRC-03]
files_modified:
  - tsextract/load.js
  - tsextract/types.js
  - tsextract/schemas.js
  - tsextract/diagnostics.js
  - tsextract/facts.js
  - tsextract/index.js
  - tsextract/tests/golden.test.js
  - tsextract/tests/types.test.js

must_haves:
  truths:
    - "tsextract builds a ts.Program + TypeChecker over the target's *.ts using synthesized CompilerOptions (strictNullChecks + experimentalDecorators), never reading the target's own tsconfig"
    - "Every schema fact is derived from the source's OWN TS property types via the TypeChecker — never from @nestjs/swagger, zod, or class-validator"
    - "The sidecar never executes the target TS (no require/import/eval/vm of target modules — only ts.createProgram over source text)"
    - "TS number maps to {prim:float,bits:64}; null/undefined arms are stripped to optional/nullable axes (not emitted as union members)"
    - "A named type-alias used as a sole type emits a named ref + its own enum schema; a residual bare literal union inlines as a sorted enum (format -> named, sort -> inline)"
    - "All 8 DTOs (including the union-arm-only OutOfStockDto) are transitively collected; an unresolvable type emits a diagnostic and OMITS the fact (no guessed fallback)"
  artifacts:
    - path: "tsextract/load.js"
      provides: "discover *.ts (sorted) + ts.createProgram + getTypeChecker (synthesized options, static-only)"
      contains: "createProgram"
    - path: "tsextract/types.js"
      provides: "TS Type -> neutral Type with axis stripping + number->float64 + aliasSymbol named-vs-inline"
    - path: "tsextract/schemas.js"
      provides: "DTO class/alias -> SchemaFact, transitive collection, schema-id = relpath.removesuffix(.ts)+.Name"
    - path: "tsextract/facts.js"
      provides: "deterministic sorted JSON marshal (mirror pyextract/facts.py)"
    - path: "tsextract/tests/golden.test.js"
      provides: "golden test pinning format=named and sort=inline against the snapshot vocabulary"
  key_links:
    - from: "tsextract/types.js"
      to: "checker.getTypeAtLocation / type.aliasSymbol"
      via: "TypeChecker resolution + residual aliasSymbol predicate"
      pattern: "aliasSymbol"
    - from: "tsextract/load.js"
      to: "ts.createProgram"
      via: "synthesized CompilerOptions (strictNullChecks, experimentalDecorators)"
      pattern: "strictNullChecks"
    - from: "tsextract/index.js"
      to: "facts.marshal"
      via: "build_doc -> sorted JSON stdout"
      pattern: "marshal"
---

<objective>
Implement the tsextract extractor CORE: the loader (ts.Program + TypeChecker), the TS-type to neutral-Type
mapper (the strip-axes + number-to-float64 + named-vs-inline rules), the schema builder (DTO class/alias to
SchemaFact with transitive collection), the diagnostics accumulator, and the deterministic facts marshal.
Routes recognition (the @Controller/@Get/@Param walk) is W3; this wave delivers everything that turns a
resolved DTO type into the correct neutral fact, proven by golden unit tests against the snapshot vocabulary.

Purpose: the type-mapping + schema-collection rules are the genuinely-new logic in this phase. Isolating
them here (with golden unit tests for the subtle format-vs-sort named-vs-inline predicate, Open Q1) keeps
the W3 snapshot flip a wiring exercise, not a debugging exercise.
Output: tsextract/{load,types,schemas,diagnostics,facts}.js implemented; index.js wired for schema
collection; node-native golden + type unit tests pinning the verified mappings.
</objective>

<execution_context>
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/04-typescript-source-tsextract/04-RESEARCH.md
@.planning/phases/04-typescript-source-tsextract/04-PATTERNS.md
@fixtures/nestjs-bookstore/src/books.dto.ts
@crates/gnr8-core/tests/snapshots/snapshot_nestjs_graph__nestjs_graph.snap
@pyextract/types.py
@pyextract/schemas.py
@pyextract/facts.py
@pyextract/diagnostics.py

<interfaces>
The neutral facts wire shapes (from facts.rs, deny_unknown_fields) the sidecar MUST emit:
- Top-level keys EXACTLY: module, routes, schemas, diagnostics
- Type adjacently tagged: type=variant, of=payload; Any = type any, of empty object
- Prim internally tagged on prim: prim string / bool / float bits 64 (TS number maps HERE) / int bits 64 signed / bytes
- Named payload = bare id string: type named, of "src/books.dto.AuthorDto"
- Enum payload = sorted string array: type enum, of ["asc","desc"]
- Union payload = array of Type, SOURCE order (not sorted): type union, of [...]
- FieldFact keys EXACTLY: json_name, required, optional, nullable, schema, description, example (last two always null)
- SchemaFact id rule: relpath(file) with .ts dropped + "." + Name => "src/books.dto.BookFormat"; module = basename(target_dir)
- DiagnosticFact keys EXACTLY: severity, message, file, line (line is a single integer)

Verified snapshot facts to reproduce (RESEARCH Pattern 3 / Pitfall 2-5):
- BookDto.format BookFormat -> named ref src/books.dto.BookFormat + BookFormat schema enum [hardcover,paperback] (sorted)
- BookFilters.sort? SortOrder|null -> INLINE enum [asc,desc] optional true nullable true; NO SortOrder schema
- BookDto.rating? number|null -> single float64 optional true nullable true (NOT a union)
- AuthorDto.bio string|null -> string nullable true
- BookDto.tags? string[] -> array of string, optional true nullable false
- BookOrError = BookDto|OutOfStockDto -> schema union body [named BookDto, named OutOfStockDto] (source order)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: load.js (ts.Program + TypeChecker, static-only) + diagnostics.js + facts.js marshal</name>
  <files>tsextract/load.js, tsextract/diagnostics.js, tsextract/facts.js, tsextract/index.js</files>
  <read_first>
    - pyextract/load.py (discover sorted, per-file diag), pyextract/diagnostics.py (warn shape), pyextract/facts.py (build_doc + _sort_doc + marshal) — twins to mirror
    - 04-PATTERNS.md load.js + diagnostics.js + facts.js sections (the divergence: ts.createProgram NOT ast.parse; NEVER require/import the target)
    - 04-RESEARCH.md Code Examples lines 365-388 (Program setup), Pitfall 1 (strictNullChecks), Pitfall 6 (getStart(sf)), Pattern 1 (wire shapes)
  </read_first>
  <action>
    Implement load.js: recursively discover *.ts under the target dir (sorted, for internal determinism; skip
    node_modules), then ts.createProgram(files, opts) + program.getTypeChecker(). SYNTHESIZE the CompilerOptions
    in the sidecar (target ES2020, experimentalDecorators true, strictNullChecks true, skipLibCheck true); do
    NOT read the target's own tsconfig (Pitfall 1: the null/undefined axes depend on strictNullChecks; synthesizing
    makes analysis deterministic regardless of the target). STATIC-ONLY (security boundary, rule 3): only
    ts.createProgram over source text — NEVER require/import/eval/vm/transpile-and-run the target. Always pass the
    SourceFile to node.getStart(sf)/node.getText(sf) (Pitfall 6). Implement diagnostics.js as a near-verbatim twin
    of pyextract/diagnostics.py: a Diagnostics accumulator with warn(message, file, line) pushing severity WARN +
    message + file + line as a Number, and items(). Implement facts.js: build_doc(module, routes, schemas,
    diagnostics) returning those four keys, and marshal(doc) that SORTS every array by the exact pyextract keys
    (schemas by id; object fields by json_name; enum members lexically; diagnostics by file,line,message; routes by
    path,method; params by name,location; responses by status; UNION members NOT sorted, source order) then
    serializes via JSON.stringify with a recursive key-sorted replacer so object key order is stable. Wire index.js
    (from W1) to call load -> schemas (Task 2) -> facts.marshal; keep the stdout=facts-only / stderr=errors /
    nonzero-exit discipline.
  </action>
  <verify>
    <automated>node tsextract/index.js fixtures/nestjs-bookstore > /tmp/ts_a.json; node tsextract/index.js fixtures/nestjs-bookstore > /tmp/ts_b.json; diff /tmp/ts_a.json /tmp/ts_b.json</automated>
    <automated>grep -rEn "eval\(|vm\.|child_process|transpileModule|\.run\(" tsextract/load.js tsextract/types.js tsextract/schemas.js tsextract/facts.js tsextract/diagnostics.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "strictNullChecks" tsextract/load.js AND grep -n "experimentalDecorators" tsextract/load.js are present; grep -n "createProgram" tsextract/load.js present.
    - tsextract reads NO target tsconfig: grep -n "tsconfig" tsextract/load.js returns nothing (options synthesized).
    - STATIC-ONLY grep gate: the second verify command returns EMPTY (no eval/vm/child_process/transpileModule/.run( of target modules anywhere in tsextract; only require('typescript')/relative requires/fs/path are used).
    - Determinism: the first verify command's diff is empty (two runs byte-identical).
    - diagnostics.js objects have EXACTLY keys severity,message,file,line.
  </acceptance_criteria>
  <done>load.js builds a Program+TypeChecker with synthesized strict options and never executes the target; diagnostics.js + facts.js are pyextract twins; marshal is deterministic and sorted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: types.js (TS Type to neutral Type) + schemas.js (DTO to SchemaFact, transitive) + golden tests</name>
  <files>tsextract/types.js, tsextract/schemas.js, tsextract/tests/types.test.js, tsextract/tests/golden.test.js</files>
  <read_first>
    - fixtures/nestjs-bookstore/src/books.dto.ts (the DTOs: AuthorDto, BookDto, BookFilters, BookFormat, SortOrder, BookOrError, OutOfStockDto, CreatedMessage, ListBooksResponse) — the input
    - crates/gnr8-core/tests/snapshots/snapshot_nestjs_graph__nestjs_graph.snap (schemas section lines 132-400) — the byte-exact spec for each schema body
    - pyextract/types.py + pyextract/schemas.py (mapping-table docstring + axes + transitive collection) — twins
    - 04-RESEARCH.md Pattern 3 (mapping), Pitfall 2 (number->float64), Pitfall 3 (axes matrix), Pitfall 4 + Open Question 1 (named-vs-inline predicate), Pitfall 5 (union-of-objects vs enum), Pattern 4 (schema id + transitive)
  </read_first>
  <behavior>
    - number -> prim float bits 64 (NEVER int); string -> prim string; boolean -> prim bool; T[] -> array of map(T)
    - rating? number|null -> single float64 with optional true, nullable true (NOT a union)
    - bio string|null -> string with nullable true, optional false
    - tags? string[] -> array of string with optional true, nullable false
    - format BookFormat -> named ref src/books.dto.BookFormat AND a BookFormat enum schema [hardcover,paperback]
    - sort? SortOrder|null -> INLINE enum [asc,desc] (sorted) with optional true, nullable true, and NO SortOrder schema
    - BookOrError -> schema with union body [named BookDto, named OutOfStockDto] in source order
    - all 8 schema ids present after transitive collection (OutOfStockDto reachable only via BookOrError union arm)
    - an unresolvable type -> a diagnostic + OMITTED fact, never type any
  </behavior>
  <action>
    Implement types.js mapType: FIRST split the type into arms (if isUnion), strip the Undefined arm (set
    optional true) and the Null arm (set nullable true); the residual is the schema type. Map number to prim
    float bits 64 (NEVER int, Pitfall 2); string to prim string; boolean to prim bool; array (checker isArrayType
    / getNumberIndexType) to array of map(elem). Residual with >1 string-literal arms -> enum of sorted members
    (inline); residual with >1 named-object arms -> union of mapped arms (SOURCE order). NAMED-VS-INLINE predicate
    (Open Q1, pin against the snapshot): check aliasSymbol on the RESIDUAL type (after stripping null/undefined);
    if present -> named ref + register the alias for schema emission (format BookFormat -> named); if absent (a
    bare literal union, e.g. sort? SortOrder|null whose residual lost the aliasSymbol) -> inline sorted enum. A DTO
    class type -> named ref + register the class. An UNRESOLVABLE type -> return a diagnostic + OMIT the fact (rule
    3, NEVER type any as a guess). Implement schemas.js: one SchemaFact per registered class/alias; id =
    relpath(file) with .ts dropped + "." + Name (slash form kept); object body = FieldFacts (required = !optional;
    nullable independent; description/example always null); string-literal-union alias -> enum body (sorted);
    object-union alias (BookOrError) -> union body (source order BookDto then OutOfStockDto). TRANSITIVE collection:
    from every type referenced by a route param/body/response, follow named refs through field types AND union arms
    until fixpoint, so OutOfStockDto is emitted; all 8 schemas appear. Add tsextract/tests/types.test.js (node
    assert) covering the verified rows, and tsextract/tests/golden.test.js asserting BOTH format -> named ref +
    BookFormat enum schema AND sort -> inline enum [asc,desc] with NO SortOrder schema, by running over the real
    fixture (Open Q1 pinned + regression-locked).
  </action>
  <verify>
    <automated>node tsextract/tests/types.test.js && node tsextract/tests/golden.test.js</automated>
    <automated>node tsextract/index.js fixtures/nestjs-bookstore > /tmp/ts_facts.json; grep -c "src/books.dto." /tmp/ts_facts.json</automated>
  </verify>
  <acceptance_criteria>
    - node tsextract/tests/types.test.js exits 0 (all verified rows pass: number->float64; rating?:number|null -> single float64 opt+null; bio:string|null -> string null; tags?:string[] -> array opt).
    - node tsextract/tests/golden.test.js exits 0 (format -> named ref src/books.dto.BookFormat + a BookFormat enum schema [hardcover,paperback]; sort -> inline enum [asc,desc] with NO src/books.dto.SortOrder schema present).
    - The facts doc over the fixture contains exactly the 8 schema ids AuthorDto, BookDto, BookFilters, BookFormat, BookOrError, CreatedMessage, ListBooksResponse, OutOfStockDto (no SortOrder schema): verify the 8 ids are present and SortOrder is absent.
    - grep -n "TypeFlags.Undefined" tsextract/types.js and grep -n "TypeFlags.Null" tsextract/types.js present (axes stripped, not emitted as union members).
    - No guessed fallback: grep tsextract/types.js shows an unresolvable branch emitting a diagnostic + returning null/omit, never a literal type:any default.
  </acceptance_criteria>
  <done>types.js maps every verified TS construct to the correct neutral Type with stripped axes and the named-vs-inline predicate; schemas.js emits all 8 transitively-collected DTOs byte-correct; golden + type tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tsextract -> target *.ts | target source is untrusted input — parsed as DATA via the Compiler API, never executed |
| tsextract -> third-party schema annotations | swagger/zod/class-validator on the target are a forbidden fact source (rule 1) |
| sidecar stdout -> host | the emitted JSON crosses into the deny_unknown_fields host DTO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Elevation of Privilege | load.js target parsing | mitigate | static ts.createProgram over source text ONLY; NEVER require/import/eval/vm/transpile-and-run the target (TSSRC-03); grep gate asserts no such constructs |
| T-04-06 | Spoofing of facts | types.js / schemas.js schema derivation | mitigate | schema facts come ONLY from the source's own TS property types via the TypeChecker; NEVER read @nestjs/swagger, zod, or class-validator (rule 1); grep gate in 04-03 asserts absence |
| T-04-07 | Tampering | unresolvable type handling | mitigate | rule 3: unresolvable/untyped -> diagnostic + OMIT the fact; never a guessed type:any default (acceptance grep) |
| T-04-08 | Tampering | non-deterministic output | mitigate | facts.marshal sorts every array + key-sorted serialize; two-run byte-identical acceptance check |
| T-04-SC | Malicious Code | tsextract dependency surface | mitigate | the sidecar imports ONLY require('typescript') + node builtins (fs/path) + relative modules; no second npm dep (rule 2) |
</threat_model>

<verification>
- node tsextract/tests/types.test.js + golden.test.js green (the verified mappings + Open Q1 pinned).
- Static-only grep gate empty; no tsconfig read; deterministic two-run output.
- All 8 DTOs transitively collected; SortOrder is NOT a standalone schema.
- The 2 nestjs snapshots stay red until W3 wires routes (this wave delivers schema/type core only).
</verification>

<success_criteria>
- The TS-type to neutral-Type mapping reproduces every verified snapshot schema body (axes, float64, named-vs-inline, union-vs-enum).
- All 8 DTOs are transitively collected with byte-correct ids and bodies; unresolvable -> diagnostic + omit.
- The sidecar is static-only, bright-line-clean, single-dep, and deterministic.
</success_criteria>

<output>
Create `.planning/phases/04-typescript-source-tsextract/04-02-SUMMARY.md` when done
</output>
