---
phase: 04-typescript-source-tsextract
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
autonomous: true
requirements: [TSSRC-01]
files_modified:
  - tsextract/routes.js
  - tsextract/index.js
  - tsextract/tests/routes.test.js
  - fixtures/nestjs-bookstore/src/books.controller.ts
  - fixtures/nestjs-bookstore/src/books.dto.ts
  - crates/gnr8-core/tests/snapshot_nestjs_graph.rs
  - crates/gnr8-core/tests/snapshot_nestjs_openapi.rs
  - crates/gnr8-core/tests/determinism.rs
  - Makefile

must_haves:
  truths:
    - "tsextract recognizes the NestJS @Controller class + @Get/@Post/@Put/@Patch/@Delete method decorators + @Param/@Query/@Body param decorators for ROUTING facts, deriving paths group-relative (the @Controller prefix is never folded)"
    - "Status code is method-derived (typed POST -> 201, else 200), overridable by @HttpCode(n) — a single deterministic rule, not a fallback"
    - "snapshot_nestjs_graph and snapshot_nestjs_openapi both pass GREEN through real tsextract extraction with ZERO snapshot edits"
    - "Every produced route/param/schema span line equals the snapshot's asserted line (fixture reconciled with blank lines / non-fact comments only)"
    - "The nestjs snapshot tests skip (not fail) when node or typescript is absent, and make check is green and deterministic"
  artifacts:
    - path: "tsextract/routes.js"
      provides: "NestJS controller/method/param decorator recognition -> RouteFacts (verb map, path :name->{name}, method-derived status)"
      contains: "getDecorators"
    - path: "crates/gnr8-core/tests/snapshot_nestjs_graph.rs"
      provides: "GREEN graph snapshot test (no #[ignore]) with skip-if-toolchain-absent guard"
    - path: "crates/gnr8-core/tests/snapshot_nestjs_openapi.rs"
      provides: "GREEN openapi snapshot test (no #[ignore]) with skip-if-toolchain-absent guard"
    - path: "tsextract/tests/routes.test.js"
      provides: "route-recognition unit test (verbs, paths, params, body, method-derived status)"
  key_links:
    - from: "tsextract/routes.js"
      to: "ts.getDecorators"
      via: "@Controller + method/param decorator AST walk"
      pattern: "getDecorators"
    - from: "crates/gnr8-core/tests/snapshot_nestjs_graph.rs"
      to: "gnr8_core::analyze::build_graph"
      via: "real tsextract extraction, #[ignore] removed"
      pattern: "build_graph"
    - from: "fixtures/nestjs-bookstore/src/books.controller.ts"
      to: "snapshot asserted span lines"
      via: "blank-line / non-fact-comment reconciliation"
      pattern: "start_line"
---

<objective>
Implement NestJS route recognition in tsextract (the @Controller/@Get/@Param decorator walk producing
RouteFacts), reconcile the fixture source lines so every span anchors to the committed snapshot's asserted
line, and flip the two nestjs snapshots (graph + openapi) from #[ignore] red to GREEN through REAL
Compiler-API extraction with ZERO snapshot edits. Add the skip-if-(node|typescript)-absent guard, the
determinism twins, and wire the nestjs tests into the make check gate.

Purpose: this is the acceptance wave — the two committed snapshots ARE the spec, and turning them green
proves the full NestJS source -> neutral IR -> OpenAPI path. The schema/type core (W2) is reused; this wave
adds routing facts + the fixture reconciliation, exactly as Phase 2 did for FastAPI/Flask.
Output: tsextract/routes.js + route unit tests; reconciled fixture; both nestjs snapshots green (no edits);
skip-guard + determinism twins; make check green and deterministic with the chosen vendoring strategy.
</objective>

<execution_context>
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/04-typescript-source-tsextract/04-RESEARCH.md
@.planning/phases/04-typescript-source-tsextract/04-PATTERNS.md
@.planning/phases/02-python-source-pyextract/02-04-SUMMARY.md
@fixtures/nestjs-bookstore/src/books.controller.ts
@crates/gnr8-core/tests/snapshots/snapshot_nestjs_graph__nestjs_graph.snap
@crates/gnr8-core/tests/snapshot_nestjs_graph.rs
@crates/gnr8-core/tests/snapshot_nestjs_openapi.rs
@pyextract/routes.py

<interfaces>
RouteFact wire shape (from facts.rs) the sidecar must emit:
- RouteFact keys EXACTLY: method, path, handler, operation_id, params, request_body, responses, span
- ParamFact keys EXACTLY: name, location, required, schema, span
- ResponseFact keys EXACTLY: status, body. request_body/body = {ref_id:<id>} or null
- SourceSpan keys EXACTLY: file, start_line, end_line

Verified NestJS recognition rules (RESEARCH Pattern 2 / Pattern 5):
- verb map: Get->GET, Post->POST, Put->PUT, Patch->PATCH, Delete->DELETE
- path: NestJS '/:bookId' -> neutral '/{bookId}'; @Controller('books') prefix NOT folded (op paths stay /, /{bookId})
- @Param -> location path, required true; @Query -> location query (required from ?/default); @Body -> request_body TypeRef (NOT a param)
- operation_id = handler = method name verbatim (listBooks, createBook, getBook, updateBook)
- status: typed POST -> 201, else 200; @HttpCode(n) overrides if present (single rule, not a fallback)
- responses: listBooks->ListBooksResponse@200, createBook->CreatedMessage@201, getBook->BookOrError@200, updateBook->CreatedMessage@200

Verified fixture line offsets (current AST anchor -> snapshot line; RESEARCH Pitfall 8):
- ops: listBooks 40->41, createBook 50->51, getBook 57->57, updateBook 66->65
- params: genre 41->42, sort 42->43, cursor 43->44, bookId(get) 59->58, fmt 60->59, bookId(put) 68->66
- schemas: BookFormat 21->36, AuthorDto 29->41, BookDto 37->47, BookFilters 48->56, OutOfStockDto 60->70, BookOrError 65->73, CreatedMessage 67->75, ListBooksResponse 72->80

Test harness (current, both #[ignore]):
- snapshot_nestjs_graph.rs: build_graph(FIXTURE_DIR) then assert_yaml_snapshot!("nestjs_graph", graph)
- snapshot_nestjs_openapi.rs: build_graph + to_openapi(&graph, "bookstore", "/books", &fixture_security()); title/base_path/security are TEST-supplied (rule 4, Pitfall 9) — do NOT touch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: routes.js NestJS recognition + route unit tests + wire index.js</name>
  <files>tsextract/routes.js, tsextract/index.js, tsextract/tests/routes.test.js</files>
  <read_first>
    - fixtures/nestjs-bookstore/src/books.controller.ts (the 4 routes + their decorators/params) — the input
    - pyextract/routes.py (recognition discipline: by-name, group-relative paths, _span shape, method-derived status from Phase 2) — analog
    - 04-RESEARCH.md Pattern 2 (decorator AST shapes), Pattern 5 (method-derived status), Pitfall 6 (getDecorators + getStart(sf))
    - 04-PATTERNS.md routes.js section
  </read_first>
  <action>
    Implement routes.js recognizeNestController(program, checker, diags): for each SourceFile, find classes
    decorated with @Controller (via ts.getDecorators guarded by ts.canHaveDecorators; read the optional string
    arg as the controller base path — recorded as provenance only, NEVER folded into op paths, rule 1). For each
    method carrying an HTTP-verb decorator (Get/Post/Put/Patch/Delete), build a RouteFact: method via the verb map;
    path = the decorator's string arg with :name converted to {name} (a bare '/' stays '/'); operation_id = handler
    = the method name verbatim. Parameters: @Param -> location path, required true; @Query -> location query with
    required derived from the questionToken/default; @Body -> request_body as a TypeRef (NOT a param) — resolve its
    DTO type via the W2 mapType/schema registration. Response: resolve the method return type to a ref_id; status
    is method-derived (typed POST -> 201, else 200), overridden by @HttpCode(n) if that decorator is present (a
    single deterministic rule, rule 3 — NOT a try-then-fallback). All spans via getLineAndCharacterOfPosition(
    node.getStart(sf)) (1-based; always pass sf, Pitfall 6). Wire index.js to call recognizeNestController and pass
    its referenced types into the W2 transitive schema collection, then facts.build_doc -> marshal. Add
    tsextract/tests/routes.test.js asserting the 4 routes' verbs/paths/params/body-refs/response-refs and the
    method-derived status (createBook 201, others 200) over the real fixture.
  </action>
  <verify>
    <automated>node tsextract/tests/routes.test.js</automated>
    <automated>node tsextract/index.js fixtures/nestjs-bookstore > /tmp/ts_routes.json; grep -c "operation_id" /tmp/ts_routes.json</automated>
  </verify>
  <acceptance_criteria>
    - node tsextract/tests/routes.test.js exits 0: 4 routes recognized (listBooks GET /, createBook POST /, getBook GET /{bookId}, updateBook PUT /{bookId}); createBook status 201, others 200; getBook/updateBook have a {bookId} path param; listBooks has cursor/genre/sort query params; createBook/updateBook have a request_body ref.
    - grep -n "getDecorators" tsextract/routes.js present; grep -n "Controller" tsextract/routes.js present.
    - The @Controller('books') prefix is NOT folded: the emitted op paths are exactly /, /, /{bookId}, /{bookId} (verify the facts doc paths contain no "/books" prefix on operations).
    - Status is a single method-derived rule: grep tsextract/routes.js shows POST->201/else->200 with an @HttpCode override branch, NOT a try-typed-then-fallback chain.
    - All bright-line clean: routes.js reads @nestjs/common routing decorators only (Controller/Get/Post/Put/Patch/Delete/Param/Query/Body/HttpCode); grep tsextract/routes.js for swagger/zod/class-validator returns nothing.
    - ROUTE-SEEDED transitive schemas end-to-end: `node tsextract/index.js fixtures/nestjs-bookstore` now emits exactly the 8 schema ids (AuthorDto, BookDto, BookFilters, BookFormat, BookOrError, CreatedMessage, ListBooksResponse, OutOfStockDto; SortOrder absent) — collection roots seeded from the route params/body/response that this task adds (the W2 schemas.test.js validated the same 8 via direct roots; this is the full-pipeline confirmation). Verify: `node tsextract/index.js fixtures/nestjs-bookstore | grep -c '"id": "src/books.dto.'` equals 8.
  </acceptance_criteria>
  <done>routes.js recognizes the NestJS envelope into RouteFacts with group-relative paths + method-derived status; route unit tests pass; index.js emits a complete facts doc (routes + transitively-collected schemas).</done>
</task>

<task type="auto">
  <name>Task 2: reconcile fixture span lines to the snapshot + a golden line-assertion test</name>
  <files>fixtures/nestjs-bookstore/src/books.controller.ts, fixtures/nestjs-bookstore/src/books.dto.ts</files>
  <read_first>
    - fixtures/nestjs-bookstore/src/books.controller.ts + books.dto.ts (current line positions) — files being edited
    - crates/gnr8-core/tests/snapshots/snapshot_nestjs_graph__nestjs_graph.snap (every start_line/end_line) — the authoritative target
    - 04-RESEARCH.md Pitfall 8 (verified offsets) + Open Question 3; 02-04-SUMMARY.md (the Phase-2 reconciliation approach: fixture-side blank lines / non-fact comments only, ZERO snapshot edits)
    - 04-PATTERNS.md "fixtures/nestjs-bookstore/src/*.ts (fixture — line reconciliation)"
  </read_first>
  <action>
    Pick ONE consistent anchor convention (operation = the method-name line; param = the param-name line; schema =
    the class/type-alias-name line) matching how routes.js/schemas.js compute spans. Then EDIT the fixture source
    — inserting BLANK LINES and NON-FACT COMMENTS ONLY (rule 1: NEVER add a swagger/zod/class-validator annotation
    or any API-fact-bearing construct; no fact may be encoded in a comment) — so every honest AST anchor lands on
    the snapshot's asserted line, per the verified offsets (ops listBooks->41, createBook->51, getBook->57,
    updateBook->65; params genre->42, sort->43, cursor->44, bookId(get)->58, fmt->59, bookId(put)->66; schemas
    BookFormat->36, AuthorDto->41, BookDto->47, BookFilters->56, OutOfStockDto->70, BookOrError->73,
    CreatedMessage->75, ListBooksResponse->80). Iterate: run the graph snapshot test, measure produced-vs-target,
    adjust the non-fact prose until every provenance block matches. Add a golden line-assertion test (Rust or a
    node check) asserting produced span line == snapshot line for each anchor, guarding against future drift.
    Correcting a snapshot line is permitted ONLY if a line is genuinely unreachable (it is not — blank lines give
    ample freedom); the goal is ZERO snapshot edits.
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test snapshot_nestjs_graph</automated>
    <automated>git diff --stat crates/gnr8-core/tests/snapshots/snapshot_nestjs_graph__nestjs_graph.snap</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p gnr8-core --test snapshot_nestjs_graph passes (the test's #[ignore] is removed in Task 3; here it passes when run with --include-ignored, then unconditionally after Task 3).
    - ZERO snapshot edits: git diff --stat on snapshot_nestjs_graph__nestjs_graph.snap is EMPTY (the committed snapshot is byte-unchanged).
    - The fixture edits are non-fact ONLY: git diff of books.controller.ts / books.dto.ts shows only blank lines and comments added (no new decorator, no new property, no changed type) — verify the diff adds no swagger/zod/class-validator import or annotation.
    - The golden line-assertion test passes (produced span line == snapshot line for every operation/param/schema anchor).
  </acceptance_criteria>
  <done>Every fixture span anchors to the snapshot's asserted line via blank-line/non-fact-comment reconciliation; the graph snapshot matches byte-for-byte with ZERO snapshot edits; a golden test locks the line mapping.</done>
</task>

<task type="auto">
  <name>Task 3: flip both nestjs snapshots green + skip-guard + determinism twins + make check gate</name>
  <files>crates/gnr8-core/tests/snapshot_nestjs_graph.rs, crates/gnr8-core/tests/snapshot_nestjs_openapi.rs, crates/gnr8-core/tests/determinism.rs, Makefile</files>
  <read_first>
    - crates/gnr8-core/tests/snapshot_nestjs_graph.rs + snapshot_nestjs_openapi.rs (the #[ignore] harness) — files being edited
    - crates/gnr8-core/tests/determinism.rs (the Go/Py twins + their skip-if-toolchain-absent pattern at lines 48-130) — analog for the nestjs twins + skip-guard
    - Makefile (gates line 54-57, red line 73-77, check line 81; goextract-build line 65) — the gate wiring
    - 04-RESEARCH.md lines 462-468 (skip-guard sketch), Open Question 2 (vendoring); 04-01 SUMMARY for which vendoring option was chosen
    - 04-PATTERNS.md "snapshot_nestjs_*.rs (flip RED->GREEN)" + ".gitignore (config)"
  </read_first>
  <action>
    Remove the #[ignore] attribute + the "red until then" .expect wording from BOTH snapshot_nestjs_graph.rs and
    snapshot_nestjs_openapi.rs so they run against real tsextract. KEEP the assertions + the committed .snap files
    UNCHANGED (zero snapshot edits); KEEP the openapi test's fixture_security() + to_openapi(&graph, "bookstore",
    "/books", &fixture_security()) call (title/base_path/security are TEST-supplied, rule 4, Pitfall 9). DESIGN a
    skip-if-(node|typescript)-absent guard (no existing test-dir analog): probe that node runs AND tsextract's
    typescript is resolvable (e.g. build_graph returning the TypeScriptToolchainMissing variant, or a direct probe)
    and, if absent, eprintln a skip note and return BEFORE asserting — mirroring the determinism.rs go-toolchain
    skip so a node-less box never hard-fails. Add nestjs determinism twins to determinism.rs (build_graph +
    to_openapi byte-identical across two runs, with the same skip-guard). Clean the 4 stale *.snap.new files. Wire
    the gate: add the nestjs snapshot tests + the nestjs determinism twins to the Makefile gates target; remove the
    nestjs entries from the red target (they are green now, like FastAPI/Flask were removed in Phase 2). If 04-01
    chose vendoring Option B (npm ci), add a tsextract-install Make target (npm ci in tsextract/) and make the
    gates/check targets depend on it BEFORE the nestjs tests; if Option A (committed node_modules), no install step
    is needed. Run make check and confirm it is GREEN and deterministic in this sandbox; if Option B makes make
    check flaky/non-hermetic, FALL BACK to Option A (commit the vendored typescript + adjust .gitignore) and
    document the switch in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test snapshot_nestjs_graph --test snapshot_nestjs_openapi</automated>
    <automated>cargo test -p gnr8-core --test determinism</automated>
    <automated>make check</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p gnr8-core --test snapshot_nestjs_graph --test snapshot_nestjs_openapi passes WITHOUT --ignored (both #[ignore] removed): grep -c "#\[ignore" on both test files is 0.
    - ZERO snapshot edits: git diff --stat on both snapshot_nestjs_*.snap files is EMPTY.
    - cargo test -p gnr8-core --test determinism passes including the two nestjs twins (build_graph + to_openapi byte-identical across two runs).
    - Skip-guard works: with node unavailable the nestjs tests SKIP (eprintln + return), they do not fail — verify the test source has an early-return-on-absent-toolchain branch (mirror determinism.rs go skip).
    - make check is GREEN end-to-end; the 4 stale *.snap.new files are removed (ls crates/gnr8-core/tests/snapshots/*nestjs*.snap.new returns nothing).
    - Gate wiring: the Makefile gates target includes the nestjs snapshot + determinism tests; the red target no longer lists the nestjs snapshots; if Option B, a tsextract-install step precedes the nestjs tests.
    - Vendoring consistent: .gitignore and the committed tree match the chosen option (either tsextract/node_modules/typescript tracked, or tsextract/node_modules ignored + npm ci wired).
  </acceptance_criteria>
  <done>Both nestjs snapshots are GREEN through real extraction with zero snapshot edits + a toolchain skip-guard; nestjs determinism twins pass; make check is green and deterministic; the gate and vendoring are wired consistently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tsextract -> @nestjs/common decorators | routing decorators are read for ROUTING facts only; schema facts never come from them |
| host test -> tsextract subprocess | the snapshot tests drive build_graph -> run_tsextract -> node over the fixture |
| make check -> node/typescript toolchain | a missing toolchain must skip gracefully, never hard-fail the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Spoofing of facts | routes.js controller-prefix folding | mitigate | the @Controller('books') prefix is recorded as provenance only, NEVER folded into op paths (rule 1); acceptance asserts op paths carry no /books prefix |
| T-04-10 | Tampering | snapshot acceptance | mitigate | ZERO snapshot edits — the committed .snap is the byte-exact spec; git diff --stat must be empty; reconciliation is fixture-side (blank lines/non-fact comments only, rule 1) |
| T-04-11 | Denial of Service | make check on a node-less box | mitigate | skip-if-(node|typescript)-absent guard mirrors the go-toolchain skip; typed TypeScriptToolchainMissing; no panic (RUST-04) |
| T-04-12 | Tampering | non-deterministic graph/openapi | mitigate | nestjs determinism twins assert two-run byte-identical build_graph + to_openapi |
| T-04-06 | Spoofing of facts | routes.js fact source | mitigate | bright line (rule 1): only @nestjs/common routing decorators are read; grep gate asserts no swagger/zod/class-validator in routes.js |
| T-04-SC | Tampering | npm/typescript supply chain | mitigate | sole npm dep typescript pinned + lockfile; vendoring (committed node_modules or npm ci) keeps make check hermetic+deterministic; no second dep |
</threat_model>

<verification>
- Both nestjs snapshots green through real tsextract with ZERO snapshot edits (git diff --stat empty).
- Every span line == snapshot line (golden line test); fixture edits are non-fact only.
- nestjs determinism twins byte-identical; skip-guard skips (not fails) when toolchain absent.
- make check GREEN and deterministic; stale *.snap.new cleaned; gate + vendoring wired consistently.
</verification>

<success_criteria>
- A NestJS service extracts routes/params/request+response DTOs into the neutral IR via real Compiler-API extraction (TSSRC-01).
- snapshot_nestjs_graph + snapshot_nestjs_openapi are GREEN with zero snapshot edits and in the make check gate.
- The fixture is reconciled fact-free; determinism + the toolchain skip-guard hold; vendoring is final and consistent.
</success_criteria>

<output>
Create `.planning/phases/04-typescript-source-tsextract/04-03-SUMMARY.md` when done
</output>
