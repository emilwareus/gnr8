---
phase: 05-typescript-target-tssdk
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - crates/gnr8-core/tests/tssdk_compile.rs
  - Makefile
autonomous: true
requirements: [TSSDK-02]
must_haves:
  truths:
    - "The generated TS SDK type-checks under `tsc --noEmit --strict --lib es2022,dom` using ONLY the vendored typescript — exit 0"
    - "The generated SDK contains no `axios`/`node-fetch`/`@types`/`from \"http\"` imports (supply-chain grep gate)"
    - "The typecheck runs node/tsc with discrete `Command` args + a unique temp `current_dir` — never a shell string"
    - "A missing node/tsc toolchain skips gracefully (no hard suite failure) via `CoreError::TypeScriptToolchainMissing`"
    - "Invalid TS surfaces a captured `CoreError` (exit-code + stderr carrier), not a panic"
    - "The hermetic test is wired into the green `make check` gate"
  artifacts:
    - path: "crates/gnr8-core/tests/tssdk_compile.rs"
      provides: "hermetic generate->write->tsc --noEmit typecheck + banned-import grep + skip guard"
      contains: "es2022,dom"
    - path: "Makefile"
      provides: "tssdk_compile in the check gate"
      contains: "tssdk"
  key_links:
    - from: "crates/gnr8-core/tests/tssdk_compile.rs"
      to: "crate::tssdk::generate + write_to_dir"
      via: "build_graph(fixture) -> generate -> write_to_dir(temp)"
      pattern: "tssdk::generate|tssdk::write_to_dir"
    - from: "crates/gnr8-core/tests/tssdk_compile.rs"
      to: "tsextract/node_modules/typescript/bin/tsc"
      via: "node <tsc> --noEmit --strict --lib es2022,dom (discrete args)"
      pattern: "node_modules/typescript/bin/tsc"
---

<objective>
Create the hermetic acceptance test `crates/gnr8-core/tests/tssdk_compile.rs` — a clone of
`tests/pysdk_compile.rs` — that builds the IR from a fixture, generates the TS SDK, writes it into a
unique stdlib temp dir, and runs the VERIFIED `tsc --noEmit --strict --target es2022 --module esnext
--moduleResolution bundler --lib es2022,dom` invocation over the generated files using ONLY the
vendored `typescript` (no new install). It asserts exit 0 (type-checks), greps for banned runtime
imports (supply-chain gate), captures invalid-TS as a typed error (not a panic), and skips gracefully
if node/tsc is absent. Wire it into the `make check` gate.

Purpose: TSSDK-02 — the load-bearing typecheck gate (the analog of pysdk's `py_compile`+import that
caught real codegen bugs string tests missed); zero runtime deps proven.
Output: `tests/tssdk_compile.rs` + a `make check` wiring edit.
</objective>

<execution_context>
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/home/vercel-sandbox/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-typescript-target-tssdk/05-RESEARCH.md
@.planning/phases/05-typescript-target-tssdk/05-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase + RESEARCH. Implement against these — no exploration needed. -->

From Plan 01 (already merged):
```rust
pub fn crate::tssdk::generate(graph: &ApiGraph, package: &str, base_path: &str) -> Result<String, CoreError>;
pub fn crate::tssdk::write_to_dir(bundle: &str, dir: &std::path::Path) -> Result<(), CoreError>;
```

Build the IR from a fixture (the pysdk_compile.rs pattern): `analyze::build_graph(FIXTURE_DIR)` ->
`ApiGraph` (with `.base_path`).

The VERIFIED tsc invocation (empirically exit 0; `,dom` is LOAD-BEARING — omit it and TS fails with
`error TS2304: Cannot find name 'fetch'`, exit 2):
```
node <repo>/tsextract/node_modules/typescript/bin/tsc \
  --noEmit --strict \
  --target es2022 --module esnext --moduleResolution bundler \
  --lib es2022,dom \
  <each generated .ts path>
```
In Rust: discrete `Command::new("node").args([...])` with `current_dir` = the temp dir (no node_modules
nearby -> no @types leak). NEVER a shell string (V13).

Error variants (crate::CoreError, already exist — no error.rs edit):
- `TypeScriptToolchainMissing { source }` (error.rs:59) — for a node/tsc spawn failure.
- `GoBuild { code, stderr }` — reused by pysdk_compile.rs as the generic exit-code+stderr carrier for a non-zero typecheck (reuse it the same way; no new variant).

The pysdk_compile.rs analog (crates/gnr8-core/tests/pysdk_compile.rs):
- `#![allow(clippy::unwrap_used, clippy::expect_used, clippy::panic)]` (test-only, line 29).
- `unique_temp_dir(label)` (59-69) — PID + nanosecond unique stdlib temp dir (no `tempfile` crate).
- `materialize_sdk()` (102-115) — build_graph -> generate -> write_to_dir into the temp dir.
- the banned-import grep (143-151) — assert ABSENT.
- the discrete-args `Command` + `current_dir` (75-95), skip guard `python_available()` (47-54),
  invalid-input->captured-error test (191-221). DO NOT port the `ROUND_TRIP_DRIVER` http.server test (238-336).

Constants to set:
- `TSC` = `concat!(env!("CARGO_MANIFEST_DIR"), "/../../tsextract/node_modules/typescript/bin/tsc")`
- `FIXTURE_DIR` = `concat!(env!("CARGO_MANIFEST_DIR"), "/../../fixtures/nestjs-bookstore")` (completes NestJS->TS; node present — RESEARCH A2)
- `PACKAGE` = e.g. `"bookstore"`
- `SDK_FILES` = the fixed file order Plan 01's mod.rs emits (`["client.ts", "errors.ts", "index.ts", "models.ts"]`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hermetic tssdk_compile.rs — generate, write, tsc --noEmit, banned-import grep, skip guard</name>
  <files>crates/gnr8-core/tests/tssdk_compile.rs</files>
  <read_first>
    - crates/gnr8-core/tests/pysdk_compile.rs (the full analog: skip guard @47-54, unique_temp_dir @59-69, discrete-args Command @75-95, materialize_sdk @102-115, banned-import grep @143-151, invalid-input->captured-error @191-221)
    - 05-PATTERNS.md "tssdk_compile.rs" section (the verbatim-port list + the one swapped mechanic) and 05-RESEARCH.md "Hermetic Test Mechanics"/"The verified hermetic typecheck invocation"
    - crates/gnr8-core/src/tssdk/mod.rs (Plan 01 output — `generate`/`write_to_dir` signatures + the exact fixed file order)
  </read_first>
  <action>
    Create `crates/gnr8-core/tests/tssdk_compile.rs` cloning `pysdk_compile.rs`. Port VERBATIM (the
    security/hermeticity discipline): the test-only `#![allow(clippy::unwrap_used, ...)]`,
    `unique_temp_dir` relabeled `gnr8-tssdk-compile-`, `materialize_sdk()` (build_graph(FIXTURE_DIR) ->
    `tssdk::generate(&graph, PACKAGE, &graph.base_path)` -> `tssdk::write_to_dir` into the unique temp
    dir), the discrete-args `Command` + `current_dir` discipline (NEVER a shell string), the
    toolchain-skip guard (replace `python_available()` with `node_available()`/`tsc_available()`
    early-return skip), and the banned-import grep over every written `.ts` for `axios`, `node-fetch`,
    `@types`, `from "http"` (assert ABSENT — the TSSDK-02 supply-chain gate). SWAP the Python toolchain
    calls for the ONE tsc run: `Command::new("node").args([TSC, "--noEmit", "--strict", "--target",
    "es2022", "--module", "esnext", "--moduleResolution", "bundler", "--lib", "es2022,dom", <each .ts
    path>])` with `current_dir` = the temp dir. Map a spawn failure to
    `CoreError::TypeScriptToolchainMissing`; map a non-zero exit to the captured-stderr `CoreError::GoBuild
    { code, stderr }` carrier (reuse, no new variant). Add the main typecheck test (assert exit 0) and the
    invalid-input test (write a deliberately broken `.ts`, run tsc, assert a captured `CoreError::GoBuild`
    with non-zero code + non-empty stderr — NOT a panic). DO NOT port the `ROUND_TRIP_DRIVER` http.server
    test (TSSDK-02 asks only for `tsc --noEmit`, RESEARCH Open Q3). Set `FIXTURE_DIR` to the
    nestjs-bookstore fixture; `node` runs both tsextract (for the IR) and tsc.
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test tssdk_compile</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p gnr8-core --test tssdk_compile` passes (node/tsc present per RESEARCH Environment Availability): the generated SDK type-checks (`tsc --noEmit` exit 0) and the invalid-input test captures a typed error.
    - The `--lib es2022,dom` flag is present in the invocation (verify: `grep -n 'es2022,dom' crates/gnr8-core/tests/tssdk_compile.rs`) — load-bearing for resolving the `fetch` global without `@types/node`.
    - The banned-import grep asserts no `axios`/`node-fetch`/`@types`/`from "http"` in any generated `.ts` (verify the assertions exist and the test passes).
    - The tsc invocation uses discrete `Command::new("node").args([...])` — no `sh -c`/shell string (verify: no `"sh"`/`"-c"` and no format!-built command string passed to the shell).
    - The temp dir is PID+nanosecond unique (`unique_temp_dir`); no `tempfile` crate added (rule 2).
    - A missing node/tsc early-returns (skip), not a panic; invalid TS yields `CoreError::GoBuild`/`TypeScriptToolchainMissing`, not a panic.
    - No new gnr8-core crate and no npm install (verify: `git diff --stat Cargo.toml` shows no new dependency; `tsextract/node_modules` unchanged).
  </acceptance_criteria>
  <done>tssdk_compile.rs builds the IR from the nestjs fixture, generates+writes the TS SDK, type-checks it with `tsc --noEmit --strict --lib es2022,dom` (exit 0), grep-gates banned imports, captures invalid TS as a typed error, and skips if node/tsc absent.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tssdk_compile into the make check gate</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (the `check` target + how `pysdk_compile`/`sdk_compile` integration tests are run in the existing gate)
  </read_first>
  <action>
    Edit the `check` target in the `Makefile` so the `tssdk_compile` integration test runs as part of the
    green gate, mirroring how `pysdk_compile` is invoked (e.g. the `cargo test -p gnr8-core` / full-suite
    line already includes integration tests, or an explicit `--test tssdk_compile` line if the gate lists
    them individually). Match the existing pattern exactly — do not restructure the Makefile.
  </action>
  <verify>
    <automated>make check</automated>
  </verify>
  <acceptance_criteria>
    - `make check` runs green and exercises `tssdk_compile` (the typecheck runs as part of the gate; verify the target's output includes the tssdk_compile test, or `grep -n tssdk Makefile` shows it wired if the gate lists tests individually).
    - The Makefile structure is otherwise unchanged (only the `check` gate extended, mirroring the pysdk_compile wiring).
  </acceptance_criteria>
  <done>`make check` is green and includes the hermetic TS SDK typecheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated `.ts` files → tsc subprocess | The typecheck must run with controlled discrete args and a clean temp dir so no ambient `@types`/tsconfig leaks in. |
| generated SDK imports → consumer supply chain | Must be grep-proven to contain zero third-party runtime deps. |
| toolchain spawn (node/tsc) → test outcome | A missing/erroring toolchain must produce a typed error or a graceful skip, never a panic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering (command injection) | the tsc invocation | mitigate | Discrete `Command::new("node").args([...])` + `current_dir`; never `sh -c`/a shell string (V13, the pysdk_compile discipline). |
| T-05-03-02 | Tampering (supply chain) | generated SDK imports | mitigate | Grep every written `.ts` for `axios`/`node-fetch`/`@types`/`from "http"`; assert ABSENT (TSSDK-02 supply-chain gate). |
| T-05-03-03 | Tampering | ambient type/tsconfig leak | mitigate | Materialize the SDK into a PID+nanosecond-unique temp dir with no nearby `node_modules`; flags-only `--lib es2022,dom` (no `@types` present to auto-include — RESEARCH Pitfall 3). |
| T-05-03-04 | DoS | missing/erroring toolchain | mitigate | Skip guard early-returns if node/tsc absent; spawn failure -> `TypeScriptToolchainMissing`; non-zero exit -> captured `GoBuild { code, stderr }`; no panic. |
| T-05-03-SC | Tampering | npm/cargo installs | mitigate | This plan installs NOTHING: `typescript` 5.9.3 is already vendored (Phase 4, committed lockfile, offline); no new crate; no npm action. The typecheck reuses the vendored compiler. Slopcheck N/A per RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
- `cargo test -p gnr8-core --test tssdk_compile` green: `tsc --noEmit --strict --lib es2022,dom` exits 0 on the generated SDK.
- Banned-import grep asserts no `axios`/`node-fetch`/`@types` in any generated `.ts`.
- `make check` green, including the hermetic typecheck.
- No new crate / no npm install (vendored typescript reused).
</verification>

<success_criteria>
- The generated TS SDK type-checks hermetically under `tsc --noEmit` using only the vendored compiler (exit 0).
- Zero runtime deps proven by the banned-import grep gate.
- Invalid TS is captured as a typed `CoreError`, not a panic; a missing toolchain skips gracefully.
- `cargo test -p gnr8-core --test tssdk_compile` and `make check` pass.
</success_criteria>

<output>
Create `.planning/phases/05-typescript-target-tssdk/05-03-SUMMARY.md` when done.
</output>
