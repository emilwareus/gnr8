---
milestone: v2.0
audited: 2026-06-26
status: passed
scores:
  requirements: 24/24
  phases: 6/6
  integration: 5/5
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 05-typescript-target-tssdk
    items:
      - "Backlog 999.1 — WR-02: TsSdk non-scalar query-param String() wire-encoding rule (deferred; folding it changes the green TsSdk snapshots)"
      - "Backlog 999.2 — WR-04: TsSdk asymmetric success/error JSON decode (same reason)"
  - phase: 01-language-neutral-ir-facts-contract-fixtures
    items:
      - "Nyquist: 01-VALIDATION.md carries nyquist_compliant: false (infrastructure phase; phase verified passed — a doc-flag PARTIAL, not a coverage gap)"
  - phase: carried-v1-debt
    items:
      - "gnr8-core OSS known-debt (serde/serde_json/blake3/thiserror) — documented in CLAUDE.md, deferred by design; v2.0 added ZERO new deps"
      - "goextract golang.org/x/tools (+x/mod, x/sync) — v1 known-debt to retire stdlib-only later"
      - "goextract compile-time path (relocatable install) — carried v1 debt"
nyquist:
  compliant_phases: [02, 03, 04, 05, 06]
  partial_phases: [01]
  missing_phases: []
  overall: partial
---

# Milestone v2.0 — Audit: PASSED

**Milestone:** v2.0 — Multi-language: TypeScript & Python (parse + generate)
**Audited:** 2026-06-26
**Status:** passed — all 24 requirements satisfied, all 6 phases verified, all 5 cross-phase seams wired, all 3 E2E flows complete. Tech debt is minimal and already tracked (backlog 999.x + documented v1 known-debt).

## Requirements coverage — 24/24 satisfied (3-source cross-referenced)

Every REQ-ID is `[x]` in REQUIREMENTS.md traceability, listed in its phase's SUMMARY frontmatter, and its phase VERIFICATION.md is `passed`.

| Group | IDs | Phase | Status |
|-------|-----|-------|--------|
| IR | IR-01..04 | 1 | satisfied |
| PYSRC | PYSRC-01..05 | 2 | satisfied |
| PYSDK | PYSDK-01..03 | 3 | satisfied |
| TSSRC | TSSRC-01..04 | 4 | satisfied |
| TSSDK | TSSDK-01..03 | 5 | satisfied |
| XLANG | XLANG-01..05 | 6 | satisfied |

No unsatisfied requirements. No orphans (every traceability ID appears in a phase VERIFICATION.md).

## Phase verifications — 6/6 passed

| Phase | Verification | Score |
|-------|-------------|-------|
| 1 Language-Neutral IR + Facts Contract + Fixtures | passed | 4/4 |
| 2 Python Source — pyextract | passed | 5/5 |
| 3 Python Target — PySdk | passed | 3/3 |
| 4 TypeScript Source — tsextract | passed | 4/4 |
| 5 TypeScript Target — TsSdk | passed | 3/3 |
| 6 Cross-Language Hardening + Examples + Docs | passed | 5/5 |

## Cross-phase integration — 5/5 seams WIRED (runtime-verified)

1. **Narrow waist (IR-03):** all 4 language sources deserialize into the SAME neutral facts DTO → `build_graph` → `lower::to_openapi` + SDK targets, with NO per-language branch in lowering (exhaustive `Type` match, no `_=>`).
2. **Single `detect_language` dispatch (rule 3):** identical one-decision routing in both `build_graph` and `diagnostics::collect`; mixed/empty tree → typed error; no fallback.
3. **E2E examples:** Go (bookstore), Python (fastapi-bookstore), TypeScript (nestjs-bookstore) each `.gnr8/` Pipeline → committed output; `make examples-check` regenerates byte-identical (`0 written, N unchanged` per language).
4. **doctor/check/watch parity:** single `source_toolchain` call per site dispatching on the detected language (Go→go, Python→python3, TS→node+typescript-resolvable).
5. **Determinism + zero-new-OSS:** gnr8-core deps unchanged vs v1 baseline; `make check` green.

Integration warning found AND resolved during this audit: `examples/taskflow` (a v1 Go custom-stage demo, outside v2.0 scope/gates) referenced the removed `Schema.kind`; repaired to the neutral `Type` body (exhaustive match) — now compiles, generates, and `gnr8 check`-clean.

## E2E flows — 3/3 complete

Go / Python / TypeScript each drive `.gnr8/` Pipeline → Source → sidecar → neutral facts → graph → OpenApi31 + SDK → committed bytes that regenerate byte-identically.

## Nyquist coverage — partial (non-blocking, discovery-only)

| Phase | VALIDATION.md | Compliant |
|-------|---------------|-----------|
| 1 | exists | false (infra phase; verified passed) |
| 2–6 | exists | true |

Phase 1's `nyquist_compliant: false` is a doc flag on the infrastructure phase (verified passed); not a coverage gap. Optionally run `/gsd:validate-phase 1`.

## Tech debt (tracked, non-blocking)

- **Backlog 999.1 / 999.2** (ROADMAP): WR-02 / WR-04 TsSdk hardening (deferred — would change green snapshots).
- **v1 known-debt** (CLAUDE.md `## Known debt`, deferred by design): gnr8-core OSS deps (serde/serde_json/blake3/thiserror); goextract golang.org/x/tools; goextract compile-time path. v2.0 added ZERO new OSS.

## Verdict

PASSED. The `ApiGraph` IR is proven a true language-neutral narrow waist across Go / FastAPI / Flask / NestJS; all v1 invariants hold (one source per fact, no fallback paths, deterministic byte-identical output); gnr8-core ships zero OSS and `typescript` is recorded as a required user toolchain. Ready to complete the milestone.
