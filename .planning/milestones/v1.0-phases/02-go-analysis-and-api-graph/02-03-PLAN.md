---
phase: 02-go-analysis-and-api-graph
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - crates/gnr8-core/src/graph/mod.rs
  - crates/gnr8-core/src/analyze/mod.rs
  - crates/gnr8-core/src/diagnostics/mod.rs
  - crates/gnr8/src/main.rs
  - crates/gnr8/src/cli.rs
  - crates/gnr8/src/render.rs
  - crates/gnr8-core/tests/snapshot_graph.rs
  - crates/gnr8-core/tests/snapshot_diagnostics.rs
  - crates/gnr8-core/tests/snapshots/snapshot_graph__goalservice_graph.snap
  - crates/gnr8-core/tests/snapshots/snapshot_diagnostics__goalservice_diagnostics.snap
  - crates/gnr8-core/tests/determinism.rs
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GO-06]
must_haves:
  truths:
    - "analyze::build_graph(fixture_dir) returns a populated ApiGraph (routes, operations, params, request bodies, responses by status, schemas with fields, source provenance on every node) instead of NotYetImplemented."
    - "Operation IDs and schema IDs are deterministic and stable: @ID annotation when present (goalUuidPut) else derived from method+normalized path; schema IDs from package-qualified type name; all serialized output sorted by a stable key so unchanged source => byte-identical output."
    - "diagnostics::collect(fixture_dir) returns text reconciled with fixtures/goalservice/expected/diagnostics.txt (the 7 WARN lines: float64×3, free-form-map×1, untyped-query×3)."
    - "snapshot_graph and snapshot_diagnostics flip from red-by-design to GREEN with real reviewed .snap files; snapshot_openapi and snapshot_sdk stay red-by-design (Phase 3)."
    - "inspect routes | schemas | graph render human-readable tables by default and machine JSON under the global --json flag, each surfacing diagnostics."
    - "Running build_graph twice on the unchanged fixture yields byte-identical serialized graphs (GRAPH-02), and every subprocess failure path returns a typed CoreError, never a panic (GO-06)."
  artifacts:
    - path: "crates/gnr8-core/src/graph/mod.rs"
      provides: "Populated ApiGraph + child structs + from_facts + stable ID derivation + sorted serialization"
      contains: "impl ApiGraph"
      min_lines: 80
    - path: "crates/gnr8-core/src/diagnostics/mod.rs"
      provides: "collect() -> String reconciled with expected/diagnostics.txt"
      contains: "pub fn collect"
    - path: "crates/gnr8/src/render.rs"
      provides: "Table + JSON renderers for inspect routes|schemas|graph"
      contains: "fn render"
    - path: "crates/gnr8-core/tests/snapshots/snapshot_graph__goalservice_graph.snap"
      provides: "The real reviewed graph snapshot (turns snapshot_graph green)"
    - path: "crates/gnr8-core/tests/snapshots/snapshot_diagnostics__goalservice_diagnostics.snap"
      provides: "The real reviewed diagnostics snapshot (turns snapshot_diagnostics green)"
    - path: "crates/gnr8-core/tests/determinism.rs"
      provides: "Two-run byte-identical graph assertion (GRAPH-02)"
      contains: "build_graph"
  key_links:
    - from: "crates/gnr8-core/src/analyze/mod.rs"
      to: "crates/gnr8-core/src/graph/mod.rs"
      via: "build_graph runs helper (02-01) -> GoFacts -> ApiGraph::from_facts"
      pattern: "ApiGraph::from_facts"
    - from: "crates/gnr8-core/src/diagnostics/mod.rs"
      to: "crates/gnr8-core/src/analyze (GoFacts diagnostics)"
      via: "collect renders the facts diagnostics to canonical text"
      pattern: "diagnostics"
    - from: "crates/gnr8/src/main.rs"
      to: "crates/gnr8/src/render.rs"
      via: "inspect arms call render(graph, json)"
      pattern: "render"
    - from: "crates/gnr8-core/tests/snapshot_graph.rs"
      to: "crates/gnr8-core/src/analyze/build_graph"
      via: "assert_yaml_snapshot of the real graph (no longer red-by-design)"
      pattern: "assert_yaml_snapshot"
---

<objective>
Build the router-agnostic Rust `ApiGraph` from the `GoFacts` JSON the helper emits (02-01 schemas + 02-02
routes), implement `analyze::build_graph` (run helper → deserialize → `ApiGraph::from_facts`) and
`diagnostics::collect`, derive deterministic stable operation/schema IDs with fully sorted serialization
(GRAPH-02), wire the three `inspect routes|schemas|graph` CLI arms to human tables + `--json`, and flip the
two red-by-design contract tests (`snapshot_graph`, `snapshot_diagnostics`) to REAL reviewed snapshots —
reconciling the diagnostics text with `fixtures/goalservice/expected/diagnostics.txt`. `snapshot_openapi` and
`snapshot_sdk` stay red-by-design (Phase 3).

Purpose: This is the phase's deliverable — the inspectable, stable, router-agnostic API graph + diagnostics +
inspect reports. It closes GRAPH-01/02/03 and the GO-06 no-panic/no-silent-drop contract, and turns the two
Phase-1 contract tests green, which is the phase gate.

Output: populated `ApiGraph` + `from_facts` + stable IDs + sorted serialization; `build_graph` and
`diagnostics::collect` implemented; `inspect routes|schemas|graph` renderers (table + JSON); two real `.snap`
files; an end-to-end determinism test. Requires the Go toolchain (the snapshot/determinism tests invoke the
helper via `go run`).
</objective>

<execution_context>
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-go-analysis-and-api-graph/02-CONTEXT.md
@.planning/phases/02-go-analysis-and-api-graph/02-RESEARCH.md
@.planning/phases/02-go-analysis-and-api-graph/02-VALIDATION.md

# REQUIRED: 02-01 + 02-02 SUMMARYs — the final GoFacts JSON shape this plan deserializes into the graph:
@.planning/phases/02-go-analysis-and-api-graph/02-01-SUMMARY.md
@.planning/phases/02-go-analysis-and-api-graph/02-02-SUMMARY.md

# The seams this plan implements (current stubs return NotYetImplemented):
@crates/gnr8-core/src/graph/mod.rs
@crates/gnr8-core/src/analyze/mod.rs
@crates/gnr8-core/src/diagnostics/mod.rs

# The two contract tests to flip GREEN + the CLI surface to wire:
@crates/gnr8-core/tests/snapshot_graph.rs
@crates/gnr8-core/tests/snapshot_diagnostics.rs
@crates/gnr8/src/main.rs
@crates/gnr8/src/cli.rs

# The diagnostics acceptance target the snapshot must reconcile with (7 exact WARN lines):
@fixtures/goalservice/expected/diagnostics.txt

<interfaces>
<!-- Contracts the executor needs. From the existing seams + 02-01/02-02 SUMMARYs. -->

Existing seam signatures (KEEP these public signatures — implement the bodies):
  analyze::build_graph(fixture_dir: &str) -> Result<graph::ApiGraph, CoreError>   (crates/gnr8-core/src/analyze/mod.rs:10)
  diagnostics::collect(fixture_dir: &str) -> Result<String, CoreError>            (crates/gnr8-core/src/diagnostics/mod.rs:10)
  ApiGraph currently: #[derive(Debug, Default, serde::Serialize)] pub struct ApiGraph {}  (graph/mod.rs) — FLESH OUT.

From 02-01: analyze::helper::run_goextract(target_dir) -> Result<GoFacts, CoreError>; analyze::facts::GoFacts
  and child DTOs (routes, schemas, diagnostics, with SourceSpan). build_graph calls run_goextract then maps
  GoFacts -> ApiGraph.

CLI (crates/gnr8/src/cli.rs): Commands::Inspect{ action: InspectAction::{Routes,Schemas,Graph} }, global
  `json: bool`. main.rs dispatch() currently returns not_yet for the 3 inspect arms — replace with real calls
  into build_graph + render. The NotYetImplemented arm in main.rs stays for init/generate/watch/check/doctor;
  the inspect arms now succeed, so the Ok(report) render path + a possible real-error path must both compile
  (main.rs already has the #[allow(unreachable_patterns)] Err(e)=>Err(e.into()) anyhow boundary for real errors).

Stable IDs (RESEARCH §"Stable operation/schema IDs", D-08): operation_id = facts.operation_id when present
  (goalUuidPut) else derived from method + normalized path (lowercase, deterministic, keep {param} stripped or
  consistent — pick one and snapshot it). schema_id = the package-qualified name from the helper (already stable).
  Sort: routes by (path, method); schemas by id; params by name; responses by status; fields by json name.

insta authoring (RESEARCH §7, Pitfall 4): snapshot_graph uses assert_yaml_snapshot; snapshot_diagnostics uses
  assert_snapshot (plain text). Author from REAL output via `cargo insta test` then `cargo insta accept`
  (install: `cargo install cargo-insta`); CI runs INSTA_UPDATE=no (a mismatch hard-fails, never auto-accepts).
  Diagnostics canonical phrasing: reconcile with expected/diagnostics.txt — normalize to one template per rule
  (Open Q2 recommendation) OR reproduce the file's lines; the .snap locks the final exact text.

Diagnostics ordering for the 7 lines: the helper sorts by (file,line,message). expected/diagnostics.txt order
  is float64×3 (Create/Update/GoalResponse .TargetValue) then free-form-map×1 (GoalResponse.Metadata) then
  untyped-query×3 (cursor/page_size/aggregation). Decide the collect() ordering to match a stable, reviewable
  sequence; document it in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate ApiGraph + from_facts + stable IDs + sorted serialization; implement build_graph + diagnostics::collect</name>
  <files>crates/gnr8-core/src/graph/mod.rs, crates/gnr8-core/src/analyze/mod.rs, crates/gnr8-core/src/diagnostics/mod.rs</files>
  <read_first>
    - .planning/phases/02-go-analysis-and-api-graph/02-RESEARCH.md (§4.2 ApiGraph layout; §"Stable operation/schema IDs"; D-07/D-08; Pitfall 4 diagnostics text)
    - .planning/phases/02-go-analysis-and-api-graph/02-01-SUMMARY.md + 02-02-SUMMARY.md (final GoFacts shape; diagnostics set emitted)
    - crates/gnr8-core/src/graph/mod.rs (the empty ApiGraph to flesh out — keep router-agnostic, D-03)
    - crates/gnr8-core/src/analyze/mod.rs (build_graph stub + the 02-01 facts/helper modules)
    - fixtures/goalservice/expected/diagnostics.txt (the 7 WARN lines collect() reconciles with)
    - thoughts/skills/rust-best-practices/references/chapter_01.md (iterators, Option/Result) + chapter_05.md (snapshot testing, one assertion per test)
  </read_first>
  <behavior>
    - graph unit test: ApiGraph::from_facts(sample GoFacts) yields routes sorted by (path,method); a route's operation_id is the @ID value when present else the method+path derivation; schemas sorted by id; every node carries a SourceSpan (provenance).
    - graph unit test: serializing the same ApiGraph twice yields byte-identical output (sorted collections; no HashMap iteration in output).
    - diagnostics unit test: collect() over the sample facts returns the 7 expected lines in the documented stable order, each a WARN with the struct.field or route identity and a file reference, reconciled with expected/diagnostics.txt.
    - analyze unit test: build_graph maps a GoToolchainMissing/HelperExit/FactsParse from the helper into the returned CoreError (no panic) — reuse the 02-01 helper error mapping.
  </behavior>
  <action>
    In graph/mod.rs: flesh out ApiGraph with router-agnostic child structs (D-03 — NO Gin/framework fields):
    Operation{ id, method, path, summary: Option, tags, secured, params: Vec<Param>, request_body: Option<SchemaRef>,
    responses: Vec<Response>, provenance: SourceSpan }, Param{ name, location, required, schema, enum_values,
    description, provenance }, Response{ status, body: Option<SchemaRef>, description }, Schema{ id, name, kind,
    fields: Vec<Field>, enum_values, provenance }, Field{ json_name, required, optional, schema, description,
    example }, plus the SchemaType already defined in analyze::facts (reuse or mirror). Derive Debug + Serialize
    (insta yaml). Use sorted Vec / BTreeMap so serialization is deterministic (GRAPH-02) — never a HashMap into
    output order. Provenance (SourceSpan) on every node (D-07).

    Add `impl ApiGraph { pub fn from_facts(facts: GoFacts) -> Self }` mapping RouteFacts->Operations (operation_id
    per the stable-ID rule: facts.operation_id else method+normalized-path derivation, documented + snapshotted),
    request_body/responses TypeRef->SchemaRef, SchemaFacts->Schemas. Sort routes by (path,method), schemas by id,
    params by name, responses by status, fields by json_name BEFORE building the final collections.

    In analyze/mod.rs: implement build_graph = run_goextract(fixture_dir)? -> ApiGraph::from_facts(facts) -> Ok.
    Remove the not_yet stub for build_graph (keep the helper/facts modules from 02-01). Update lib.rs's
    `build_graph_is_not_yet_implemented` test: it now expects success OR — if the Go toolchain is unavailable in
    the test env — a typed CoreError::GoToolchainMissing (NOT NotYetImplemented). Adjust that test accordingly
    (rename to reflect real behavior, keep no unwrap in lib).

    In diagnostics/mod.rs: implement collect = run_goextract(fixture_dir)? then render facts.diagnostics to the
    canonical text (one line per diagnostic, stable order, reconciled with expected/diagnostics.txt). Normalize to
    one template per rule (float64-narrowing / free-form-map / untyped-query) carrying the field/route identity
    (Pitfall 4 / Open Q2). Return the joined String. NO unwrap/expect (RUST-04).

    All sorting and ID derivation deterministic. Keep clippy clean (pedantic warns; unwrap/expect/panic deny).
  </action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo clippy --all-targets --all-features --locked -- -D warnings</automated>
    <automated>cargo test -p gnr8-core --lib graph</automated>
    <automated>cargo test -p gnr8-core --lib diagnostics</automated>
    <automated>cargo test -p gnr8-core --lib analyze</automated>
  </verify>
  <done>
    graph/mod.rs has a populated router-agnostic ApiGraph + from_facts with stable IDs and sorted, deterministic serialization, provenance on every node. build_graph runs the helper and builds the graph (typed errors on failure, no panic). diagnostics::collect renders the 7-line canonical text reconciled with expected/diagnostics.txt. lib unit tests + clippy + fmt clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire inspect routes|schemas|graph renderers (table + --json) into the gnr8 binary</name>
  <files>crates/gnr8/src/render.rs, crates/gnr8/src/main.rs, crates/gnr8/src/cli.rs</files>
  <read_first>
    - .planning/phases/02-go-analysis-and-api-graph/02-RESEARCH.md (§7 inspect rendering; D-09 table default / --json toggle)
    - crates/gnr8/src/main.rs (dispatch() + Report render path + the anyhow boundary; the 3 inspect arms to replace)
    - crates/gnr8/src/cli.rs (InspectAction::{Routes,Schemas,Graph}; global json flag)
    - .planning/phases/02-go-analysis-and-api-graph/02-CONTEXT.md (D-09 reports explain inferred facts + list diagnostics)
  </read_first>
  <action>
    Add crates/gnr8/src/render.rs with three renderers consuming the gnr8_core::graph::ApiGraph (+ diagnostics):
    `render_routes`, `render_schemas`, `render_graph`. Each takes the graph and a `json: bool`. When json: emit
    serde_json::to_string_pretty of the relevant slice (routes/schemas/whole graph) — keep it the machine form.
    When human: print a simple aligned text table (no extra table crate — RESEARCH "no new Rust crates"; use
    plain `{:<width}` formatting or writeln!). routes table columns: METHOD, PATH, OPERATION, SECURED, REQUEST,
    RESPONSES; schemas table: ID/NAME, KIND, FIELDS (count or names), ENUM; graph: a compact combined view. Each
    report appends the diagnostics list (from diagnostics::collect) so reports "explain inferred facts and list
    diagnostics" (D-09).

    In main.rs dispatch(): replace the 3 `not_yet("inspect ...", 2)` arms with calls that build_graph(fixture_dir)
    + collect diagnostics and route to the render functions. The inspect commands need a target dir — for this
    phase default it to the goalservice fixture path (resolve like the contract tests, or accept it as a positional
    arg on the Inspect subcommand if cli.rs is extended; prefer adding an optional `path` arg defaulting to the
    fixture so `gnr8 inspect routes` works out of the box AND `gnr8 inspect routes <dir>` works). Keep init/generate/
    watch/check/doctor returning NotYetImplemented. Ensure the success path renders (human or --json) and any real
    CoreError (e.g. GoToolchainMissing) flows through the existing anyhow boundary as a clean message + exit 1
    (not a panic). Add a small render unit/integration test (or assert via `cargo test -p gnr8`).

    If cli.rs gains an optional path arg on Inspect, update its existing parse tests so they still pass.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo clippy --all-targets --all-features --locked -- -D warnings</automated>
    <automated>cargo test -p gnr8</automated>
    <automated>cargo run -p gnr8 -- inspect routes fixtures/goalservice | grep -Eiq 'POST|GET|PUT|DELETE' && echo "routes table OK"</automated>
    <automated>cargo run -p gnr8 -- --json inspect schemas fixtures/goalservice | python3 -c "import sys,json; json.load(sys.stdin); print('json schemas OK')"</automated>
  </verify>
  <done>
    render.rs renders routes/schemas/graph as human tables and as --json; main.rs inspect arms call build_graph + render and list diagnostics; `gnr8 inspect routes fixtures/goalservice` shows the route table and `--json inspect schemas` emits valid JSON. cli.rs tests still pass. clippy + fmt clean. A real toolchain error surfaces cleanly (no panic).
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip snapshot_graph + snapshot_diagnostics to real reviewed snapshots; add end-to-end determinism test</name>
  <files>crates/gnr8-core/tests/snapshot_graph.rs, crates/gnr8-core/tests/snapshot_diagnostics.rs, crates/gnr8-core/tests/snapshots/snapshot_graph__goalservice_graph.snap, crates/gnr8-core/tests/snapshots/snapshot_diagnostics__goalservice_diagnostics.snap, crates/gnr8-core/tests/determinism.rs</files>
  <read_first>
    - crates/gnr8-core/tests/snapshot_graph.rs + snapshot_diagnostics.rs (the red-by-design mechanism: .expect() on the seam; FIXTURE_DIR resolution)
    - fixtures/goalservice/expected/diagnostics.txt (the 7 WARN lines the diagnostics .snap must reconcile with)
    - .planning/phases/02-go-analysis-and-api-graph/02-RESEARCH.md (§7 authoring snapshots from real output; Pitfall 4; §"Sampling Rate" phase gate)
    - .planning/phases/02-go-analysis-and-api-graph/02-VALIDATION.md (determinism double-run check; phase gate = full suite green except snapshot_openapi/sdk)
    - Makefile + .github/workflows/ci.yml (contract job stays non-blocking this phase; promotion to blocking is Phase 3)
  </read_first>
  <action>
    The two tests already call the now-real seams (build_graph / diagnostics::collect) and assert via insta; their
    bodies need no logic change (the .expect() now succeeds because the seams are implemented). Update only the
    red-by-design header comments to reflect they are now GREEN (remove "red-by-design until then" language) and
    keep the scoped `#![allow(clippy::unwrap_used, clippy::expect_used)]`.

    Author the two real .snap files from ACTUAL output: install cargo-insta (`cargo install cargo-insta`), run
    `cargo insta test -p gnr8-core --test snapshot_graph --test snapshot_diagnostics`, then `cargo insta accept`.
    Review the captured snapshots: the graph .snap must show all 4 operations (with operation_id goalUuidPut for
    PUT), request/response schema refs, params incl. the aggregation enum, secured=true, provenance spans, and all
    8 schemas + the TargetDirection enum. The diagnostics .snap must contain the 7 WARN lines reconciled with
    fixtures/goalservice/expected/diagnostics.txt (float64×3, free-form-map×1, untyped-query×3) in the documented
    stable order. Commit both .snap files (they are the locked contract). Do NOT touch the snapshot_openapi /
    snapshot_sdk tests or author their snapshots — they stay red-by-design for Phase 3.

    Add tests/determinism.rs: an integration test that calls gnr8_core::analyze::build_graph(FIXTURE_DIR) twice,
    serializes each to a String (serde_yaml or serde_json via the graph's Serialize), and asserts the two strings
    are byte-identical (GRAPH-02). Gate it to skip gracefully (return early, not fail) if the Go toolchain is
    absent — but on this dev machine + CI (go 1.26 available) it must run and pass. Use the same scoped
    unwrap/expect allow as the other test targets.

    Confirm `make gates` (blocking) still passes and `make contract` now shows snapshot_graph + snapshot_diagnostics
    GREEN with only snapshot_openapi + snapshot_sdk RED.
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test snapshot_graph</automated>
    <automated>cargo test -p gnr8-core --test snapshot_diagnostics</automated>
    <automated>cargo test -p gnr8-core --test determinism</automated>
    <automated>cargo test -p gnr8-core --test snapshot_openapi 2>&1 | grep -Eiq 'FAILED|panic' && echo "openapi still red-by-design (expected)"</automated>
    <automated>make gates</automated>
  </verify>
  <done>
    snapshot_graph + snapshot_diagnostics are GREEN with real reviewed .snap files committed (diagnostics reconciled with expected/diagnostics.txt's 7 lines). determinism.rs proves two build_graph runs are byte-identical. snapshot_openapi + snapshot_sdk remain red-by-design (Phase 3). `make gates` blocking gate passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GoFacts JSON → ApiGraph | The helper's JSON (already serde-validated with deny_unknown_fields in 02-01) is mapped into the Rust-owned graph; the graph is the trusted source of truth. |
| CLI arg (inspect path) → build_graph → `go run` | The inspect target dir flows to the 02-01 subprocess driver, which passes it as a discrete Command arg (no shell). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | non-deterministic graph serialization (HashMap iteration) | mitigate | Use sorted Vec / BTreeMap and sort-before-build in from_facts; determinism.rs asserts two runs byte-identical (GRAPH-02). |
| T-02-11 | DoS | panic when the Go toolchain is missing during inspect/build_graph | mitigate | build_graph propagates CoreError::GoToolchainMissing (02-01) via `?`; main.rs anyhow boundary renders it as a clean message + exit 1, never a panic (GO-06). |
| T-02-12 | Repudiation/Integrity | snapshot auto-accept hiding a regression | mitigate | CI runs insta in INSTA_UPDATE=no (CI=true); a mismatched/missing .snap hard-fails, never auto-accepts (existing CI policy). |
| T-02-13 | Tampering | inspect path arg injected into a shell | mitigate | Reuses the 02-01 discrete-arg Command driver (T-02-01); inspect never builds a shell string. |
</threat_model>

<verification>
- `cargo test -p gnr8-core --test snapshot_graph --test snapshot_diagnostics --test determinism` — all GREEN.
- `cargo test -p gnr8-core --test snapshot_openapi --test snapshot_sdk` — still RED-by-design (Phase 3 unchanged).
- `cargo fmt --all -- --check` + `cargo clippy --all-targets --all-features --locked -- -D warnings` — clean (RUST-03/RUST-04).
- `cargo run -p gnr8 -- inspect routes fixtures/goalservice` shows the route table; `--json inspect schemas` emits valid JSON (GRAPH-03).
- `make gates` (blocking) passes; the `contract` job now has 2 of 4 snapshots green.
- Determinism: build_graph twice → byte-identical (GRAPH-02). Diagnostics text reconciled with expected/diagnostics.txt's 7 WARN lines (GO-06/D-10).
</verification>

<success_criteria>
- analyze::build_graph + diagnostics::collect are implemented; the router-agnostic ApiGraph models routes, operations, params, request bodies, responses, schemas, and source provenance with deterministic stable IDs and fully sorted serialization (GRAPH-01, GRAPH-02).
- inspect routes|schemas|graph render human tables by default and machine JSON under --json, each surfacing diagnostics (GRAPH-03).
- snapshot_graph + snapshot_diagnostics are GREEN with real reviewed snapshots; the diagnostics text reconciles with expected/diagnostics.txt's 7 WARN lines; snapshot_openapi + snapshot_sdk stay red-by-design (GO-06, phase gate).
- Two build_graph runs are byte-identical and every subprocess failure path is a typed CoreError, never a panic.
</success_criteria>

<output>
Create `.planning/phases/02-go-analysis-and-api-graph/02-03-SUMMARY.md` when done — record the final ApiGraph struct layout, the stable-ID derivation actually used, the diagnostics canonical phrasing/order chosen (and how it reconciles with expected/diagnostics.txt), the inspect path-arg behavior, and confirmation that snapshot_graph + snapshot_diagnostics are green while snapshot_openapi + snapshot_sdk remain red-by-design for Phase 3.
</output>
