---
phase: 03-openapi-and-go-sdk-generation
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - crates/gnr8-core/src/sdk/mod.rs
  - crates/gnr8-core/tests/sdk_compile.rs
  - crates/gnr8-core/tests/determinism.rs
  - Makefile
  - .github/workflows/ci.yml
autonomous: true
requirements: [SDK-05]
tags: [rust, go-build, httptest, hermetic, ci, determinism, integration-test]

must_haves:
  truths:
    - "A new tests/sdk_compile.rs materializes the generated SDK to a hermetic temp dir with a stdlib-only go.mod (zero requires), runs `go build ./...`, and it succeeds (SDK-05 compiles)"
    - "The same test writes a fixed httptest-based smoke *_test.go, runs `go test ./...`, and it passes — constructing the Client and calling a fixture operation, asserting method/path/body + response decode + a 4xx→APIError path (SDK-05 exercised; SDK-04 typed error)"
    - "The compile test is hermetic: the generated go.mod has zero external requires so go build/test never touches the network (GOPROXY=off-safe); it skips gracefully if the Go toolchain is absent"
    - "A `go build` non-zero exit maps to CoreError::GoBuild, never a panic"
    - "Determinism is asserted for both artifacts: to_openapi twice and generate twice are byte-identical"
    - "All four contract tests (snapshot_graph/diagnostics/openapi/sdk) plus sdk_compile run in the BLOCKING CI gates job; the non-blocking contract job is retired (D-07)"
  artifacts:
    - path: "crates/gnr8-core/tests/sdk_compile.rs"
      provides: "Integration test: temp dir + stdlib-only go.mod + go build + httptest smoke (SDK-05) + 4xx→APIError"
      min_lines: 80
    - path: "crates/gnr8-core/tests/determinism.rs"
      provides: "Extended with to_openapi + sdk::generate two-run byte-identical assertions"
      contains: "to_openapi"
    - path: ".github/workflows/ci.yml"
      provides: "snapshot_openapi/snapshot_sdk/sdk_compile promoted to blocking gates; contract job removed"
      contains: "sdk_compile"
    - path: "Makefile"
      provides: "gates target includes the four contract tests + sdk_compile; contract target retired"
      contains: "sdk_compile"
  key_links:
    - from: "crates/gnr8-core/tests/sdk_compile.rs"
      to: "crates/gnr8-core/src/sdk/mod.rs"
      via: "generate(&graph) + write_to_dir(&bundle, dir)"
      pattern: "sdk::(generate|write_to_dir)"
    - from: "crates/gnr8-core/tests/sdk_compile.rs"
      to: "go build ./..."
      via: "std::process::Command discrete args in the temp dir"
      pattern: "go.*build"
    - from: ".github/workflows/ci.yml"
      to: "crates/gnr8-core/tests/sdk_compile.rs"
      via: "blocking gates job runs --test sdk_compile"
      pattern: "sdk_compile"
---

<objective>
Prove the generated Go SDK genuinely compiles and answers a real HTTP round-trip (SDK-05 — the phase's
hardest acceptance bar), add end-to-end determinism assertions for both artifacts, and promote all four
contract tests + the new compile test into the blocking CI gate while retiring the non-blocking `contract`
job (D-07). After this plan, the core source→OpenAPI→Go-SDK loop is proven end-to-end and all four contract
tests are green and blocking.

Purpose: A string snapshot can look correct yet not compile (RESEARCH Pitfall 3). Only the real Go compiler
plus an httptest round-trip prove SDK-05. The compile test is fully hermetic (stdlib-only go.mod, zero
requires) so it never touches the network (RESEARCH Pitfall 5).

Output: `tests/sdk_compile.rs` (temp dir + go.mod + go build + httptest smoke), extended
`tests/determinism.rs`, and updated `Makefile` + `ci.yml` promoting the contract suite to blocking.
</objective>

<execution_context>
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-openapi-and-go-sdk-generation/03-CONTEXT.md
@.planning/phases/03-openapi-and-go-sdk-generation/03-RESEARCH.md

# The seams this test exercises (from 03-01 + 03-02 — both wave 1):
@crates/gnr8-core/src/sdk/mod.rs
@crates/gnr8-core/src/lower/mod.rs
# The verified discrete-arg subprocess pattern to reuse for `go build`/`go test`:
@crates/gnr8-core/src/analyze/helper.rs
# The existing determinism test to extend + the green-test skip-gracefully pattern:
@crates/gnr8-core/tests/determinism.rs
# The SDK shape being compiled (stdlib-only imports → zero-require go.mod):
@fixtures/goalservice/expected/sdk/client.go
@fixtures/goalservice/expected/sdk/goals.go
@fixtures/goalservice/expected/sdk/errors.go
# The CI + Makefile gate structure to promote (contract → blocking):
@.github/workflows/ci.yml
@Makefile
@.planning/phases/02-go-analysis-and-api-graph/02-03-SUMMARY.md
@thoughts/skills/rust-best-practices/SKILL.md

<interfaces>
<!-- Key contracts the executor consumes. Extracted from the codebase — use directly, no exploration. -->

The seams this test calls (implemented by 03-01 + 03-02):
  pub fn lower::to_openapi(graph: &ApiGraph) -> Result<String, CoreError>
  pub fn sdk::generate(graph: &ApiGraph) -> Result<String, CoreError>
  pub fn sdk::write_to_dir(bundle: &SdkBundle, dir: &std::path::Path) -> Result<(), CoreError>
  pub fn analyze::build_graph(fixture_dir: &str) -> Result<ApiGraph, CoreError>

The CoreError variant this plan uses (already added by 03-01 — do NOT edit error.rs):
  CoreError::GoBuild { code: Option<i32>, stderr: String }

The discrete-arg subprocess pattern to mirror for `go build`/`go test` (helper.rs / RESEARCH Code Examples):
  Command::new("go").args(["build", "./..."]).current_dir(&sdk_dir).output()
    .map_err(|source| CoreError::GoToolchainMissing { source })?;
  if !output.status.success() { return Err(CoreError::GoBuild { code, stderr }); }

The graceful-skip pattern (tests/determinism.rs) — early-return when the Go toolchain is unavailable so the
test never fails for a missing dependency:
  let Ok(first) = gnr8_core::analyze::build_graph(FIXTURE_DIR) else { eprintln!("skipping..."); return; };

FIXTURE_DIR constant used by the existing tests:
  const FIXTURE_DIR: &str = concat!(env!("CARGO_MANIFEST_DIR"), "/../../fixtures/goalservice");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/sdk_compile.rs — materialize SDK to a hermetic temp dir, go build, and an httptest smoke go test (SDK-05)</name>
  <files>crates/gnr8-core/tests/sdk_compile.rs, crates/gnr8-core/src/sdk/mod.rs</files>
  <behavior>
    - Test: build_graph(FIXTURE) → sdk::generate → write_to_dir into a unique temp subdir, write a generated go.mod (module gnr8sdktest + go 1.26, ZERO requires), run `go build ./...` → exit 0 (SDK-05 compiles). Skips gracefully (early return) if the Go toolchain is absent.
    - Test: into the same temp dir write a fixed smoke_test.go that spins up an httptest server, constructs the Client via NewClient(srv.URL), calls CreateGoal (POST /goal/), asserts the server saw method POST + path /goal/ + the JSON body, returns 201 with a CommandMessageWithUUID, and the decoded out.UUID matches; then `go test ./...` → pass (SDK-05 exercised).
    - Test: the smoke test also exercises a 4xx path — the stub returns 404 with an HttpError body for a DeleteGoal/UpdateGoal call and asserts the SDK returns a *APIError with StatusCode==404 (SDK-04 typed error path).
    - Test: a forced `go build` of invalid Go surfaces CoreError::GoBuild, never a panic (no unwrap/expect on the subprocess result in the harness helper).
  </behavior>
  <action>
    Create `crates/gnr8-core/tests/sdk_compile.rs` (a NEW integration test, scoped
    `#![allow(clippy::unwrap_used, clippy::expect_used)]` like the other test targets — tests may use
    unwrap). Use the same `FIXTURE_DIR` constant as the existing tests. The test: (1) builds the graph via
    `build_graph(FIXTURE_DIR)`, skipping gracefully (early `return`) if the Go toolchain is unavailable
    (mirror determinism.rs so a missing dep never fails the suite); (2) calls `sdk::generate` and
    `sdk::write_to_dir` into a UNIQUE temp subdir under `std::env::temp_dir()` — choose the zero-dependency
    `std::env::temp_dir()` plus a uniquely-named subdir (PID plus a nanosecond timestamp) rather than adding
    the `tempfile` crate (RESEARCH tagged `tempfile` `[ASSUMED]`; the `std` path adds NO dependency and keeps
    this plan autonomous — threat T-03-03-SC); clean the dir up at the end (best-effort `remove_dir_all`); (3)
    writes a generated `go.mod` with EXACTLY `module gnr8sdktest` plus `go 1.26` and ZERO `require` lines so
    the build is hermetic and never reaches the module proxy (RESEARCH Pitfall 5 — GOPROXY=off-safe); (4)
    runs `go build ./...` in the dir via a discrete-arg `Command` (mirror helper.rs — no shell, threat
    T-03-03-01); a non-zero exit maps to `CoreError::GoBuild { code, stderr }` (asserted via a small harness
    helper that returns Result), never a panic.

    Write a FIXED `smoke_test.go` into the temp dir from the Rust harness (NOT part of the snapshot-ed SDK
    bundle — keep the bundle production-SDK-only, RESEARCH Open Q2 recommendation b): it uses
    `net/http/httptest` (stdlib, hermetic) to (a) serve POST `/goal/`, assert the SDK sent method POST plus
    path `/goal/` plus the marshaled CreateGoalInput body, respond 201 with a `CommandMessageWithUUID`,
    construct the Client with `NewClient(srv.URL)`, call `CreateGoal(context.Background(), ...)`, and assert
    the decoded response; and (b) serve a 404 with an `HttpError` body for a delete/update call and assert the
    SDK returns a `*APIError` with `StatusCode == 404` (SDK-04). Run `go test ./...` in the dir (discrete
    args); a non-zero exit fails the Rust test with the captured stderr (so a smoke regression is visible).
    The Go package name in the generated SDK is `goalservice`; write the smoke test to share that package
    (read the package clause from the written bundle files — it is the source of truth — and match it). This
    task requires the Go toolchain — present on dev plus CI.
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test sdk_compile 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <done>tests/sdk_compile.rs materializes the SDK to a hermetic stdlib-only temp dir, `go build ./...` exits 0 (SDK-05 compiles), and the httptest `go test ./...` smoke passes (constructs Client, asserts method/path/body + response decode + 4xx→APIError); a go build non-zero exit maps to CoreError::GoBuild with no panic; the test skips gracefully without the Go toolchain; no network access (zero-require go.mod).</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/determinism.rs with to_openapi + sdk::generate two-run byte-identical assertions</name>
  <files>crates/gnr8-core/tests/determinism.rs</files>
  <action>
    Extend `crates/gnr8-core/tests/determinism.rs` (already exists; keep the existing graph two-run test).
    Add two new `#[test]` functions following the SAME graceful-skip pattern (early-return if `build_graph`
    fails for a missing toolchain): one asserts `lower::to_openapi(&graph)` over two builds of the fixture is
    byte-identical (idempotent OpenAPI generation — RESEARCH Pitfall 4 / TARGET-API §5.6); one asserts
    `sdk::generate(&graph)` over two builds is byte-identical (idempotent SDK generation). Build the graph
    twice (or build once and lower/generate twice — either proves idempotence; building twice also proves the
    upstream graph determinism still holds end-to-end). Keep the scoped `#![allow(clippy::unwrap_used,
    clippy::expect_used)]` already on the file. No production code changes.
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test determinism 2>&1 | grep -q "test result: ok" && cargo clippy --all-targets --all-features --locked -- -D warnings</automated>
  </verify>
  <done>determinism.rs has three passing tests: graph, OpenAPI, and SDK are each byte-identical across two runs; clippy -D warnings clean.</done>
</task>

<task type="auto">
  <name>Task 3: Promote all four contract tests + sdk_compile to the blocking CI gate; retire the non-blocking contract job (D-07)</name>
  <files>.github/workflows/ci.yml, Makefile</files>
  <action>
    Update `.github/workflows/ci.yml`: in the BLOCKING `gates` job's test step, add `snapshot_openapi`,
    `snapshot_sdk`, and the new `sdk_compile` to the `cargo test -p gnr8-core --test ...` invocation alongside
    the existing `snapshot_graph --test snapshot_diagnostics --test determinism` (all four contract tests are
    now green per 03-01/03-02 plus this plan, so they belong in the blocking gate — D-07). REMOVE the entire
    non-blocking `contract` job (the `continue-on-error: true` job that ran `snapshot_openapi --test
    snapshot_sdk`) — it is now retired (Open Q1 option d's final state). The `gates` job already sets up the
    Go toolchain (needed for build_graph/gofmt/go build); keep that. Update the file's header comment to
    reflect that all four contract tests are green plus blocking and the contract job is gone.

    Update `Makefile`: fold `snapshot_openapi`, `snapshot_sdk`, and `sdk_compile` into the `gates` target's
    `cargo test -p gnr8-core --test ...` line; retire the `contract` target (remove it from `.PHONY` and
    delete the recipe, or repoint it to a note that all contract tests are now in `gates`). Update the
    Makefile header comment: there are no longer "four red-by-design contract tests" — they are all green and
    in the blocking gate. `make check` (fmt-check + clippy + test + fixture-build + goextract-build) must
    pass end-to-end with the full suite now green (no red-by-design failures remain).
  </action>
  <verify>
    <automated>grep -q sdk_compile /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/.github/workflows/ci.yml && grep -q snapshot_openapi /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/.github/workflows/ci.yml && grep -q sdk_compile /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/Makefile && make -C /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7 gates</automated>
    <automated>test -z "$(grep -nE '^[[:space:]]*continue-on-error:[[:space:]]*true' /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/.github/workflows/ci.yml)"</automated>
  </verify>
  <done>ci.yml gates job runs snapshot_openapi/snapshot_sdk/sdk_compile and the non-blocking contract job (continue-on-error) is removed; Makefile gates includes the four contract tests + sdk_compile and the contract target is retired; `make gates` passes (full suite green, no red-by-design failures).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gnr8-core test harness → go build / go test subprocess | The Rust test spawns `go build`/`go test` in a program-controlled temp dir. The generated Go is program-produced; the go.mod is fixed; invocation must use discrete args (no shell). |
| temp dir filesystem | The compile test writes program-generated filenames into a unique subdir under the OS temp dir; cleaned up after the run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering / EoP | `go build` / `go test` subprocess invocation | mitigate | Discrete `Command::args([...])` with `current_dir(temp)`, never `sh -c` / string interpolation; reuse the verified helper.rs pattern (ASVS V12). Temp paths are program-controlled, not user input. |
| T-03-03-02 | Tampering (supply-chain) | hermetic `go build` reaching the network | mitigate | The generated go.mod has ZERO `require`s (the SDK is stdlib-only — verified in expected/sdk/*.go), so go build/test never fetches from the module proxy — GOPROXY=off-safe (RESEARCH Pitfall 5). No go.sum needed. |
| T-03-03-03 | Tampering | temp-dir path traversal / collision | mitigate | Write only program-generated filenames into a uniquely-named subdir (PID + nanosecond timestamp) under `std::env::temp_dir()`; best-effort cleanup. No user-supplied path component. |
| T-03-03-04 | Denial of Service | go build non-zero exit handling | mitigate | A `go build`/`go test` non-zero exit maps to `CoreError::GoBuild` (or a captured-stderr test failure), never an `unwrap`/`panic` in the harness helper (RUST-04). |
| T-03-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO new crates. RESEARCH's only candidate (`tempfile`, `[ASSUMED]`) is DECLINED in favor of the zero-dependency `std::env::temp_dir()` path, so no package legitimacy checkpoint is required and the plan stays autonomous. |
</threat_model>

<verification>
- `cargo test -p gnr8-core --test sdk_compile` — GREEN (go build + httptest smoke + 4xx→APIError).
- `cargo test -p gnr8-core --test determinism` — three tests GREEN (graph + OpenAPI + SDK byte-identical).
- `cargo test -p gnr8-core` — full crate suite GREEN (all four contract tests now pass).
- `make gates` — passes with snapshot_openapi/snapshot_sdk/sdk_compile in the blocking set.
- `make check` — passes end-to-end (no red-by-design failures remain).
- `ci.yml` has no `continue-on-error: true` job; `gates` runs all four contract tests + sdk_compile.
- `cargo fmt --all -- --check` + `cargo clippy --all-targets --all-features --locked -- -D warnings` — clean.
- Hermetic: the compile test runs with zero network access (zero-require go.mod).
</verification>

<success_criteria>
- The generated Go SDK compiles (`go build ./...` exit 0) and is exercised by an httptest smoke test (SDK-05).
- The smoke test constructs the Client, calls a fixture operation, and asserts method/path/body + response decode + a 4xx→`APIError` path (SDK-05 + SDK-04).
- The compile test is hermetic (stdlib-only go.mod, zero requires, GOPROXY=off-safe) and skips gracefully without the Go toolchain.
- A `go build` non-zero exit maps to `CoreError::GoBuild`; no panic in the harness.
- `to_openapi` and `sdk::generate` are each byte-identical across two runs (idempotent generation).
- All four contract tests + sdk_compile are in the blocking CI gate; the non-blocking `contract` job is retired (D-07).
- No new crates added (the `tempfile` `[ASSUMED]` candidate is declined for the zero-dependency `std` path).
</success_criteria>

<output>
Create `.planning/phases/03-openapi-and-go-sdk-generation/03-03-SUMMARY.md` when done.
</output>
