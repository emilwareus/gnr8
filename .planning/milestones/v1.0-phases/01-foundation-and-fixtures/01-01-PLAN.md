---
phase: 01-foundation-and-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - rust-toolchain.toml
  - .gitignore
  - crates/gnr8-core/Cargo.toml
  - crates/gnr8-core/src/lib.rs
  - crates/gnr8-core/src/error.rs
  - crates/gnr8-core/src/analyze/mod.rs
  - crates/gnr8-core/src/graph/mod.rs
  - crates/gnr8-core/src/lower/mod.rs
  - crates/gnr8-core/src/sdk/mod.rs
  - crates/gnr8-core/src/diagnostics/mod.rs
  - crates/gnr8/Cargo.toml
  - crates/gnr8/src/main.rs
  - crates/gnr8/src/cli.rs
  - docs/poc-contract.md
autonomous: true
requirements: [POC-01, POC-02, POC-03, RUST-01, RUST-02, RUST-04]

must_haves:
  truths:
    - "A developer runs `cargo run -p gnr8 -- --help` and sees init/generate/watch/check/inspect/doctor"
    - "A developer runs `cargo run -p gnr8 -- inspect --help` and sees routes/schemas/graph subcommands"
    - "Running an unimplemented command prints a clear 'not yet implemented (phase N)' message and exits non-zero WITHOUT panicking"
    - "The PoC contract (Gin-only router, OpenAPI 3.1.0, Go SDK shape, .gnr8/ split, non-goals) is documented before analyzer work begins"
    - "Library code (gnr8-core) contains no anyhow and no production unwrap/expect/panic"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace manifest with [workspace.lints], [workspace.dependencies], members"
      contains: "[workspace.lints.clippy]"
    - path: "crates/gnr8-core/src/error.rs"
      provides: "CoreError thiserror enum incl. NotYetImplemented variant"
      contains: "NotYetImplemented"
    - path: "crates/gnr8-core/src/lib.rs"
      provides: "Module seams (analyze/graph/lower/sdk/diagnostics) + not_yet helper"
      contains: "pub fn not_yet"
    - path: "crates/gnr8/src/cli.rs"
      provides: "clap derive Cli/Commands/InspectAction with global --json/-v"
      contains: "InspectAction"
    - path: "crates/gnr8/src/main.rs"
      provides: "anyhow boundary + dispatch + non-panicking NotYetImplemented exit"
      contains: "NotYetImplemented"
    - path: "docs/poc-contract.md"
      provides: "POC-01/02/03 scope lock, locked surface, non-goals table"
      contains: "OpenAPI 3.1.0"
  key_links:
    - from: "crates/gnr8/src/main.rs"
      to: "gnr8_core::not_yet"
      via: "dispatch arms call gnr8_core stubs"
      pattern: "gnr8_core::not_yet"
    - from: "crates/gnr8-core/src/lib.rs"
      to: "crates/gnr8-core/src/error.rs"
      via: "pub use error::CoreError"
      pattern: "pub use error::CoreError"
---

<objective>
Scaffold the two-crate Cargo workspace, the clap-derive CLI command surface, the typed error
boundary, the (stubbed) gnr8-core module seams, and the committed PoC contract document.

Purpose: Lock the PoC contract (POC-01/02/03) and stand up the Rust foundation (RUST-01/02/04) that
every later phase builds on. This plan delivers a thin, compiling, clippy-clean binary whose commands
parse and exit cleanly with a typed "not yet implemented" message — and the module seams that Phase 2/3
will fill and that Plan 01-03's red-by-design tests will call.

Output: `Cargo.toml` workspace + `crates/gnr8-core` lib (error enum, not_yet helper, five module
seams) + `crates/gnr8` bin (clap CLI, anyhow boundary, dispatch) + `docs/poc-contract.md` +
`.gitignore`/`rust-toolchain.toml` housekeeping. NO analysis/lowering/SDK logic — seams only.
</objective>

<execution_context>
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-fixtures/01-CONTEXT.md
@.planning/phases/01-foundation-and-fixtures/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@thoughts/skills/rust-best-practices/references/chapter_02.md
@thoughts/skills/rust-best-practices/references/chapter_04.md

<interfaces>
<!-- These are the contracts this plan CREATES. Plan 01-03's red-by-design tests call the
     gnr8-core module-seam functions below; keep these exact names + signatures so 01-03's
     test files compile against them. Implementations return Err(CoreError::NotYetImplemented). -->

CoreError (gnr8-core, thiserror enum) — variant required this phase:
  NotYetImplemented { command: String, phase: u8 }
  Display: "'{command}' is not yet implemented (arrives in phase {phase})"

Public helper (gnr8-core):
  pub fn not_yet<T>(command: &str, phase: u8) -> Result<T, CoreError>

Module seams (gnr8-core) — each is a pub fn that today returns Err(CoreError::NotYetImplemented):
  analyze::build_graph(fixture_dir: &str) -> Result<graph::ApiGraph, CoreError>   // phase 2
  lower::to_openapi(graph: &graph::ApiGraph) -> Result<String, CoreError>          // phase 3
  sdk::generate(graph: &graph::ApiGraph) -> Result<String, CoreError>             // phase 3
  diagnostics::collect(fixture_dir: &str) -> Result<String, CoreError>            // phase 2+

graph::ApiGraph — a minimal, router-agnostic placeholder struct this phase (D-03):
  derive(Debug, serde::Serialize); fields may be empty/unit for now. MUST NOT contain
  Gin-specific fields (no gin_context etc.). Real shape lands in Phase 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Cargo workspace skeleton, lints, deps, and housekeeping files</name>
  <files>Cargo.toml, crates/gnr8-core/Cargo.toml, crates/gnr8/Cargo.toml, rust-toolchain.toml, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md "Pattern 1: Workspace Cargo.toml" (lines ~249-332) — exact workspace/member manifests, lint policy, [workspace.dependencies] versions
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md ".gitignore additions (Rust)" (lines ~606-614)
    - thoughts/skills/rust-best-practices/references/chapter_02.md — workspace lint setup, clippy invocation
    - existing .gitignore (already keeps Cargo.lock; ignores target/debug) — APPEND, do not overwrite
  </read_first>
  <action>
    Create the root workspace `Cargo.toml` with `[workspace]` members `crates/gnr8-core` and
    `crates/gnr8`, `resolver = "2"`, and `[workspace.package]` (`edition = "2021"` per D-08,
    `rust-version = "1.85"`, `license = "MIT"` to match the repo LICENSE). Add `[workspace.lints.rust]`
    with `unsafe_code = "forbid"` and `unreachable_pub = "warn"`; add `[workspace.lints.clippy]` with
    `all = { level = "deny", priority = -1 }`, `pedantic = { level = "warn", priority = -1 }`, and the
    RUST-04 enforcement lints `unwrap_used = "deny"`, `expect_used = "deny"`, `panic = "deny"`,
    `todo = "warn"`. Add `[workspace.dependencies]` pinning the EXACT verified versions from RESEARCH.md:
    `clap = { version = "4.6", features = ["derive"] }`, `thiserror = "2.0"` (NOTE: v2, not v1),
    `anyhow = "1.0"`, `serde = { version = "1.0", features = ["derive"] }`, `serde_json = "1.0"`,
    `insta = { version = "1.48", features = ["yaml"] }`, and `gnr8-core = { path = "crates/gnr8-core" }`.

    Create `crates/gnr8-core/Cargo.toml`: package `gnr8-core` v0.1.0 inheriting
    `edition.workspace`/`rust-version.workspace`/`license.workspace`, `[lints] workspace = true`,
    dependencies `thiserror = { workspace = true }` and `serde = { workspace = true }`,
    dev-dependency `insta = { workspace = true }`. Do NOT add anyhow here (D-09).

    Create `crates/gnr8/Cargo.toml`: package `gnr8` v0.1.0 with same workspace inheritance,
    `[[bin]] name = "gnr8" path = "src/main.rs"`, `[lints] workspace = true`, dependencies
    `gnr8-core = { workspace = true }`, `clap = { workspace = true }`, `serde_json = { workspace = true }`,
    `anyhow = { workspace = true }` (anyhow ONLY here, per D-09).

    Create `rust-toolchain.toml` pinning `channel = "stable"` for CI reproducibility.

    APPEND to existing `.gitignore` (do not remove existing Cargo/RustRover entries): a `/target` line
    (if not already covered by the existing `target` entry, leave existing as-is and add insta entries),
    `*.snap.new`, and `*.pending-snap`. Add a comment line stating Cargo.lock MUST stay committed
    (binary project needs it for `clippy --locked`). Do NOT add any `Cargo.lock` ignore line.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 >/dev/null 2>&1 && grep -q 'unwrap_used' Cargo.toml && grep -q 'thiserror = "2.0"' Cargo.toml && ! grep -E '^\s*Cargo\.lock' .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo metadata --no-deps` exits 0 (workspace is parseable; both members resolve)
    - `grep -c 'unwrap_used\|expect_used\|panic' Cargo.toml` >= 1 (RUST-04 lints present)
    - `grep 'thiserror = "2.0"' Cargo.toml` matches (v2 pinned, not v1)
    - `grep -E '^\s*Cargo\.lock' .gitignore` returns NOTHING (lockfile stays committed — Open Q2)
    - `crates/gnr8-core/Cargo.toml` has NO `anyhow` line (`! grep -q anyhow crates/gnr8-core/Cargo.toml`)
  </acceptance_criteria>
  <done>Workspace manifests + housekeeping files exist; `cargo metadata` resolves both crates; RUST-04 lints and v2 thiserror pinned; Cargo.lock not ignored; anyhow absent from core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement gnr8-core — CoreError, not_yet helper, and the five module seams</name>
  <files>crates/gnr8-core/src/lib.rs, crates/gnr8-core/src/error.rs, crates/gnr8-core/src/analyze/mod.rs, crates/gnr8-core/src/graph/mod.rs, crates/gnr8-core/src/lower/mod.rs, crates/gnr8-core/src/sdk/mod.rs, crates/gnr8-core/src/diagnostics/mod.rs</files>
  <read_first>
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md "Pattern 3: thiserror core error + not_yet helper" (lines ~435-460) — exact CoreError + not_yet shapes
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md "Architectural Responsibility Map" + system diagram (lines ~64-74, ~163-200) — module seam list and which phase fills each
    - .planning/phases/01-foundation-and-fixtures/01-CONTEXT.md D-03 (router-agnostic graph), D-09 (thiserror in lib, no anyhow), D-12 (skeletal messages)
    - thoughts/skills/rust-best-practices/references/chapter_04.md — thiserror crate-error pattern, Result vs panic
    - The `<interfaces>` block in this plan's <context> — the exact fn names/signatures Plan 01-03 will call
  </read_first>
  <behavior>
    - Test `core_error_not_yet_implemented_display`: formatting `CoreError::NotYetImplemented { command: "generate".into(), phase: 3 }` yields the string `'generate' is not yet implemented (arrives in phase 3)`.
    - Test `not_yet_returns_typed_error`: `gnr8_core::not_yet::<()>("init", 4)` returns `Err(CoreError::NotYetImplemented { command, phase })` where command == "init" and phase == 4 (no panic).
    - Test `build_graph_is_not_yet_implemented`: `analyze::build_graph("anywhere")` returns `Err(CoreError::NotYetImplemented { .. })` (seam stubbed, phase 2).
  </behavior>
  <action>
    Write `error.rs` with a `#[derive(Debug, thiserror::Error)]` `pub enum CoreError` carrying
    `#[error("'{command}' is not yet implemented (arrives in phase {phase})")] NotYetImplemented {
    command: String, phase: u8 }`. Leave a documented comment placeholder for future real variants
    (e.g. PackageLoad) but add no other variants this phase.

    Write `lib.rs` declaring `pub mod error;` + `pub use error::CoreError;`, declaring the five module
    seams `pub mod analyze; pub mod graph; pub mod lower; pub mod sdk; pub mod diagnostics;`, and the
    generic `pub fn not_yet<T>(command: &str, phase: u8) -> Result<T, CoreError>` returning
    `Err(CoreError::NotYetImplemented { command: command.to_string(), phase })`. Add a unit test module
    (gated `#[cfg(test)]`, with `#![allow(clippy::unwrap_used, clippy::expect_used)]` scoped to that
    module) implementing the three behaviors above.

    In `graph/mod.rs` define a minimal router-agnostic placeholder `#[derive(Debug, Default,
    serde::Serialize)] pub struct ApiGraph { /* HTTP route facts land in Phase 2; empty for now */ }`.
    Per D-03 it MUST NOT contain any Gin-specific field. Add a short module doc comment noting the
    router-agnostic intent.

    In `analyze/mod.rs` write `pub fn build_graph(_fixture_dir: &str) -> Result<crate::graph::ApiGraph,
    crate::CoreError>` returning `crate::not_yet("analyze::build_graph", 2)`. In `lower/mod.rs` write
    `pub fn to_openapi(_graph: &crate::graph::ApiGraph) -> Result<String, crate::CoreError>` returning
    `crate::not_yet("lower::to_openapi", 3)`. In `sdk/mod.rs` write `pub fn generate(_graph:
    &crate::graph::ApiGraph) -> Result<String, crate::CoreError>` returning
    `crate::not_yet("sdk::generate", 3)`. In `diagnostics/mod.rs` write `pub fn collect(_fixture_dir:
    &str) -> Result<String, crate::CoreError>` returning `crate::not_yet("diagnostics::collect", 2)`.

    No `unwrap`/`expect`/`panic`/`todo!()`/`unimplemented!()` anywhere in non-test gnr8-core code — the
    stubs return the typed error instead (RUST-04). Test-module unwraps are allowed via the scoped allow.
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --lib 2>&1 | grep -qE 'test result: ok' && cargo clippy -p gnr8-core --all-targets --all-features -- -D warnings 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p gnr8-core --lib` reports `test result: ok` (the three unit tests pass)
    - `cargo clippy -p gnr8-core --all-targets --all-features -- -D warnings` exits 0
    - `grep -rn 'unwrap()\|\.expect(\|panic!\|unimplemented!\|todo!' crates/gnr8-core/src --include=*.rs | grep -v '#\[cfg(test)\]' | grep -v 'mod tests'` finds no production occurrences (RUST-04). Manually confirm any hits are inside `#[cfg(test)]`.
    - `! grep -rq 'gin' crates/gnr8-core/src/graph/mod.rs` (D-03: no Gin specifics in graph types)
    - `grep -q 'pub fn build_graph' crates/gnr8-core/src/analyze/mod.rs` and the same for to_openapi/generate/collect (seams 01-03 will call exist)
  </acceptance_criteria>
  <done>gnr8-core compiles clippy-clean; CoreError + not_yet + three unit tests green; five module seams exported returning typed NotYetImplemented; graph type is router-agnostic; no prod panic-paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the gnr8 binary — clap CLI surface, dispatch, anyhow boundary; write the PoC contract doc</name>
  <files>crates/gnr8/src/cli.rs, crates/gnr8/src/main.rs, docs/poc-contract.md</files>
  <read_first>
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md "Pattern 2: clap derive CLI" (lines ~334-433) — Cli/Commands/InspectAction structs, dispatch, non-panicking NotYetImplemented exit, exit code 2
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md "PoC Contract Documentation" (lines ~652-665) — the exact required contents of docs/poc-contract.md
    - .planning/phases/01-foundation-and-fixtures/01-CONTEXT.md D-02/D-04/D-05/D-06/D-11/D-12 — router=Gin, OpenAPI 3.1.0, SDK shape, .gnr8/ split, command surface, --json/-v + clean exit
    - .planning/REQUIREMENTS.md "Out of Scope" table (lines ~117-130) — copy verbatim into the contract non-goals section (POC-03)
    - .planning/research/TARGET-API.md §7 (lines ~308-323) — confirm the Gin recognized-patterns wording for the contract doc
  </read_first>
  <behavior>
    - Test `cli_parses_all_top_level_commands`: `Cli::try_parse_from(["gnr8","generate"])`, and likewise for init/watch/check/doctor, all return Ok with the matching `Commands` variant.
    - Test `cli_parses_inspect_subcommands`: `Cli::try_parse_from(["gnr8","inspect","routes"])` (and schemas/graph) return Ok with the matching `InspectAction`.
    - Test `cli_global_json_flag`: `Cli::try_parse_from(["gnr8","--json","doctor"])` returns Ok with `json == true`; `["gnr8","-v","doctor"]` yields `verbose >= 1`.
    - Test `cli_rejects_unknown_command`: `Cli::try_parse_from(["gnr8","bogus"])` returns Err (clap rejects it; no panic).
  </behavior>
  <action>
    Write `cli.rs` per RESEARCH Pattern 2: `#[derive(Debug, Parser)] pub struct Cli` with
    `#[command(name = "gnr8", version, about = "...")]`, a global `--json` bool (`#[arg(long, global =
    true)]`), a global `-v/--verbose` count (`#[arg(short, long, global = true, action =
    clap::ArgAction::Count)] pub verbose: u8`), and `#[command(subcommand)] pub command: Commands`.
    Define `#[derive(Debug, Subcommand)] pub enum Commands { Init, Generate, Watch, Check, Inspect {
    #[command(subcommand)] action: InspectAction }, Doctor }` and `#[derive(Debug, Subcommand)] pub enum
    InspectAction { Routes, Schemas, Graph }` (D-11). Give each command/subcommand a one-line doc comment
    (clap uses it as help text — drives the RUST-02 help surface). For `inspect` with no subcommand,
    keep clap's default (error + usage); this is the documented chosen behavior (Open Q4).

    Add a `#[cfg(test)]` module (with `#![allow(clippy::unwrap_used, clippy::expect_used)]` scoped) that
    uses `Cli::try_parse_from` to implement the four behaviors above. These are parse tests — they must
    not invoke dispatch (which would error with NotYetImplemented).

    Write `main.rs` as the SOLE anyhow boundary (D-09): `fn main() -> anyhow::Result<()>` parses
    `Cli::parse()`, calls a `dispatch(&cli) -> Result<Report, gnr8_core::CoreError>` where every arm maps
    to a `gnr8_core::not_yet(<command-name>, <phase>)` call (init→4, generate→3, watch→4, check→4,
    doctor→5, inspect routes/schemas/graph→2). On `Ok(report)` render JSON via
    `serde_json::to_string_pretty` when `cli.json` else a plain human line (plain `println!` is fine this
    phase). On `Err(CoreError::NotYetImplemented { .. })` print `eprintln!("gnr8: {e}")` and
    `std::process::exit(2)` (clean, non-panicking; D-12). On any other `Err`, convert via `?`/`.into()`
    so anyhow surfaces it. Define a minimal `Report` type (e.g. a unit/placeholder struct deriving
    `serde::Serialize`) sufficient to compile the render path; real reports arrive in later phases. Do NOT
    use unwrap/expect/panic in non-test binary code; route the skeletal path through the typed error, not
    `unimplemented!()` (RESEARCH anti-pattern).

    Write `docs/poc-contract.md` (committed; POC-01/02/03) containing exactly: (1) Scope lock — Go source
    -> OpenAPI -> Go SDK, nothing else this milestone (D-01). (2) Locked surface — Router family: Gin only,
    recognized patterns `Router.Group` + `group.METHOD(path, handler)`, `ShouldBindJSON`, `c.Param`/
    `c.Query` (D-02); OpenAPI target 3.1.0 (D-04); Go SDK shape = Client + functional options +
    tag-grouped typed ops + request/response model structs + typed API error + `context.Context`-first
    (D-05); `.gnr8/` layout = checked-in code-as-config dir + git-ignored cache/output lifecycle dir,
    detailed surface deferred to Phase 4 (D-06). (3) Non-goals — copy the REQUIREMENTS.md "Out of Scope"
    table verbatim, and state that any item entering the PoC requires an explicit roadmap update (POC-03).
    (4) A short note that although Gin is the only recognized router, the internal graph stores
    router-agnostic HTTP route facts so chi/echo/net-http can be added later without reshaping (D-03 seam).
  </action>
  <verify>
    <automated>cargo test -p gnr8 2>&1 | grep -qE 'test result: ok' && cargo run -q -p gnr8 -- --help 2>&1 | grep -q 'inspect' && cargo run -q -p gnr8 -- inspect --help 2>&1 | grep -q 'routes' && grep -q 'OpenAPI 3.1.0\|3.1.0' docs/poc-contract.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p gnr8` reports `test result: ok` (the four parse tests pass)
    - `cargo run -p gnr8 -- --help` output lists init, generate, watch, check, inspect, doctor (RUST-02)
    - `cargo run -p gnr8 -- inspect --help` output lists routes, schemas, graph (RUST-02)
    - `cargo run -p gnr8 -- generate; echo $?` prints a "not yet implemented (arrives in phase 3)" line to stderr and exits with code 2 (NOT a panic backtrace) (D-12)
    - `cargo clippy -p gnr8 --all-targets --all-features --locked -- -D warnings` exits 0
    - `docs/poc-contract.md` contains "3.1.0", "Gin", and a non-goals/Out of Scope section (POC-02/03)
  </acceptance_criteria>
  <done>Binary parses the full command surface; --help/--version work; skeletal commands exit cleanly (code 2, no panic); anyhow lives only in main.rs; PoC contract doc committed covering scope/surface/non-goals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → gnr8 CLI args | clap parses untrusted argv; no file/network access exercised this phase |
| developer → dependency registry | crates.io packages pulled at build time (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | crates.io dependency supply chain (clap/thiserror/serde/insta/anyhow) | mitigate | Versions verified on authoritative crates.io sparse index + named in project STACK.md/skill (RESEARCH Package Legitimacy Audit); commit `Cargo.lock`; pin via `[workspace.dependencies]` single-source. |
| T-01-02 | Tampering/EoP | unsafe memory in gnr8-core/gnr8 | mitigate | `unsafe_code = "forbid"` in `[workspace.lints.rust]` (Task 1). |
| T-01-03 | DoS | panic-based crash / unclear failure on unimplemented command | mitigate | RUST-04: clippy denies `unwrap_used`/`expect_used`/`panic`; skeletal path returns typed `CoreError::NotYetImplemented` and exits code 2, never `unimplemented!()` (Tasks 2,3). |
| T-01-04 | Spoofing/Tampering | user-supplied source/file paths via CLI | accept (deferred) | Not exposed this phase — commands take no path args yet; flagged for Phase 2 when `generate`/`inspect` accept paths (RESEARCH Security Domain V12). |
| T-01-SC | Tampering | cargo install of pinned crates (first build) | mitigate | All crates ecosystem-standard, registry-verified, named in STACK.md/skill; none `[ASSUMED]`/`[SUS]`/`[SLOP]` (RESEARCH audit). No package-legitimacy human checkpoint required. |
</threat_model>

<verification>
- `cargo build --workspace` compiles both crates.
- `cargo clippy --all-targets --all-features --locked -- -D warnings` exits 0 (RUST-03 partial; full gate wired in 01-03).
- `cargo fmt --all -- --check` reports no diffs.
- `cargo test --workspace` green (all Plan-01-01 tests are real, passing unit/parse tests — the red-by-design snapshot tests are added in Plan 01-03, not here).
- `cargo run -p gnr8 -- generate` exits 2 with a clear message and no panic.
</verification>

<success_criteria>
- A developer can run `cargo run -p gnr8 -- --help` / `inspect --help` and see the full planned command surface (RUST-02, ROADMAP success criterion 1).
- gnr8-core has no anyhow and no production unwrap/expect/panic; clippy enforces it (RUST-04).
- `docs/poc-contract.md` documents the locked router/OpenAPI-version/SDK-shape/.gnr8 split + non-goals before analyzer work begins (POC-01/02/03).
- Workspace is parseable, clippy-clean, fmt-clean; Cargo.lock committed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-fixtures/01-01-SUMMARY.md` when done.
</output>
