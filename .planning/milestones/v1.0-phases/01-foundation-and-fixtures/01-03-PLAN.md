---
phase: 01-foundation-and-fixtures
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - crates/gnr8-core/tests/snapshot_graph.rs
  - crates/gnr8-core/tests/snapshot_openapi.rs
  - crates/gnr8-core/tests/snapshot_sdk.rs
  - crates/gnr8-core/tests/snapshot_diagnostics.rs
  - Makefile
  - .github/workflows/ci.yml
  - docs/poc-contract.md
autonomous: false
requirements: [RUST-03, FIX-03, FIX-04]

must_haves:
  truths:
    - "Four contract snapshot tests exist (graph, openapi, sdk, diagnostics) and FAIL clearly today (red-by-design) because gnr8-core seams return CoreError::NotYetImplemented"
    - "None of the four contract tests uses #[ignore] (silent skip is forbidden — FIX-04)"
    - "`make check` runs fmt-check + clippy(-D warnings, --locked) + test + fixture go build/vet"
    - "GitHub Actions CI has a BLOCKING job (fmt/clippy/test-minus-contract + go-fixture build/vet) and a separate NON-BLOCKING 'contract' job running the red-by-design tests (RESEARCH Open Q1 option d)"
    - "The CI red/blocking policy is documented in docs/poc-contract.md"
  artifacts:
    - path: "crates/gnr8-core/tests/snapshot_graph.rs"
      provides: "Red-by-design graph contract test calling analyze::build_graph"
      contains: "build_graph"
    - path: "crates/gnr8-core/tests/snapshot_openapi.rs"
      provides: "Red-by-design openapi contract test calling lower::to_openapi"
      contains: "to_openapi"
    - path: "crates/gnr8-core/tests/snapshot_sdk.rs"
      provides: "Red-by-design sdk contract test calling sdk::generate"
      contains: "generate"
    - path: "crates/gnr8-core/tests/snapshot_diagnostics.rs"
      provides: "Red-by-design diagnostics contract test calling diagnostics::collect"
      contains: "collect"
    - path: "Makefile"
      provides: "fmt/clippy/test/fixture-build/check targets"
      contains: "fixture-build"
    - path: ".github/workflows/ci.yml"
      provides: "Blocking gates job + go-fixture job + non-blocking contract job"
      contains: "continue-on-error"
  key_links:
    - from: "crates/gnr8-core/tests/snapshot_graph.rs"
      to: "gnr8_core::analyze::build_graph"
      via: "test calls the seam and .expect()s, panicking on NotYetImplemented"
      pattern: "analyze::build_graph"
    - from: "crates/gnr8-core/tests/snapshot_graph.rs"
      to: "fixtures/goalservice"
      via: "FIXTURE_DIR points at the Go fixture from Plan 01-02"
      pattern: "fixtures/goalservice"
    - from: ".github/workflows/ci.yml"
      to: "fixtures/goalservice"
      via: "go-fixture job runs go build/vet in the fixture dir"
      pattern: "fixtures/goalservice"
---

<objective>
Wire the local + CI quality gates, the insta snapshot harness, and the four RED-BY-DESIGN contract
tests that define expected graph/OpenAPI/SDK/diagnostics behavior before the analyzer exists.

Purpose: Satisfy RUST-03 (fmt/clippy/test gates wired) and FIX-03/FIX-04 (snapshot tests cover all four
artifact families and fail clearly — not skipped — until Phases 2–3 implement the seams). This plan
depends on the gnr8-core module seams from Plan 01-01 and the Go fixture from Plan 01-02.

Output: four `crates/gnr8-core/tests/snapshot_*.rs` integration tests (red-by-design), a `Makefile`,
a GitHub Actions `ci.yml` (blocking gates + go-fixture jobs + a separate non-blocking "contract" job),
and a CI-policy note appended to `docs/poc-contract.md`.

NOTE: this plan is NOT autonomous — Task 3 is a blocking human-verify checkpoint confirming the phase
end-state (gates green, contract suite visibly red).
</objective>

<execution_context>
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-fixtures/01-CONTEXT.md
@.planning/phases/01-foundation-and-fixtures/01-RESEARCH.md
@.planning/phases/01-foundation-and-fixtures/01-VALIDATION.md
@thoughts/skills/rust-best-practices/references/chapter_05.md
@.planning/phases/01-foundation-and-fixtures/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-fixtures/01-02-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 01-01 (gnr8-core seams). These functions ALL return
     Err(CoreError::NotYetImplemented) today, so a test that .expect()s their Ok value panics →
     the test FAILS (red-by-design). Phases 2-3 implement them → tests turn green on snapshot review. -->

gnr8_core::analyze::build_graph(fixture_dir: &str) -> Result<gnr8_core::graph::ApiGraph, CoreError>
gnr8_core::lower::to_openapi(graph: &gnr8_core::graph::ApiGraph) -> Result<String, CoreError>
gnr8_core::sdk::generate(graph: &gnr8_core::graph::ApiGraph) -> Result<String, CoreError>
gnr8_core::diagnostics::collect(fixture_dir: &str) -> Result<String, CoreError>

graph::ApiGraph derives serde::Serialize (so insta::assert_yaml_snapshot! can render it once green).

Fixture input (from Plan 01-02): fixtures/goalservice/ — referenced via a
CARGO_MANIFEST_DIR-relative const FIXTURE_DIR = concat!(env!("CARGO_MANIFEST_DIR"),
"/../../fixtures/goalservice").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the four red-by-design contract snapshot tests (graph/openapi/sdk/diagnostics)</name>
  <files>crates/gnr8-core/tests/snapshot_graph.rs, crates/gnr8-core/tests/snapshot_openapi.rs, crates/gnr8-core/tests/snapshot_sdk.rs, crates/gnr8-core/tests/snapshot_diagnostics.rs</files>
  <read_first>
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md "Pattern 4: insta snapshot test that is RED BY DESIGN" (lines ~462-496) — the exact test shape, FIXTURE_DIR const, the two red-by-design mechanisms, and the #[ignore] prohibition
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md Pitfall 2 (clippy unwrap in tests, lines ~519-523) and Pitfall 3 (insta auto-create, lines ~525-529)
    - .planning/phases/01-foundation-and-fixtures/01-CONTEXT.md D-13 (insta), D-15 (red-by-design acceptance target, fail clearly not skipped)
    - .planning/phases/01-foundation-and-fixtures/01-VALIDATION.md "Phase Requirements → Test Map" (the four snapshot commands) — confirms per-test commands
    - thoughts/skills/rust-best-practices/references/chapter_05.md — insta snapshots, one assertion per test, test naming
    - The `<interfaces>` block above — the exact seam fns each test calls
  </read_first>
  <action>
    Create four integration tests under `crates/gnr8-core/tests/`. Each file begins with
    `#![allow(clippy::unwrap_used, clippy::expect_used)]` (Pitfall 2 — tests may unwrap; do NOT weaken
    the workspace policy) and defines `const FIXTURE_DIR: &str = concat!(env!("CARGO_MANIFEST_DIR"),
    "/../../fixtures/goalservice");`.

    `snapshot_graph.rs` — `#[test] fn graph_matches_expected_for_goalservice()` calls
    `gnr8_core::analyze::build_graph(FIXTURE_DIR).expect("Phase 2 must implement build_graph;
    red-by-design until then")` and then `insta::assert_yaml_snapshot!("goalservice_graph", graph)`. The
    `.expect()` panics TODAY (seam returns NotYetImplemented) → the test FAILS. This is the primary
    red-by-design mechanism (RESEARCH Pattern 4 note: mechanism 1) and needs no pre-authored `.snap`.

    `snapshot_openapi.rs` — build the graph the same way, then call `gnr8_core::lower::to_openapi(&graph)
    .expect(...)` and `insta::assert_snapshot!("goalservice_openapi", openapi)` (or assert_yaml_snapshot
    if it returns structured data; to_openapi returns String → use assert_snapshot for the YAML text).

    `snapshot_sdk.rs` — build the graph, call `gnr8_core::sdk::generate(&graph).expect(...)` and
    `insta::assert_snapshot!("goalservice_sdk", sdk)`.

    `snapshot_diagnostics.rs` — call `gnr8_core::diagnostics::collect(FIXTURE_DIR).expect(...)` and
    `insta::assert_snapshot!("goalservice_diagnostics", diags)` (plain-text diagnostics per RESEARCH).

    CRITICAL (FIX-04): do NOT add `#[ignore]` to ANY of these tests, and do NOT pre-create their `.snap`
    files. The redness must come from the real failing `.expect()` on the stubbed seam. Each test names
    the responsible phase in its `.expect()` message so the failure is self-documenting. Keep one
    behavioral assertion per test (skill ch.5).
  </action>
  <verify>
    <automated>cargo test -p gnr8-core --test snapshot_graph 2>&1 | grep -qiE 'FAILED|panicked|not yet implemented' && ! grep -rqn '#\[ignore' crates/gnr8-core/tests/ && cargo clippy -p gnr8-core --tests --all-features -- -D warnings 2>&1 | tail -1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p gnr8-core --test snapshot_graph 2>&1 | grep -qiE 'FAILED|panicked'` succeeds (the graph test FAILS red-by-design today)
    - Same red result for `--test snapshot_openapi`, `--test snapshot_sdk`, `--test snapshot_diagnostics`
    - `grep -rn '#\[ignore' crates/gnr8-core/tests/` returns NOTHING (FIX-04: no silent skips)
    - `ls crates/gnr8-core/tests/snapshots/*.snap 2>/dev/null` returns nothing (no pre-authored snapshots — redness comes from the stub, not a missing-file race)
    - `cargo clippy -p gnr8-core --tests --all-features -- -D warnings` exits 0 (the scoped allow keeps test unwraps clippy-clean)
  </acceptance_criteria>
  <done>Four red-by-design contract tests exist, each calls its gnr8-core seam, fails clearly today via a panicking .expect() (no #[ignore], no pre-authored .snap), and is clippy-clean. They turn green on snapshot review in Phases 2-3.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Makefile and the GitHub Actions CI workflow (blocking gates + non-blocking contract job)</name>
  <files>Makefile, .github/workflows/ci.yml, docs/poc-contract.md</files>
  <read_first>
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md "Makefile" (lines ~549-573) and "GitHub Actions CI" (lines ~575-602) — exact target/job shapes
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md Open Q1 + Phase-1 CI reality callout (lines ~604, ~697-700) — option (d): blocking gates job + separate non-blocking contract job; promote contract to blocking in Phase 3
    - .planning/phases/01-foundation-and-fixtures/01-CONTEXT.md D-16 (fmt-check + clippy --locked -D warnings + test, Makefile + GitHub Actions)
    - .planning/phases/01-foundation-and-fixtures/01-RESEARCH.md Pitfall 4 (Cargo.lock + --locked) and Pitfall 5 (Go fixture compile gated in CI)
  </read_first>
  <action>
    Write `Makefile` with `.PHONY` targets (RESEARCH Makefile example, D-16): `fmt` (`cargo fmt`),
    `fmt-check` (`cargo fmt --all -- --check`), `clippy` (`cargo clippy --all-targets --all-features
    --locked -- -D warnings`), `test` (`cargo test --all-features`), `fixture-build` (`cd
    fixtures/goalservice && go build ./... && go vet ./...`), and `check: fmt-check clippy test
    fixture-build` (full local gate mirroring CI), plus `all: check`. Note: the `test` target WILL
    surface the four red-by-design failures locally — that is expected and is how a developer sees the
    contract (per VALIDATION.md sampling notes).

    Write `.github/workflows/ci.yml` implementing RESEARCH Open Q1 OPTION (d) — three jobs:
    1. `gates` (BLOCKING, the RUST-03 gate): checkout, `dtolnay/rust-toolchain@stable` with
       `components: rustfmt, clippy`, `Swatinem/rust-cache@v2`, then `cargo fmt --all -- --check`,
       `cargo clippy --all-targets --all-features --locked -- -D warnings`, and a `cargo test` that
       EXCLUDES the four red-by-design contract tests (run library/unit + the binary parse tests only —
       e.g. `cargo test -p gnr8-core --lib && cargo test -p gnr8` so the gates job stays green). This job
       blocks merges.
    2. `go-fixture` (BLOCKING): checkout, `actions/setup-go@v5` (`go-version: '1.26'`), then
       `cd fixtures/goalservice && go build ./... && go vet ./...` (Pitfall 5).
    3. `contract` (NON-BLOCKING): `continue-on-error: true`, same Rust toolchain, runs the four
       red-by-design tests explicitly (`cargo test -p gnr8-core --test snapshot_graph --test
       snapshot_openapi --test snapshot_sdk --test snapshot_diagnostics`). It is VISIBLY RED but does not
       block merges; a comment in the workflow notes it is promoted to blocking when Phase 3 completes.
    Trigger on `push` to main + `pull_request`.

    APPEND a "CI policy" section to `docs/poc-contract.md` (created by Plan 01-01): document option (d) —
    the blocking `gates`/`go-fixture` jobs enforce RUST-03; the non-blocking `contract` job runs the four
    red-by-design snapshot tests (FIX-04 — visibly red, never `#[ignore]`d) and is promoted to blocking
    in Phase 3 once the analyzer + lowering land. State that local `make check` shows the same redness.
  </action>
  <verify>
    <automated>grep -q 'fixture-build' Makefile && grep -q 'cargo clippy --all-targets --all-features --locked -- -D warnings' Makefile && grep -q 'continue-on-error' .github/workflows/ci.yml && grep -q 'fixtures/goalservice' .github/workflows/ci.yml && grep -qi 'contract\|red-by-design\|non-blocking' docs/poc-contract.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `make check` runs fmt-check, clippy, test, and fixture-build in sequence (target exists: `grep -q '^check:' Makefile`)
    - `grep -q 'cargo clippy --all-targets --all-features --locked -- -D warnings' Makefile` (exact D-16 clippy invocation, incl. `--locked` — Pitfall 4)
    - `.github/workflows/ci.yml` defines a blocking gates job, a `go-fixture` job running `go build`/`go vet` in `fixtures/goalservice`, and a `contract` job with `continue-on-error: true` (option d)
    - `docs/poc-contract.md` now documents the blocking-vs-non-blocking CI policy and the Phase-3 promotion (Open Q1)
  </acceptance_criteria>
  <done>Makefile exposes fmt/clippy(--locked,-D warnings)/test/fixture-build/check; CI has blocking gates + go-fixture jobs and a separate non-blocking contract job for the red-by-design suite; the policy is documented in the PoC contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full phase gate state (gates green, contract suite visibly red)</name>
  <what-built>
    Plan 01-01 (workspace + CLI + contract doc), Plan 01-02 (Go Gin fixture + expected/ scaffolds), and
    Plan 01-03 (red-by-design tests + Makefile + CI) together complete Phase 1. This checkpoint confirms
    the intended end-state: quality gates pass, the fixture compiles, and the four contract tests are
    visibly RED (not skipped) — the red-by-design contract the rest of the milestone builds against.
  </what-built>
  <how-to-verify>
    Run, from the repo root:
    1. `make fmt-check` → no diffs (exit 0).
    2. `make clippy` → exits 0 (no warnings; RUST-04 lints satisfied).
    3. `make fixture-build` → Go fixture builds + vets clean (exit 0).
    4. `cargo test -p gnr8-core --lib && cargo test -p gnr8` → green (unit + CLI parse tests pass).
    5. `cargo test -p gnr8-core --test snapshot_graph --test snapshot_openapi --test snapshot_sdk --test snapshot_diagnostics` → these FAIL (panic: "not yet implemented (arrives in phase N)"). This redness is CORRECT and required (FIX-04).
    6. `grep -rn '#\[ignore' crates/gnr8-core/tests/` → returns nothing (no silent skips).
    7. `cargo run -p gnr8 -- --help` and `cargo run -p gnr8 -- inspect --help` → show the full command surface.
    Confirm: gates 1–4 green, step 5 visibly red (4 failing tests), steps 6–7 as described.
  </how-to-verify>
  <resume-signal>Type "approved" if gates are green and the four contract tests are visibly red (not skipped); otherwise describe which check deviated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo source | GitHub Actions executes build/test on pushed code (pinned actions) |
| developer → cargo/go toolchains | gates run pinned toolchains; supply chain enters at install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | CI red-by-design suite hides a real regression behind expected redness | mitigate | The contract job is SEPARATE and non-blocking; the BLOCKING gates job runs fmt/clippy/go-build + genuinely-green unit/parse tests, so a real regression (compile/clippy/unit failure) still blocks. Promotion of the contract job to blocking is scheduled for Phase 3 (documented in poc-contract.md). |
| T-03-02 | Tampering | GitHub Actions third-party actions (checkout/setup-go/rust-toolchain/rust-cache) | mitigate | Pin to major-version tags from well-known publishers (actions/*, dtolnay, Swatinem) per RESEARCH CI example; no unpinned/forked actions. |
| T-03-03 | DoS | insta auto-accepting an empty snapshot turns a red test falsely green | mitigate | Primary red mechanism is a panicking `.expect()` on the stubbed seam (fires BEFORE assert_*), which `cargo insta accept` cannot silence; CI uses plain `cargo test` (INSTA_UPDATE=no). No `.snap` is pre-authored (RESEARCH Pitfall 3). |
| T-03-SC | Tampering | npm/pip/cargo installs in CI | mitigate | No new package installs introduced here; deps already pinned via `[workspace.dependencies]` + committed `Cargo.lock` (`--locked`). No `[ASSUMED]`/`[SUS]` packages → no legitimacy checkpoint required (RESEARCH audit). |
</threat_model>

<verification>
- `make check` runs the full local gate (fmt-check + clippy --locked -D warnings + test + fixture go build/vet).
- The four `snapshot_*` tests FAIL clearly (panic naming the responsible phase); none is `#[ignore]`d (FIX-04).
- `.github/workflows/ci.yml` separates blocking gates (RUST-03) from the non-blocking contract suite (option d).
- The Go fixture is compiled in CI (Pitfall 5 mitigated).
</verification>

<success_criteria>
- Snapshot tests cover graph, OpenAPI, Go SDK, and diagnostics (FIX-03) and fail clearly before behavior is implemented (FIX-04).
- fmt/clippy/test quality gates are wired locally (Makefile) and in CI, with clippy denying warnings (RUST-03).
- CI reconciles RUST-03 (gates pass) with FIX-04 (contract tests visibly red) via Open Q1 option (d).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-fixtures/01-03-SUMMARY.md` when done.
</output>
