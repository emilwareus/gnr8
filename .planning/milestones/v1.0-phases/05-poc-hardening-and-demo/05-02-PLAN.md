---
phase: 05-poc-hardening-and-demo
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/demo.md
  - docs/evidence.md
autonomous: false
requirements: [HARD-02, HARD-03]
must_haves:
  truths:
    - "A developer can follow docs/demo.md from a fresh checkout and reproduce: build gnr8 → scratch-copy the fixture → init → generate → see OpenAPI + Go SDK → edit a Go source file → re-generate → see outputs update"
    - "The demo never mutates the committed fixture (it operates on a scratch copy)"
    - "docs/evidence.md asserts make check GREEN (captured from a real run) and maps all 37 v1 requirements to where each is satisfied"
    - "docs/evidence.md references the benchmark numbers from scripts/bench.sh and the doctor command from 05-01"
  artifacts:
    - path: "docs/demo.md"
      provides: "copy-pasteable fresh-checkout → source-edit → updated-outputs walkthrough on a scratch fixture copy"
      min_lines: 60
      contains: "mktemp"
    - path: "docs/evidence.md"
      provides: "milestone HARD-03 sign-off: make check green + 37-requirement traceability table"
      min_lines: 60
      contains: "make check"
  key_links:
    - from: "docs/demo.md"
      to: "fixtures/goalservice (scratch copy)"
      via: "cp -R / mktemp -d, never in place"
      pattern: "cp -R"
    - from: "docs/evidence.md"
      to: ".planning/REQUIREMENTS.md"
      via: "37 v1 requirement IDs → satisfied-by file/test"
      pattern: "HARD-0"
    - from: "docs/demo.md"
      to: "gnr8 doctor"
      via: "demo references the doctor command from 05-01"
      pattern: "doctor"
---

<objective>
Write the headline reproducible demo (`docs/demo.md`) and the final milestone verification evidence
(`docs/evidence.md`). This satisfies HARD-02 (a documented demo showing Go source changing → OpenAPI
updating → Go SDK updating) and the verification half of HARD-03 (all tests/snapshots/quality gates
pass; 37 v1 requirements mapped to where satisfied). This is the final plan of the milestone — the
ready-for-review sign-off backing the milestone audit.

Purpose: Make the PoC reviewable. The demo is the headline "source edit → updated OpenAPI + SDK" story
reproducible from a fresh checkout; the evidence is the proof every v1 requirement is met and every gate
is green.

Output: `docs/demo.md` (new), `docs/evidence.md` (new). Documentation + verification only — no code
changes. Depends on 05-01 (the demo references `gnr8 doctor` and `scripts/bench.sh`).
</objective>

<execution_context>
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/Users/emilwareus/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-poc-hardening-and-demo/05-CONTEXT.md
@.planning/phases/05-poc-hardening-and-demo/05-RESEARCH.md
@.planning/phases/05-poc-hardening-and-demo/05-VALIDATION.md

# 05-01 deliverables the demo + evidence reference (read its SUMMARY for the exact doctor/bench surface):
@.planning/phases/05-poc-hardening-and-demo/05-01-SUMMARY.md

# The exact gate the evidence asserts green + the demo subject:
@Makefile
@.github/workflows/ci.yml

<interfaces>
<!-- Facts the demo/evidence must use verbatim. Extracted from the codebase. -->

The CLI surface (from crates/gnr8/src/cli.rs — use these exact command names):
- `gnr8 init` — scaffolds `.gnr8/` (config.toml + .gitignore + cache dir).
- `gnr8 generate [--force]` — generates OpenAPI + Go SDK; `--json` is a global flag.
- `gnr8 watch [--debounce-ms N]` — watches source, regenerates on change (cold scenario at startup).
- `gnr8 check` — dry-run; exits 1 on drift.
- `gnr8 inspect routes|schemas|graph [DIR]` — explains inferred facts.
- `gnr8 doctor [--json]` — (NEW in 05-01) grouped health report; exit 0 healthy / 1 actionable.

The default config `gnr8 init` writes (from crates/gnr8-core/src/workspace/mod.rs — the demo's scratch
copy IS the Go module, so the defaults work as-is):
- `inputs = ["."]`, `output.openapi = "openapi.yaml"`, `output.sdk_dir = "sdk"`, `output.go_module = "example.com/yourservice/sdk"`.

The demo subject (from fixtures/goalservice — DO NOT mutate in place; it has `expected/` golden + is CI-gated):
- A concrete single-field source edit target: `internal/common/dto/goal.go`, struct `CreateGoalInput` (the `TargetValue *float64` field lives here) — append one JSON-tagged field to show the OpenAPI schema + SDK model updating.
- `*.go` source under `internal/`; the fixture is a standalone Go module (its own go.mod).

The gate the evidence asserts (from Makefile):
- `make check` = `fmt-check clippy test fixture-build goextract-build` (the FULL local gate).
- `make gates` = the blocking subset (lib + bin tests + 4 contract snapshots + determinism + sdk_compile + lifecycle).
- Both require the Go toolchain (go 1.26.2 present) — they invoke `go run`/`gofmt`/`go build`.

The 37 v1 requirements to map (from .planning/REQUIREMENTS.md "## v1 Requirements" + "## Traceability"):
- POC-01..03, RUST-01..04, FIX-01..04, GO-01..06, GRAPH-01..03, OAPI-01..03, SDK-01..05, WS-01..04, WATCH-01..03, HARD-01..03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/demo.md — reproducible fresh-checkout → source-edit → updated-outputs walkthrough</name>
  <files>docs/demo.md</files>
  <action>Create `docs/demo.md` (HARD-02 / D-04): a copy-pasteable walkthrough a developer can run from a fresh checkout. Structure it as numbered sections: (1) Prerequisites — Rust (cargo >= 1.85) + Go (go 1.26) on PATH. (2) Build — `cargo build --release -p gnr8`; note the binary at `target/release/gnr8`. (3) Make a SCRATCH COPY — `WORK="$(mktemp -d)"; cp -R fixtures/goalservice "$WORK/svc"; cd "$WORK/svc"`, with an explicit callout that the committed `fixtures/goalservice` MUST NOT be used in place (it has an `expected/` golden dir and is a CI-gated Go module — Pitfall 2). (4) Initialize — `gnr8 init` (scaffolds `.gnr8/`; show the default config knobs `inputs = ["."]`, `output.openapi = "openapi.yaml"`, `output.sdk_dir = "sdk"`). (5) First generate — `gnr8 generate` (cold), then show the produced artifacts: `head openapi.yaml` (paths/operations/schemas) and `ls sdk/` plus a peek at a generated SDK model + client (the OpenAPI + compiling Go SDK from source). (6) Diagnose — `gnr8 doctor` (the 05-01 command): explain it reports the fixture's informational unsupported-pattern WARNs with `file:line` AND lifecycle/staleness, exiting 0 when healthy. (7) THE HEADLINE EDIT — edit one Go source file: add a JSON-tagged field (e.g. `BenchField string` with a json tag `benchField,omitempty`) to the `CreateGoalInput` struct in `internal/common/dto/goal.go`; show the exact before/after of the struct. (8) Re-generate — `gnr8 generate` again and show the new field now appears in `openapi.yaml` and the corresponding SDK model under `sdk/` (the "source edit -> updated OpenAPI + SDK" payoff); note only affected outputs were rewritten and unchanged ones were skipped (WATCH-01 no-op). (9) Watch (optional) — `gnr8 watch` runs a cold regen + prints latency, an edit triggers a single-file-edit regeneration, Ctrl-C stops. (10) Benchmark — point at `scripts/bench.sh` for reproducible cold / warm-no-op / single-file-edit numbers. Each step shows the EXACT command and the expected output SHAPE (not exact bytes — note outputs are deterministic but paths are scratch-dir-specific). Document expected behavior in prose; do NOT inline large generated files. NO heredoc to write the file (use the Write tool).</action>
  <verify>
    <human-check>
      What was built: docs/demo.md — a reproducible fresh-checkout demo.
      How to verify (run on a scratch copy, NOT the committed fixture):
      1. From the repo root, follow docs/demo.md exactly: build, cp -R the fixture into a mktemp dir, cd in, gnr8 init, gnr8 generate.
      2. Confirm openapi.yaml and sdk/*.go are produced.
      3. Run gnr8 doctor — confirm it prints a grouped report and the informational WARNs, exit 0.
      4. Make the documented field edit to internal/common/dto/goal.go, re-run gnr8 generate, confirm the new field appears in openapi.yaml and the SDK model.
      5. Back in the repo, run git status — confirm NO changes under fixtures/goalservice/.
    </human-check>
    <automated>test -f /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/demo.md && grep -q "mktemp" /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/demo.md && grep -q "cp -R" /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/demo.md && grep -q "gnr8 doctor" /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/demo.md && grep -q "CreateGoalInput" /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/demo.md</automated>
  </verify>
  <done>`docs/demo.md` exists, is copy-pasteable from a fresh checkout, uses a `mktemp`/`cp -R` scratch copy (never the committed fixture), walks init → generate → doctor → source-edit → re-generate with the new field appearing in both OpenAPI and SDK, and references `scripts/bench.sh`. The five grep gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: docs/evidence.md — make check GREEN capture + 37-requirement traceability sign-off</name>
  <files>docs/evidence.md</files>
  <action>Create `docs/evidence.md` (HARD-03 / D-05): the final milestone verification artifact, GENERATED from a real gate run (not from memory — Pitfall 6). Steps the executor performs: (1) RUN `make check` in this session and capture the actual result (each sub-target: fmt-check, clippy -D warnings, test (lib + bin + 4 contract snapshots + determinism + sdk_compile + lifecycle), fixture-build, goextract-build) — record PASS/FAIL per gate from the real output; if any gate is RED, STOP and report it (the milestone is not done until green). (2) Write `docs/evidence.md` containing: a header stating the milestone (v1.0) and the date; a "Gate Results" section listing each `make check` sub-gate with its captured PASS status and the command that produced it; a "Requirement Traceability" table with one row per v1 requirement (all 37: POC-01..03, RUST-01..04, FIX-01..04, GO-01..06, GRAPH-01..03, OAPI-01..03, SDK-01..05, WS-01..04, WATCH-01..03, HARD-01..03) mapping each ID -> the file(s)/test(s) where it is satisfied (cross-reference .planning/REQUIREMENTS.md "## Traceability" for the phase mapping, and name the concrete artifact: e.g. SDK-05 -> `crates/gnr8-core/tests/sdk_compile.rs`; GRAPH-02 -> `crates/gnr8-core/tests/determinism.rs`; WATCH-03 -> `crates/gnr8/src/watch.rs` LatencyReport + `scripts/bench.sh`; HARD-01 -> `crates/gnr8/src/doctor.rs` + `run_doctor`; HARD-02 -> `docs/demo.md`; HARD-03 -> this evidence + `make check`); a "Benchmarks" section referencing `scripts/bench.sh` and recording the representative cold / warm-no-op / single-file-edit numbers captured from a run on THIS machine, explicitly labeled environment-dependent and reproducible-via-`scripts/bench.sh` (never asserted as thresholds — Pitfall 3); and a "Sign-off" line stating all 37 v1 requirements are satisfied and `make check` is green, ready for the milestone audit. Verify the table has exactly 37 rows / 37 distinct requirement IDs. NO heredoc to write the file (use the Write tool).</action>
  <verify>
    <human-check>
      What was built: docs/evidence.md — the milestone HARD-03 sign-off.
      How to verify:
      1. Confirm `make check` was actually run this session and is GREEN (re-run `make check` if in doubt — it is the authoritative gate).
      2. Confirm docs/evidence.md lists each make check sub-gate as PASS with the producing command.
      3. Confirm the traceability table has all 37 v1 requirement IDs, each mapped to a concrete file/test.
      4. Confirm the Benchmarks section references scripts/bench.sh with representative, clearly-labeled-environment-dependent numbers.
    </human-check>
    <automated>test -f /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/evidence.md && grep -q "make check" /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/evidence.md && grep -q "scripts/bench.sh" /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/evidence.md && for id in POC-01 RUST-04 FIX-04 GO-06 GRAPH-02 OAPI-03 SDK-05 WS-04 WATCH-03 HARD-01 HARD-02 HARD-03; do grep -q "$id" /Users/emilwareus/conductor/workspaces/gnr8/tripoli-v7/docs/evidence.md || { echo "MISSING $id"; exit 1; }; done; echo "all-ids-present"</automated>
  </verify>
  <done>`docs/evidence.md` exists, was generated from a real `make check` run (captured PASS per sub-gate), maps all 37 v1 requirement IDs to concrete files/tests, references `scripts/bench.sh` with labeled representative numbers, and carries the ready-for-review sign-off. The id-presence grep gate prints `all-ids-present`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo reader → committed fixture | a developer copy-pasting the demo could mutate `fixtures/goalservice` in place if not steered to a scratch copy |
| evidence claim → real gate state | the evidence asserts gate status; the risk is asserting green without running the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | demo mutating committed fixture | mitigate | The demo's FIRST post-build step is `cp -R fixtures/goalservice "$(mktemp -d)/svc"` with an explicit "do not run in place" callout; the committed `expected/` golden + CI-gated module stay pristine (Pitfall 2). Verified by `git status` showing no fixture changes after a demo run. |
| T-05-02-02 | Repudiation / Spoofing | evidence asserting green without a real run | mitigate | `docs/evidence.md` is GENERATED from an actual `make check` run captured this session, not from memory (Pitfall 6); the verify step re-runs the gate if in doubt. |
| T-05-02-03 | Tampering | benchmark numbers presented as exact thresholds | accept | Numbers are captured as REPRESENTATIVE + environment-dependent + reproducible-via-`scripts/bench.sh`; no CI threshold is asserted (Pitfall 3). Accepted: wall-clock is inherently env-dependent and informational. |
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase — docs only (RESEARCH Package Legitimacy Audit: zero new dependencies). No install task; nothing to gate. |
</threat_model>

<verification>
Per-task `<verify>` blocks are authoritative (each carries both an `<automated>` grep gate and a
`<human-check>` per `human_verify_mode: end-of-phase`). Plan-level checks before declaring done:
- `docs/demo.md` and `docs/evidence.md` both exist and pass their grep gates.
- `make check` is GREEN this session (the authoritative HARD-03 gate; evidence is generated from it).
- A demo dry-run on a scratch copy produces OpenAPI + SDK, the documented edit updates both, and
  `git status` shows NO changes under `fixtures/goalservice/`.
- The evidence traceability table covers all 37 v1 requirement IDs.
- The WHOLE workspace ends GREEN — this is the milestone gate.
</verification>

<success_criteria>
- HARD-02: `docs/demo.md` is a copy-pasteable, fresh-checkout-reproducible demo showing Go source
  changing → OpenAPI updating → Go SDK updating, run on a scratch fixture copy (committed fixture never
  mutated), referencing `gnr8 doctor` and `scripts/bench.sh` from 05-01.
- HARD-03 (verification half): `docs/evidence.md` asserts `make check` GREEN (captured from a real run)
  and maps all 37 v1 requirements to where each is satisfied — the ready-for-review milestone sign-off.
- Documentation + verification only; no code changes; no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-poc-hardening-and-demo/05-02-SUMMARY.md` when done.
</output>
