---
milestone: v1.0
audited: 2026-06-25
status: passed
scores:
  requirements: 38/38
  phases: 5/5
  integration: 14/14 seams wired
  flows: 6/6 E2E flows complete
gaps: {}
tech_debt:
  - phase: 02-go-analysis-and-api-graph
    items:
      - "goextract helper path is compile-time-baked (concat!(CARGO_MANIFEST_DIR, '/../../goextract')) — works in-repo for the PoC; a relocated/installed binary would not find the Go sidecar. v2 portability note (ADV/install packaging)."
      - "diagnostics::collect (standalone module fn) is production-orphaned — consumed only by snapshot_diagnostics test; live diagnostics flow through graph.diagnostics + lifecycle::diagnostics_only. Redundant computation; harmless. Consider removing or routing the live path through it in v2."
  - phase: 01-foundation-and-fixtures
    items:
      - "01-VALIDATION.md frontmatter still nyquist_compliant: false (never flipped to true after planning). Doc-hygiene only; phase passed verification and gates are green."
  - phase: 04-gnr8-lifecycle-and-watch-mode
    items:
      - "naming.types collision/cycle rejection (WR-02 fix) changes generation behavior on conflicting config — accepted as a correctness improvement; flagged for human awareness."
  - phase: 05-poc-hardening-and-demo
    items:
      - "Review IN-items deferred (duplicate verdict predicates kept in sync, a misleading comment, bench.sh awk-anchor guard) — cosmetic, out of critical_warning scope."
nyquist:
  compliant_phases: [02, 03, 04, 05]
  partial_phases: [01]
  missing_phases: []
  overall: "4/5 compliant; Phase 1 VALIDATION frontmatter stale (false), phase itself verified passed"
---

# Milestone v1.0 — Audit (gnr8: Go → OpenAPI → Go SDK PoC)

**Status: PASSED.** All 38 v1 requirements satisfied, all 5 phases verified `passed`, all cross-phase
seams wired, the headline E2E flow proven live, `make check` green. Minor tech debt (below) is
documented and out of the PoC's definition of done.

## Requirements Coverage (3-source cross-reference)

All 38 v1 requirement IDs are **satisfied** — confirmed across phase VERIFICATION.md (status passed),
SUMMARY frontmatter (requirements-completed), and REQUIREMENTS.md traceability (`[x]`, 0 unchecked).

| Group | IDs | Phase | Status |
|-------|-----|-------|--------|
| PoC contract | POC-01/02/03 | 1 | satisfied (docs/poc-contract.md) |
| Rust foundation | RUST-01/02/03/04 | 1 | satisfied |
| Fixtures | FIX-01/02/03/04 | 1 | satisfied |
| Go analysis | GO-01..06 | 2 | satisfied (goextract → ApiGraph) |
| API graph | GRAPH-01/02/03 | 2 | satisfied (stable IDs, inspect) |
| OpenAPI | OAPI-01/02/03 | 3 | satisfied (valid 3.1.0) |
| Go SDK | SDK-01..05 | 3 | satisfied (compiles + httptest) |
| Workspace | WS-01..04 | 4 | satisfied (init, ownership) |
| Watch | WATCH-01/02/03 | 4 | satisfied (no-op, loop-safe, latency) |
| Hardening | HARD-01/02/03 | 5 | satisfied (doctor, demo, evidence) |

No unsatisfied, partial, or orphaned requirements. FAIL gate not triggered.

## Phase Verification Summary

| Phase | Status | Notes |
|-------|--------|-------|
| 1. Foundation And Fixtures | passed | workspace + CLI + Gin fixture + 4 contract tests |
| 2. Go Analysis And API Graph | passed | goextract + ApiGraph + inspect + diagnostics (4 review warnings fixed) |
| 3. OpenAPI And Go SDK Generation | passed | lower + sdk; codegen generality fixes (HttpError→graph-derived) |
| 4. `.gnr8` Lifecycle And Watch Mode | passed | gap-closed (no-op output-exclusion) + 4 warnings fixed |
| 5. PoC Hardening And Demo | passed | doctor + bench + demo + evidence; 2 doctor warnings fixed |

## Cross-Phase Integration (live-verified)

**Verdict: INTEGRATION OK.** 14/14 seams wired; full E2E trace verified with real commands:
`init → config → generate (build_graph[goextract] → exclude_output_paths → to_openapi + sdk::generate →
plan_writes → apply_writes → blake3 manifest) → check → doctor → edit Go field → generate (only the 2
affected files rewritten; field in BOTH openapi.yaml + sdk/models.go) → watch (latency) → make check`.
The CLI-generated SDK compiles (`go build`, GOPROXY=off) and passes an httptest round-trip incl. a
404→`*APIError` path. No `NotYetImplemented` reachable on any live command path.

### Integration warnings (non-blocking, → tech debt)
- `diagnostics::collect` production-orphaned (test-only); live diagnostics covered via `graph.diagnostics`.
- `goextract` helper path compile-time-baked — works in-repo; relocatable-install portability is a v2 note.

## Nyquist Coverage

4/5 phases `nyquist_compliant: true`; Phase 1's VALIDATION frontmatter is stale (`false`) — doc hygiene
only (the phase passed verification and gates are green). No action required for milestone completion.

## Definition of Done — met

- [x] All v1 requirements mapped to roadmap phases and satisfied (38/38).
- [x] Phase tests and fixture snapshots pass (all 4 contract tests green + blocking).
- [x] Rust formatting, tests, and clippy gates pass (`make check` exit 0).
- [x] The demo workflow is documented and reproducible from a fresh checkout (docs/demo.md, verified).
- [x] No implementation scope exceeded the Go-to-Go PoC (deferred items correctly v2-scoped).

**Recommendation:** Proceed to complete the milestone. Tech-debt items are minor, documented, and
appropriate to defer to v2.
