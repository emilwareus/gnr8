openapi: 3.1.0
info:
  title: Brownfield Shared Schemas
  version: 1.0.0
paths: {}
components:
  schemas:
    Shared.Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
