//! `format!`-based Python SDK emitters (D-05: no template engine; small internal templating only).
//!
//! Filled in by Tasks 2 and 3 of plan 03-01. This stub keeps the crate compiling once `mod emit;` is
//! declared in [`super`]; the emitters (`py_type`, `emit_models`, `emit_client`, `emit_errors`,
//! `emit_operations`, `emit_init`) and their unit tests arrive next.
