# SDK Reference

Generated by `gnr8` for `sdk` (Python).

## Operations

| Method | Path | Operation | Request | Responses |
|--|--|--|--|--|
| GET | `/orders/` | `list_orders` | `-` | 200 |
| POST | `/orders/` | `create_order` | `app.dto.OrderInput` | 201 |
| POST | `/orders/raw` | `create_order_raw` | `-` | 201 |
| GET | `/orders/{order_id}` | `get_order` | `-` | 200 |

## Operation Documentation

### `list_orders`

- Summary: List orders, optionally narrowed to one stock status.

Orders are returned newest first. Omit the status filter to list every order
regardless of availability.

### `create_order`

- Summary: Place a new order.

The order is confirmed immediately and its confirmation number is returned.

### `create_order_raw`

- Summary: Place an order from a raw, unvalidated payload.

Provided for clients that cannot produce the typed order shape. The payload is
accepted as-is and validated downstream.

### `get_order`

- Summary: Fetch one order by its identifier.


## Schemas

| Schema | Kind |
|--|--|
| `Availability` | enum |
| `OrderConfirmation` | object |
| `OrderInput` | object |
| `Price` | object |

## Diagnostics

- WARN: untyped query param 'q' on GET /orders/: read via request.args.get with no annotation; param type/required-ness under-specified, type inferred as string only (app/routes.py:37)
- WARN: untyped request body on POST /orders/raw: read via request.json with no typed DTO; body shape under-specified, no schema inferred (app/routes.py:66)
