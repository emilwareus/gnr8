# Bookstore API TypeScript SDK

This directory is generated by `gnr8`. Do not edit generated files directly; edit `.gnr8/src/main.rs` or the source service, then run `gnr8 generate`.

## Package

- Language: TypeScript
- Package/module: `sdk`
- Base path: `/`
- Operations: 4
- Schemas: 8

## Agent workflow

1. Read `reference.md` in this directory for operation and schema names.
2. Construct the generated `Client` with `{ baseUrl }`.
3. Pass typed request models and path/query parameters according to the generated method signatures.
4. Handle generated `ApiError` values for non-2xx responses.

## Retries and timeouts

`timeoutMs` bounds each individual attempt, not the whole call: with retries, total wall time can reach `timeoutMs x (maxRetries + 1)` plus the waits between them. Total time spent waiting between retries is capped at 60s, so a longer `Retry-After` is honoured only up to that cap. Pass `signal` to interrupt a retry wait.

## TypeScript quick start

```typescript
import { Client } from './client';
const client = new Client({ baseUrl: 'https://api.example.com' });
// Call generated async methods with typed request objects.
```
