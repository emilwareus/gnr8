# Publishing Go SDK

Package: `example.com/bookstore/sdk`
Version: `0.1.0`

`gnr8` never stores registry credentials and never uploads packages. Run these commands in this generated SDK directory after reviewing the generated files.

1. `go test ./...`
2. `go vet ./...`
3. Tag and publish from your repository using your normal Go module release process.
