# Bookstore API Go SDK

This directory is generated by `gnr8`. Do not edit generated files directly; edit `.gnr8/src/main.rs` or the source service, then run `gnr8 generate`.

## Package

- Language: Go
- Package/module: `sdk`
- Base path: `/`
- Operations: 5
- Schemas: 8

## Agent workflow

1. Read `reference.md` in this directory for operation and schema names.
2. Construct the generated `Client` with the service base URL.
3. Pass typed request models and path/query parameters according to the generated method signatures.
4. Handle generated `*APIError` values for rejected HTTP responses.

## Retries and timeouts

`WithTimeout` bounds the whole call, including retries and the waits between them. Total time spent waiting between retries is capped at 60s, so a longer `Retry-After` is honoured only up to that cap. Cancelling the context interrupts a retry wait.

## Go quick start

```go
client := sdk.NewClient("https://api.example.com")
// Call methods on client with context.Context first.
```
